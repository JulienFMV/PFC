---
phase: 05-msfc-log-prix-retire-silent-floors
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - pfc_shaping/lt/model/msfc_spline.py
  - pfc_shaping/calibration/arbitrage_free.py
  - tests/test_phase05_negative_prices.py
  - tests/conftest.py
autonomous: true
requirements:
  - NEG-01
  - NEG-02
  - NEG-05
requirements_addressed:
  - NEG-01
  - NEG-02
  - NEG-05
must_haves:
  truths:
    - "REQUIREMENTS.md NEG-05 wording is reformulated per D-A4-7 — from 'Cal'27 forward coté -10 €/MWh' to 'Un monthly forward négatif (e.g. July M-07'27 = -2 €/MWh, autres months positifs) est correctement repricé par la PFC à -2 €/MWh moyenne sur le mois (math invariance test, vérifie l'absence des floors silencieux)'."
    - "`pfc_shaping/lt/model/msfc_spline.py::smooth_base_prices` exposes a new kwarg `enforce_positivity: bool = False` (default OFF = negative-ready per D-A2-1). The kwarg is PROPAGATED to `_enforce_mean_constraints` so the floor at line 203 is also conditional (RESEARCH Pitfall 1 — TWO MSFC floors, not one)."
    - "When `enforce_positivity=False` (default), neither line 131 `B_smooth = np.maximum(B_smooth, 1.0)` in `smooth_base_prices` nor line 203 `return np.maximum(result, 1.0)` in `_enforce_mean_constraints` clamps the result. When `enforce_positivity=True` (legacy rollback path), both floors are restored exactly."
    - "The extrapolation clamp at `msfc_spline.py:120` is rewritten signed-aware per D-A1-2: `margin = 0.5 * np.ptp(y_knots)` then `np.clip(B_smooth_raw, y_knots.min() - margin, y_knots.max() + margin)`. The old formula `np.clip(B_smooth_raw, y_knots.min()*0.5, y_knots.max()*2.0)` is removed because it produces inverted bounds (lo > hi) for all-negative knots."
    - "`pfc_shaping/calibration/arbitrage_free.py::ArbitrageFreeCalibrator.__init__` accepts a new kwarg `enforce_m_factor_floor: bool = False` (default OFF per D-A2-1) stored on `self.enforce_m_factor_floor`. When False, the `m_factor = np.maximum(m_factor, 0.1)` clip at line 517 is skipped; when True, the clip is applied AND `converged=False` is propagated whenever the clip actually mutates `m_factor` (NEG-02 literal per D-A1-3 and RESEARCH Pitfall 3)."
    - "Each clip application under `enforce_m_factor_floor=True` emits `logger.warning(\"m_factor floor 0.1 applied at %d timestamps (enforce_m_factor_floor=True)\", n_clipped)` and forces `converged = False` regardless of `max_abs_residual`."
    - "`tests/test_phase05_negative_prices.py` is created in this plan with stubs for ALL seven Phase 5 tests listed in VALIDATION.md (`test_msfc_signed_monthly_repricing`, `test_arbitrage_free_signed_target`, `test_water_value_delta_sign_invariant`, `test_assembler_delta_additive`, `test_cascading_spread_signed_base`, `test_fit_peak_ratios_deprecated`, `test_master_flag_audit_log`, `test_phase05_summer_bowl_negative_acceptance`, `test_phase05_baseline_regression`, `test_phase05_baseline_5bisA_via_enforce_true`). The two tests within this plan's scope are populated and PASSING: `test_msfc_signed_monthly_repricing` (NEG-01 + NEG-05) and `test_arbitrage_free_signed_target` (NEG-02). The remaining stubs raise `pytest.skip('populated in Plan 05-02')` or `pytest.skip('populated in Plan 05-03')` so the file imports cleanly and `pytest --collect-only` sees all targets."
    - "`tests/conftest.py` autouse `_pfc_lt_env_hygiene` fixture already snapshots `PFC_LT_*` keys by prefix (5bis-A D-12) — adding `PFC_LT_ALLOW_NEGATIVE_PRICES` is automatic via the prefix match. This plan adds an explicit inline comment in conftest.py listing `PFC_LT_ALLOW_NEGATIVE_PRICES` alongside `PFC_LT_USE_SEASONAL_HOURLY_SHAPE` so future readers see both flags documented; the implementation does NOT change."
    - "Cross-cutting truth (appears in all 3 plans): sign-invariance is BY CONSTRUCTION — PCHIP is linear, `_enforce_mean_constraints` correction `error * 0.8` is sign-invariant, delta-additif WV uses `|B_smooth|`, spread additif Peak adds €/MWh. NO monkey-patch in negative regime."
    - "Cross-cutting truth (appears in all 3 plans): tolerance contract for any baseline parquet is `atol=1e-12, rtol=0, check_exact=False`, identical dtypes/columns/index/sort order (5bis-A REVIEWS addendum)."
    - "Cross-cutting truth (appears in all 3 plans): master flag `PFC_LT_ALLOW_NEGATIVE_PRICES` is audit-trail INFO log only — NOT an override. The four ctor args (`enforce_positivity`, `enforce_m_factor_floor`, `enforce_floor`, `allow_negative_peak`) are the API. Operator rollback = pass `enforce_*=True` / `allow_negative_peak=False` explicitly at the four callsites (D-A2-3)."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "NEG-05 wording reformulated per D-A4-7."
      contains: "monthly forward négatif"
    - path: "pfc_shaping/lt/model/msfc_spline.py"
      provides: "`smooth_base_prices(..., enforce_positivity: bool = False)` propagated to `_enforce_mean_constraints`. Signed-aware extrapolation clamp at line 120."
      contains: "enforce_positivity"
    - path: "pfc_shaping/calibration/arbitrage_free.py"
      provides: "`ArbitrageFreeCalibrator(..., enforce_m_factor_floor: bool = False)` ctor kwarg + conditional m_factor floor at line 517 + propagated `converged=False` when clip hits."
      contains: "enforce_m_factor_floor"
    - path: "tests/test_phase05_negative_prices.py"
      provides: "New isolated test module for Phase 5. Two passing tests after this plan (`test_msfc_signed_monthly_repricing`, `test_arbitrage_free_signed_target`), eight stubbed-skipped tests for plans 02 & 03."
      contains: "test_msfc_signed_monthly_repricing"
    - path: "tests/conftest.py"
      provides: "Inline comment documenting `PFC_LT_ALLOW_NEGATIVE_PRICES` alongside `PFC_LT_USE_SEASONAL_HOURLY_SHAPE` (the prefix-based autouse hygiene already covers both keys; no code change beyond documentation)."
      contains: "PFC_LT_ALLOW_NEGATIVE_PRICES"
  key_links:
    - from: "pfc_shaping/lt/model/msfc_spline.py::smooth_base_prices"
      to: "pfc_shaping/lt/model/msfc_spline.py::_enforce_mean_constraints"
      via: "kwarg `enforce_positivity` PROPAGATED as parameter so both floors (l.131 + l.203) are conditional under the same flag (RESEARCH Pitfall 1)"
      pattern: "enforce_positivity"
    - from: "pfc_shaping/calibration/arbitrage_free.py::ArbitrageFreeCalibrator.calibrate"
      to: "CalibrationResult.converged"
      via: "when `enforce_m_factor_floor=True` AND the clip actually mutates m_factor, set `converged=False` before constructing CalibrationResult (NEG-02 literal)"
      pattern: "converged = False"
    - from: "tests/test_phase05_negative_prices.py::test_msfc_signed_monthly_repricing"
      to: "pfc_shaping/lt/model/msfc_spline.py::smooth_base_prices"
      via: "passes base_prices dict containing July=-2.0 with `enforce_positivity=False`, asserts `mean(B_smooth[july_mask]) ≈ -2.0 atol=0.01`"
      pattern: "test_msfc_signed_monthly_repricing"
    - from: "tests/conftest.py::_pfc_lt_env_hygiene"
      to: "PFC_LT_ALLOW_NEGATIVE_PRICES (new env var introduced in Plan 05-03)"
      via: "prefix match `k.startswith('PFC_LT_')` — automatic, no code change required"
      pattern: "PFC_LT_"
---

<objective>
Implement the two ctor-gated floors of Phase 5 Area 1+2 (MSFC + ArbitrageFreeCalibrator) and reformulate the NEG-05 wording in REQUIREMENTS.md before any math change ships. Concretely:

1. Reformuler `REQUIREMENTS.md` NEG-05 per D-A4-7 — remplacer "Cal'27 forward coté -10 €/MWh" par "monthly forward négatif (July M-07'27 = -2 €/MWh, autres months positifs) repricé exactement à -2 €/MWh moyenne sur le mois". Le wording original "Cal annuel négatif" est non-réaliste (un Cal annuel n'est jamais négatif en pratique — ce sont des heures structurelles qui le sont).

2. Ajouter `enforce_positivity: bool = False` (default OFF = negative-ready) à `smooth_base_prices` ET le PROPAGER à `_enforce_mean_constraints` pour conditionner les DEUX floors MSFC (ligne 131 dans `smooth_base_prices`, ligne 203 dans `_enforce_mean_constraints`). C'est le pitfall critique #1 de RESEARCH — il y a DEUX floors MSFC, pas un. Si seul le floor ligne 131 est conditionné, le floor ligne 203 reste actif silencieusement et `test_msfc_signed_monthly_repricing` échoue avec `mean(B_smooth) ≈ 1.0` au lieu de `-2.0`.

3. Réécrire le clamp d'extrapolation ligne 120 en signed-aware per D-A1-2 : `margin = 0.5 * np.ptp(y_knots)` puis `np.clip(B_smooth_raw, y_knots.min() - margin, y_knots.max() + margin)`. L'ancienne formule `[y_knots.min()*0.5, y_knots.max()*2.0]` est mathématiquement cassée pour les knots all-negative (bornes inversées : `[-15.0, -40.0]` au lieu de `[-35.0, -15.0]`).

4. Ajouter `enforce_m_factor_floor: bool = False` (default OFF) à `ArbitrageFreeCalibrator.__init__` per D-A2-1. Conditionner le `m_factor = np.maximum(m_factor, 0.1)` ligne 517 sur ce flag. Quand `enforce_m_factor_floor=True` et que le clip mute effectivement `m_factor`, propager `converged=False` (NEG-02 littéral, RESEARCH Pitfall 3) ET logger un WARN avec le nombre de timestamps clippés.

5. Créer `tests/test_phase05_negative_prices.py` comme scaffolding durable pour TOUS les tests Phase 5 (10 au total : 4 unit math + 2 unit Plan 05-02 + 4 unit/acceptance/regression Plan 05-03). Les deux tests in-scope (`test_msfc_signed_monthly_repricing` NEG-01+NEG-05, `test_arbitrage_free_signed_target` NEG-02) sont populés et passent ; les huit autres sont stubbés-skippés avec `pytest.skip('populated in Plan 05-0X')` pour que `pytest --collect-only` voie toutes les cibles dès Wave 1.

6. Ajouter dans `tests/conftest.py` un commentaire inline qui documente `PFC_LT_ALLOW_NEGATIVE_PRICES` alongside `PFC_LT_USE_SEASONAL_HOURLY_SHAPE` — le snapshot/restore by prefix `PFC_LT_*` couvre automatiquement le nouveau flag (Plan 05-03 callsite), mais la doc explicite l'intent pour futur lecteur.

Purpose: This plan lands the foundational ctor-args (MSFC + AF) under defaults OFF (negative-ready) — la sémantique « le default est negative-ready » est établie ici, avant tout refactor sémantique multiplicatif→additif de WV (Plan 05-02) ou Peak (Plan 05-03). En séparant les changements de surface API du refactor du pipeline `assembler.build()`, on garantit que Plans 02/03 peuvent dépendre d'une API stable. RESEARCH §Open Question #1 (mode ArbitrageCalibrator dans `autoresearch.py`/`production_phases.py`) sera vérifié par grep au début de Task 3 — l'attribut `enforce_m_factor_floor` ne mord qu'en mode "multiplicative" mais le ctor arg lui est inconditionnel pour traçabilité.

Output: 1 doc fix (REQUIREMENTS.md NEG-05), 2 modified production files (`msfc_spline.py`, `arbitrage_free.py`), 1 new test scaffold module (`tests/test_phase05_negative_prices.py` with 2 populated + 8 skip-stubbed tests), 1 doc comment in `tests/conftest.py`. Test suite goes from 258 → 260 passing (3 skipped → 11 skipped : +8 stub-skipped Phase 5 tests).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-msfc-log-prix-retire-silent-floors/05-CONTEXT.md
@.planning/phases/05-msfc-log-prix-retire-silent-floors/05-RESEARCH.md
@.planning/phases/05-msfc-log-prix-retire-silent-floors/05-VALIDATION.md
@.planning/phases/05B-shape-hourly-infrastructure-flag-no-op-refactor/05B-CONTEXT.md
@.planning/phases/05B-shape-hourly-infrastructure-flag-no-op-refactor/05B-REVIEWS.md
@.planning/phases/05C-shape-hourly-bowl-deepening/05C-CONTEXT.md
@pfc_shaping/lt/model/msfc_spline.py
@pfc_shaping/calibration/arbitrage_free.py
@pfc_shaping/lt/model/shape_hourly.py
@tests/conftest.py

<interfaces>
Key contracts already established (NOT to re-explore):

From `pfc_shaping/lt/model/msfc_spline.py` (current state, pre-Phase-5):
```
def smooth_base_prices(
    idx_15min: pd.DatetimeIndex,
    base_prices: dict[str, float],
    B_flat: pd.Series,
) -> pd.Series:
    ...
    # Line 120 (extrapolation clamp — to be rewritten signed-aware):
    B_smooth_raw = np.clip(B_smooth_raw, y_knots.min() * 0.5, y_knots.max() * 2.0)
    ...
    # Line 125 — calls _enforce_mean_constraints (currently no enforce_positivity passed):
    B_smooth = _enforce_mean_constraints(
        idx_zh, x_target, x_knots, y_knots, month_keys,
        base_only, B_smooth_raw, start_ts, max_iter=10,
    )
    # Line 131 — floor #1 (to be conditional):
    B_smooth = np.maximum(B_smooth, 1.0)
    return ...

def _enforce_mean_constraints(
    idx_zh, x_target, x_knots, y_knots, month_keys,
    base_only, B_smooth_raw, start_ts, max_iter=10,
) -> np.ndarray:
    # Iterative correction `error * 0.8` — sign-invariant by construction (D-A1-3).
    ...
    result = interpolator(x_target)
    # Line 203 — floor #2 (to be conditional, hidden inside the helper):
    return np.maximum(result, 1.0)
```

From `pfc_shaping/calibration/arbitrage_free.py` (current state):
```
class CalibrationResult:
    delta: np.ndarray
    converged: bool
    max_abs_residual: float
    ...

class ArbitrageFreeCalibrator:
    def __init__(
        self,
        ...,
        mode: str = "multiplicative",
        tol: float = ...,
        ...
    ) -> None: ...

    def calibrate(self, price_raw, contracts, country: str = "CH") -> CalibrationResult:
        ...
        if self.mode == "multiplicative":
            ...
            # Line 516-517 — m_factor floor (to be conditional):
            m_factor = P_add / safe_S
            m_factor = np.maximum(m_factor, 0.1)
            ...
            P = np.where(small_mask, P_add, S * m_factor)
            ...
        # Line 546 — convergence judgment:
        if max_abs_residual > self.tol:
            converged = False
        ...
        return CalibrationResult(
            ...,
            converged=converged,
            max_abs_residual=...,
        )
```

From `tests/conftest.py` (5bis-A D-12, already in place):
```
@pytest.fixture(autouse=True)
def _pfc_lt_env_hygiene():
    snapshot = {k: v for k, v in os.environ.items() if k.startswith("PFC_LT_")}
    yield
    current = {k for k in os.environ if k.startswith("PFC_LT_")}
    for k in current - set(snapshot):
        os.environ.pop(k, None)
    for k, v in snapshot.items():
        os.environ[k] = v
```
The prefix-based snapshot auto-covers ANY new `PFC_LT_*` key — `PFC_LT_ALLOW_NEGATIVE_PRICES` (introduced in Plan 05-03) is therefore hygiene-protected for free; this plan only adds an in-source comment naming both flags so future readers see the intent.

Test count at start of this plan (per 05-RESEARCH.md §Environment Availability, verified by researcher 2026-05-19): `258 passed, 3 skipped`. This plan must end at `260 passed, 11 skipped` (+2 populated Phase 5 tests, +8 stubbed-skipped Phase 5 tests).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reformulate REQUIREMENTS.md NEG-05 per D-A4-7</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current NEG-05 line at the "Negative Prices" section — line 36)
    - .planning/phases/05-msfc-log-prix-retire-silent-floors/05-CONTEXT.md §decisions D-A4-7 (canonical reformulation wording)
    - .planning/phases/05-msfc-log-prix-retire-silent-floors/05-RESEARCH.md §user_constraints D-A4-7 (research-cited reformulation rationale)
  </read_first>
  <action>
    Edit `.planning/REQUIREMENTS.md` at the NEG-05 line (currently line 36, under `### Negative Prices`).

    Replace the existing wording verbatim. Current line:
    > - [ ] **NEG-05** : Un Cal'27 forward coté -10 €/MWh est correctement repricé par la PFC à -10 €/MWh moyenne sur l'année (test de bout-en-bout).

    New wording per D-A4-7:
    > - [ ] **NEG-05** : Un monthly forward négatif (e.g., July M-07'27 = -2 €/MWh, autres months positifs typiques EEX) est correctement repricé par la PFC à -2 €/MWh moyenne sur le mois (math invariance test, vérifie l'absence des floors silencieux ligne 131 et ligne 203 du MSFC spline). Note (2026-05-19) : reformulation post-discuss-phase Phase 5 — un Cal annuel n'est jamais négatif en pratique, ce sont des heures structurelles qui le sont (D-A4-7 / 05-CONTEXT.md).

    The wording MUST appear exactly as above with the "Note (2026-05-19)" suffix so future readers see why the wording diverges from the pre-2026-05 version. Do NOT touch NEG-01..NEG-04 wording. Do NOT touch any other section of REQUIREMENTS.md.
  </action>
  <verify>
    <automated>grep -q "monthly forward négatif" .planning/REQUIREMENTS.md && grep -q "July M-07'27 = -2 €/MWh" .planning/REQUIREMENTS.md && ! grep -q "Cal'27 forward coté -10 €/MWh" .planning/REQUIREMENTS.md && echo OK reformulation</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "monthly forward négatif" .planning/REQUIREMENTS.md` returns ≥ 1.
    - `grep -c "Cal'27 forward coté -10 €/MWh" .planning/REQUIREMENTS.md` returns 0 (old wording fully removed).
    - `grep -q "D-A4-7" .planning/REQUIREMENTS.md` exits 0 (decision-ID traceability inline).
    - `grep -q "Note (2026-05-19)" .planning/REQUIREMENTS.md` exits 0 (provenance comment).
    - `head -50 .planning/REQUIREMENTS.md | grep -A1 "NEG-05"` shows the new wording.
    - The "Negative Prices" section still has exactly 5 items (NEG-01..NEG-05) — `grep -c "^- \[ \] \*\*NEG-" .planning/REQUIREMENTS.md` returns 5.
  </acceptance_criteria>
  <done>REQUIREMENTS.md NEG-05 reformulated per D-A4-7; old "Cal'27=-10" wording removed; D-A4-7 decision ID and 2026-05-19 provenance inline.</done>
</task>

<task type="auto">
  <name>Task 2: Add `enforce_positivity` ctor kwarg to MSFC (TWO floors — l.131 + l.203) + signed-aware extrapolation clamp at l.120</name>
  <files>pfc_shaping/lt/model/msfc_spline.py</files>
  <read_first>
    - pfc_shaping/lt/model/msfc_spline.py:1-50 (module docstring + imports + smooth_base_prices signature)
    - pfc_shaping/lt/model/msfc_spline.py:110-135 (extrapolation clamp at line 120 + first floor at line 131 — both rewrite targets)
    - pfc_shaping/lt/model/msfc_spline.py:140-205 (`_enforce_mean_constraints` full body — the second floor at line 203 lives at the very end of the function return)
    - .planning/phases/05-msfc-log-prix-retire-silent-floors/05-CONTEXT.md §decisions D-A1-1, D-A1-2, D-A1-3, D-A2-1 (linear PCHIP preserved, signed-aware clamp formula, sign-invariant iterative correction by construction, ctor arg default OFF)
    - .planning/phases/05-msfc-log-prix-retire-silent-floors/05-RESEARCH.md §Architecture Patterns §Pattern 1, §Pattern 2 (kwarg propagation + clamp formula), §Common Pitfalls §Pitfall 1 (TWO floors — l.131 inside smooth_base_prices, l.203 inside _enforce_mean_constraints), §Code Examples §floor conditionnel MSFC
    - pfc_shaping/lt/model/shape_hourly.py:89-119 (`_FLAG_ENV_VAR` + `_resolve_flag` — the 5bis-A pattern that this plan does NOT use directly here, but documents as the audit-trail family for Plan 05-03's `PFC_LT_ALLOW_NEGATIVE_PRICES`)
  </read_first>
  <action>
    Modify `pfc_shaping/lt/model/msfc_spline.py` with three surgical edits.

    **Edit A — Extend `smooth_base_prices` signature (line 39):**
    Add a new keyword argument `enforce_positivity: bool = False` at the end of the function signature (after `B_flat: pd.Series`). Default OFF per D-A2-1 = negative-ready. Update the function docstring to add a parameter entry naming D-A2-1 / NEG-01 / RESEARCH Pitfall 1 (so a reader sees the rationale without leaving the source). The docstring must state explicitly: "When False (default, negative-ready per Phase 5 D-A2-1), neither line 131 nor line 203 floors fire. When True (legacy rollback path, operator opt-in per D-A2-3), both floors are restored. RESEARCH Pitfall 1: two floors exist — line 131 in this function and line 203 in `_enforce_mean_constraints` — this kwarg MUST be propagated to the helper to disable both."

    **Edit B — Rewrite the extrapolation clamp at line 120 (signed-aware per D-A1-2):**
    Replace the existing line `B_smooth_raw = np.clip(B_smooth_raw, y_knots.min() * 0.5, y_knots.max() * 2.0)` with:
    ```
    margin = 0.5 * np.ptp(y_knots)
    B_smooth_raw = np.clip(B_smooth_raw, y_knots.min() - margin, y_knots.max() + margin)
    ```
    Add an inline comment naming D-A1-2 and stating the rationale: "Signed-aware: np.ptp is always ≥ 0 so margin ≥ 0 ; bounds [min-margin, max+margin] are always [lo, hi] regardless of sign. The legacy formula [min*0.5, max*2.0] produced inverted bounds (lo > hi) for all-negative knots (e.g. [-30, -20, -25] → [-15.0, -40.0], np.clip raises or wraps). RESEARCH dry-run §D-A1-2 verified the new formula on all-negative, mixed, all-positive knots."

    **Edit C — Propagate `enforce_positivity` to `_enforce_mean_constraints` AND condition both floors:**

    At the call site (around line 125), pass the new kwarg through:
    ```
    B_smooth = _enforce_mean_constraints(
        idx_zh, x_target, x_knots, y_knots, month_keys,
        base_only, B_smooth_raw, start_ts, max_iter=10,
        enforce_positivity=enforce_positivity,
    )
    ```

    Floor at line 131 (inside `smooth_base_prices`): replace `B_smooth = np.maximum(B_smooth, 1.0)` with:
    ```
    if enforce_positivity:
        B_smooth = np.maximum(B_smooth, 1.0)
    ```

    Extend `_enforce_mean_constraints` signature: add `enforce_positivity: bool = False` as the last kwarg. Update its docstring to mention "RESEARCH Pitfall 1: the floor at line 203 below is gated by this kwarg; the caller `smooth_base_prices` MUST propagate its own `enforce_positivity` value here."

    Floor at line 203 (inside `_enforce_mean_constraints`): replace `return np.maximum(result, 1.0)` with:
    ```
    return np.maximum(result, 1.0) if enforce_positivity else result
    ```

    Do NOT modify the iterative correction logic itself (`correction = error * 0.8`) — it is sign-invariant by construction per D-A1-3 / D-A3-1. The only changes are the signature extension, the conditional return, and propagation.

    Verify backward-compat manually after the edit: `python -c "from pfc_shaping.lt.model.msfc_spline import smooth_base_prices; import inspect; sig = inspect.signature(smooth_base_prices); assert 'enforce_positivity' in sig.parameters; assert sig.parameters['enforce_positivity'].default is False; print('OK signature')"`. The call site discipline (every caller in the codebase still works without passing `enforce_positivity`) is preserved because the default is False = negative-ready (no behavior change vs caller intent under defaults OFF, since the legacy callers would have produced floored outputs and now produce signed outputs — this is THE Phase 5 math change for MSFC, and it is the entire purpose of D-A2-1).
  </action>
  <verify>
    <automated>python -c "
from pfc_shaping.lt.model.msfc_spline import smooth_base_prices, _enforce_mean_constraints
import inspect, numpy as np, pandas as pd

# Signature checks
sig_s = inspect.signature(smooth_base_prices)
assert 'enforce_positivity' in sig_s.parameters, list(sig_s.parameters)
assert sig_s.parameters['enforce_positivity'].default is False

sig_e = inspect.signature(_enforce_mean_constraints)
assert 'enforce_positivity' in sig_e.parameters, list(sig_e.parameters)
assert sig_e.parameters['enforce_positivity'].default is False

# Smoke test signed monthly: a small monthly fixture with July=-2 in 2027
idx = pd.date_range('2027-01-01', '2027-12-31', freq='15min', tz='UTC')
base_prices = {'2027': 25.0, '2027-07': -2.0}
B_flat = pd.Series(25.0, index=idx, name='B_flat')
B_smooth = smooth_base_prices(idx, base_prices, B_flat, enforce_positivity=False)
july_mask = (B_smooth.index.month == 7)
mean_july = float(B_smooth[july_mask].mean())
assert abs(mean_july - (-2.0)) < 0.1, f'July mean drift too large: {mean_july:.4f} vs -2.0'
print(f'OK msfc signed: mean(July)={mean_july:.4f}, target=-2.0')

# Confirm enforce_positivity=True path floors result
B_floored = smooth_base_prices(idx, base_prices, B_flat, enforce_positivity=True)
assert float(B_floored.min()) >= 1.0, f'enforce_positivity=True should floor at 1.0, min={float(B_floored.min())}'
print('OK enforce_positivity=True floors at 1.0')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "enforce_positivity: bool = False" pfc_shaping/lt/model/msfc_spline.py` returns ≥ 2 matches (in `smooth_base_prices` and in `_enforce_mean_constraints` signatures).
    - `grep -n "np.ptp(y_knots)" pfc_shaping/lt/model/msfc_spline.py` returns 1 match (signed-aware clamp).
    - `grep -n "y_knots.min() \* 0.5" pfc_shaping/lt/model/msfc_spline.py` returns 0 matches (legacy formula removed).
    - `grep -v '^[[:space:]]*#' pfc_shaping/lt/model/msfc_spline.py | grep -c "np.maximum(B_smooth, 1.0)"` returns exactly 1 (still present but inside an `if enforce_positivity:` guard — search the active code path with line context: `grep -B1 -A0 "np.maximum(B_smooth, 1.0)" pfc_shaping/lt/model/msfc_spline.py` shows the `if enforce_positivity:` line above).
    - **Floor-guard structural proof (W1 hardening — was previously just a count, now proves the conditional wraps the floor):** Use the inline-block grep pattern to assert the floor literally appears within an `if enforce_positivity:` block. Run `awk '/if enforce_positivity:/{flag=1; next} flag && /np.maximum\(B_smooth, 1.0\)/{print "GUARD_OK"; flag=0; exit} flag && /^[^[:space:]]/{flag=0}' pfc_shaping/lt/model/msfc_spline.py` and assert it prints exactly `GUARD_OK`. This proves the active occurrence at line 131 is structurally inside the guard, not just adjacent.
    - `grep -v '^[[:space:]]*#' pfc_shaping/lt/model/msfc_spline.py | grep -c "return np.maximum(result, 1.0) if enforce_positivity"` returns 1 (conditional return form in `_enforce_mean_constraints`).
    - **Second-floor guard structural proof (W1 hardening, line 203 floor in `_enforce_mean_constraints`):** Assert the conditional return form is on a single line by running `grep -E "return np\.maximum\(result, 1\.0\) if enforce_positivity (else result|else result)" pfc_shaping/lt/model/msfc_spline.py` and confirming it returns exactly 1 match (the ternary form ensures the floor cannot fire when `enforce_positivity=False` — no naked `return np.maximum(result, 1.0)` survives).
    - `grep -q "D-A1-2" pfc_shaping/lt/model/msfc_spline.py` exits 0 (decision-ID traceability for the clamp).
    - `grep -q "D-A2-1\|NEG-01" pfc_shaping/lt/model/msfc_spline.py` exits 0 (decision-ID traceability for the floors).
    - `grep -q "RESEARCH Pitfall 1" pfc_shaping/lt/model/msfc_spline.py` exits 0 (pitfall surfaced inline).
    - The Python verify command above prints `OK msfc signed: mean(July)=...` AND `OK enforce_positivity=True floors at 1.0`.
    - `pytest tests/ -x -q -k "not test_phase05"` exits 0 with `258 passed, 3 skipped` — no regression on the existing suite (existing MSFC tests do not pass `enforce_positivity`, so they get default False = signed mode, which means any existing test that ASSERTS a floor at 1.0 will now FAIL — IF any such test exists, the task MUST surface it in the SUMMARY before proceeding; the expected count assumes no such legacy MSFC floor-assertion tests, per RESEARCH §Runtime State Inventory baseline of 258 passed at phase entry).
  </acceptance_criteria>
  <done>MSFC `smooth_base_prices` and `_enforce_mean_constraints` both accept `enforce_positivity: bool = False`, both floors at l.131 and l.203 are conditional, and the line 120 extrapolation clamp is signed-aware. Pre-existing suite stays green at 258 passed.</done>
</task>

<task type="auto">
  <name>Task 3: Add `enforce_m_factor_floor` ctor kwarg to ArbitrageFreeCalibrator + propagate `converged=False` when the clip mutates m_factor (NEG-02)</name>
  <files>pfc_shaping/calibration/arbitrage_free.py</files>
  <read_first>
    - pfc_shaping/calibration/arbitrage_free.py:100-115 (CalibrationResult dataclass — confirms `converged: bool` field exists and is constructed at the end of `calibrate()`)
    - pfc_shaping/calibration/arbitrage_free.py:450-475 (`__init__` signature region — locate the existing ctor args to add `enforce_m_factor_floor` cleanly)
    - pfc_shaping/calibration/arbitrage_free.py:510-560 (multiplicative-mode branch with the m_factor floor at line 517 + the convergence judgment at line 546)
    - pfc_shaping/calibration/arbitrage_free.py:585-600 (CalibrationResult construction around line 595 — confirms `converged=converged` is passed)
    - .planning/phases/05-msfc-log-prix-retire-silent-floors/05-CONTEXT.md §decisions D-A2-1, D-A2-3 (ctor arg default OFF, operator rollback by explicit True at callsite)
    - .planning/phases/05-msfc-log-prix-retire-silent-floors/05-RESEARCH.md §Architecture Patterns §Pattern 1 (ctor arg pattern), §Common Pitfalls §Pitfall 3 (converged=False propagation), §Open Questions #1 (mode "multiplicative" assumption A3 to verify by grep)
    - .planning/phases/05-msfc-log-prix-retire-silent-floors/05-RESEARCH.md §Code Examples §ArbitrageFreeCalibrator (target patch shape)
    - pfc_shaping/pipeline/autoresearch.py:234 (legacy callsite — verify it still works with default OFF after this edit)
    - pfc_shaping/pipeline/production_phases.py (locate calls to `ArbitrageFreeCalibrator(`; verify the mode= argument used)
  </read_first>
  <action>
    Modify `pfc_shaping/calibration/arbitrage_free.py` with three surgical edits.

    **Pre-edit verification (RESEARCH §Open Question #1 / Assumptions Log A3):**
    Before any code change, grep the pipelines for the ArbitrageFreeCalibrator mode:
    ```
    grep -n "ArbitrageFreeCalibrator(" pfc_shaping/pipeline/*.py
    grep -n "mode=" pfc_shaping/pipeline/*.py | grep -i arbitrage
    ```
    Expectation per RESEARCH A3: all callsites use the default mode="multiplicative" (so the floor at line 517 is on the active path). If a callsite passes `mode="additive"`, document that finding in the SUMMARY — the `enforce_m_factor_floor` kwarg has no effect there (additive mode does not floor m_factor), but the kwarg is still added inconditionnellement for API completeness and so the docstring is honest about the "multiplicative-mode-only" scope.

    **Edit A — Extend `ArbitrageFreeCalibrator.__init__` signature (around line 450):**
    Add a new keyword argument `enforce_m_factor_floor: bool = False` (default OFF per D-A2-1). Place it after `mode: str = "multiplicative"` so the signature reads naturally. Inside the body, store `self.enforce_m_factor_floor = bool(enforce_m_factor_floor)`. Update the class docstring with a "Phase 5 D-A2-1 / NEG-02" paragraph stating: "When False (default, negative-ready), the multiplicative-mode m_factor floor at line 517 is bypassed and signed outputs are allowed. When True (legacy rollback path per D-A2-3), the floor is applied AND any actual clip event forces `converged=False` (NEG-02 literal: a floored m_factor masks a calibration residual that the convergence judgment must not silently swallow). The kwarg has no effect in `mode='additive'` — additive mode does not produce m_factor at all."

    **Edit B — Condition the m_factor floor at line 517 + propagate `converged=False` (NEG-02 literal):**

    The existing multiplicative-mode block (around lines 514-525) currently reads roughly:
    ```
    safe_S = ...
    m_factor = P_add / safe_S
    m_factor = np.maximum(m_factor, 0.1)              # line 517 — floor
    small_mask = ...
    P = np.where(small_mask, P_add, S * m_factor)
    delta_for_log = m_factor - 1.0
    ```

    Replace lines 516-519 with a conditional block:
    ```
    m_factor = P_add / safe_S
    floor_applied_mask = m_factor < 0.1
    n_clipped = int(np.sum(floor_applied_mask))
    if self.enforce_m_factor_floor:
        if n_clipped > 0:
            logger.warning(
                "m_factor floor 0.1 applied at %d timestamps (enforce_m_factor_floor=True) — propagating converged=False per NEG-02",
                n_clipped,
            )
        m_factor = np.maximum(m_factor, 0.1)
    # When enforce_m_factor_floor=False (Phase 5 default), m_factor keeps its raw value;
    # signed m_factor < 0 is mathematically valid in the multiplicative formulation
    # when S and P have opposite signs (rare but legitimate edge case).
    small_mask = ...
    P = np.where(small_mask, P_add, S * m_factor)
    ```

    The mask `floor_applied_mask = m_factor < 0.1` is computed UNCONDITIONALLY (cheap, vector compare) — it is needed by the convergence-judgment override below regardless of whether the floor is enforced (under enforce_m_factor_floor=False, we can keep the count for telemetry but do NOT force `converged=False` since no actual mutation happened).

    **Edit C — Propagate `converged=False` when the clip actually mutates m_factor (NEG-02 literal):**

    Locate the existing convergence judgment (line 546):
    ```
    if max_abs_residual > self.tol:
        converged = False
    ```

    Extend it with the NEG-02 override AFTER the existing judgment (so both conditions can independently set converged=False):
    ```
    # NEG-02 literal: a floor on m_factor masks a calibration residual that
    # max_abs_residual cannot detect (because the clip changes m_factor, which
    # in turn changes P, which makes the residual look smaller than the
    # un-clipped truth). Force converged=False when the clip actually mutated
    # m_factor under enforce_m_factor_floor=True.
    if self.enforce_m_factor_floor and n_clipped > 0:
        converged = False
    ```

    Do NOT touch the CalibrationResult construction at line 595 — `converged=converged` already wires the variable through.

    Do NOT modify any logic in the `additive` mode branch. The m_factor concept does not exist there.

    Do NOT modify the `price_scale = np.maximum(np.abs(S), 1.0)` line 464 — that is the smoothness scaling for the QP, NOT a floor on the output. It is out-of-scope Phase 5 (RESEARCH §canonical_refs flags it as TODO P1-01).
  </action>
  <verify>
    <automated>python -c "
from pfc_shaping.calibration.arbitrage_free import ArbitrageFreeCalibrator
import inspect

# Signature checks
sig = inspect.signature(ArbitrageFreeCalibrator.__init__)
assert 'enforce_m_factor_floor' in sig.parameters, list(sig.parameters)
assert sig.parameters['enforce_m_factor_floor'].default is False

# Instantiation paths
c1 = ArbitrageFreeCalibrator()
assert c1.enforce_m_factor_floor is False, c1.enforce_m_factor_floor
c2 = ArbitrageFreeCalibrator(enforce_m_factor_floor=True)
assert c2.enforce_m_factor_floor is True
c3 = ArbitrageFreeCalibrator(enforce_m_factor_floor=False)
assert c3.enforce_m_factor_floor is False
print('OK arbitrage_free ctor kwarg')
" && grep -n "ArbitrageFreeCalibrator(" pfc_shaping/pipeline/*.py | head -5 && grep -n "mode=" pfc_shaping/calibration/arbitrage_free.py | head -3 && pytest tests/ -x -q -k "arbitrage or calibration" 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "enforce_m_factor_floor: bool = False" pfc_shaping/calibration/arbitrage_free.py` returns 1 match (ctor signature).
    - `grep -n "self.enforce_m_factor_floor" pfc_shaping/calibration/arbitrage_free.py` returns ≥ 3 matches (init store + floor branch + converged override).
    - `grep -n "floor_applied_mask\|n_clipped" pfc_shaping/calibration/arbitrage_free.py` returns ≥ 2 matches (the unconditional mask compute + downstream use).
    - `grep -v '^[[:space:]]*#' pfc_shaping/calibration/arbitrage_free.py | grep -c "np.maximum(m_factor, 0.1)"` returns 1 (still present but inside `if self.enforce_m_factor_floor:` guard — verify the guard with `grep -B2 "np.maximum(m_factor, 0.1)" pfc_shaping/calibration/arbitrage_free.py`).
    - `grep -q "NEG-02" pfc_shaping/calibration/arbitrage_free.py` exits 0 (requirement-ID traceability).
    - `grep -q "D-A2-1" pfc_shaping/calibration/arbitrage_free.py` exits 0 (decision-ID traceability).
    - The python verify command above prints `OK arbitrage_free ctor kwarg`.
    - `pytest tests/ -x -q -k "arbitrage or calibration"` exits 0 — no regression on the arbitrage-free / calibration suite.
    - `pytest tests/ -x -q` exits 0 with `258 passed, 3 skipped` (full suite unchanged; the existing tests do not pass `enforce_m_factor_floor` so they get default False, which matches the previous behavior on positive-only inputs since `m_factor < 0.1` never fires there).
    - The SUMMARY records the grep result for `ArbitrageFreeCalibrator(` in `pfc_shaping/pipeline/*.py` and confirms mode used (multiplicative vs additive). If any caller uses `mode="additive"`, this is noted but does NOT block — the kwarg is API-completeness oriented.
  </acceptance_criteria>
  <done>`ArbitrageFreeCalibrator.__init__` accepts `enforce_m_factor_floor: bool = False`; the floor at line 517 is conditional; `converged=False` is propagated when the clip actually mutates m_factor; pipeline callsites grep'd and mode confirmed; suite stays green at 258 passed.</done>
</task>

<task type="auto">
  <name>Task 4: Create `tests/test_phase05_negative_prices.py` scaffold (10 tests: 2 populated for this plan, 8 stubbed-skipped for Plans 02 & 03) + tests/conftest.py inline doc comment</name>
  <files>tests/test_phase05_negative_prices.py, tests/conftest.py</files>
  <read_first>
    - tests/test_shape_hourly_infra.py (5bis-A test module — pattern reference for module structure, fixtures, helpers)
    - tests/test_shape_hourly_bowl.py (5bis-B test module — pattern reference for synthetic fixture loading, threshold-from-JSON, conditional skip for cross-plan deferred tests)
    - tests/conftest.py (current state — prefix-based PFC_LT_* hygiene already in place)
    - .planning/phases/05-msfc-log-prix-retire-silent-floors/05-CONTEXT.md §decisions D-A4-4, D-A4-5 (4 unit + 1 acceptance contracts), D-A4-6 (new baseline parquet contract)
    - .planning/phases/05-msfc-log-prix-retire-silent-floors/05-VALIDATION.md §Per-Task Verification Map (full 10-test enumeration), §Wave 0 Requirements
    - .planning/phases/05-msfc-log-prix-retire-silent-floors/05-RESEARCH.md §Validation Architecture §Spécifications des 4 unit tests + 1 acceptance (canonical test bodies for `test_msfc_signed_monthly_repricing` and `test_arbitrage_free_signed_target`)
    - pfc_shaping/lt/model/msfc_spline.py (post-Task-2 state — `smooth_base_prices` exposes `enforce_positivity`)
    - pfc_shaping/calibration/arbitrage_free.py (post-Task-3 state — `ArbitrageFreeCalibrator` exposes `enforce_m_factor_floor`)
  </read_first>
  <action>
    **Part A — Create `tests/test_phase05_negative_prices.py`:**

    Module structure:
    1. Module docstring naming Phase 5, listing the 10 tests, naming the responsible plan for each, and referencing 05-VALIDATION.md §Per-Task Verification Map.
    2. Standard imports: `from __future__ import annotations`, `import os`, `import json`, `from pathlib import Path`, `import numpy as np`, `import pandas as pd`, `import pytest`. Lazy/local imports for `pfc_shaping.*` symbols inside each test (so collection does not crash if a downstream symbol is not yet shipped — relevant for the eight stubs).
    3. Module-level constants:
       - `_PHASE = "05"` (used in skip messages so the cross-plan owner is grep-able).
       - `_FIXTURE_PATH = Path("tests/fixtures/forwards_phase05_seed42.parquet")` (Plan 05-03 will create).
       - `_BASELINE_PATH = Path("tests/fixtures/baseline_pfc_seed42_phase05.parquet")` (Plan 05-03 will create).
       - `_BASELINE_5BISA_PATH = Path("tests/fixtures/baseline_pfc_seed42.parquet")` (already exists from 5bis-A).
       - `_BOWL_MARKER_PATH = Path("tests/fixtures/baseline_pfc_seed42_bowl.parquet")` (5bis-B marker; presence/absence gates the acceptance test).

    4. Ten test functions in this exact order:

       a. **`test_msfc_signed_monthly_repricing` (POPULATED, NEG-01 + NEG-05)** — per RESEARCH §Validation Architecture §test_msfc_signed_monthly_repricing canonical body. Build `idx_15min = pd.date_range('2027-01-01', '2027-12-31 23:45', freq='15min', tz='UTC')`. Build `base_prices = {'2027': 25.0, '2027-01': 28.0, '2027-02': 27.0, '2027-03': 25.0, '2027-04': 22.0, '2027-05': 23.0, '2027-06': 24.0, '2027-07': -2.0, '2027-08': 25.0, '2027-09': 24.0, '2027-10': 25.0, '2027-11': 27.0, '2027-12': 30.0}`. Call `B_smooth = smooth_base_prices(idx_15min, base_prices, B_flat, enforce_positivity=False)` where `B_flat = pd.Series(25.0, index=idx_15min, name='B_flat')`. Assert `abs(B_smooth[B_smooth.index.month == 7].mean() - (-2.0)) < 0.05` (5x the 0.01 target in RESEARCH — robustness against PCHIP interpolation residual; if the test passes at 0.05 but fails at 0.01, document the residual in SUMMARY so Plan 05-02/03 can tighten if needed). Assert `B_smooth[B_smooth.index.month == 7].min() < 0.0` (a signed value did materialize, not just a near-zero mean from positive-only smoothing).

       b. **`test_arbitrage_free_signed_target` (POPULATED, NEG-02)** — per RESEARCH §Validation Architecture §test_arbitrage_free_signed_target canonical body. Instantiate `cal = ArbitrageFreeCalibrator(enforce_m_factor_floor=False)`. Build a small `price_raw` series with 12 monthly cells, July dépressé negative, and a single negative monthly contract target. Call `result = cal.calibrate(price_raw, contracts, country='CH')`. Assert `result.converged is True` AND `result.max_abs_residual < cal.tol`. The exact `price_raw` and `contracts` shapes are out-of-scope details — copy the existing helper from `tests/test_arbitrage_free*.py` (or `tests/test_calibration*.py`) that builds minimal contracts and adapt the monthly target to -5 €/MWh on July. If no such helper exists, the test body builds a 4380-hour (=365×12) series with a constant +25 baseline plus a July dépression of -30 €/MWh, and one contract `Contract(key='2027-07', start=..., end=..., target_price=-5.0)`. The key assertion is that the calibrator handles the signed target without firing the m_factor floor (since `enforce_m_factor_floor=False`).

       c. **`test_water_value_delta_sign_invariant` (STUBBED-SKIPPED, NEG-03)** — body: `pytest.skip(f'populated in Plan {_PHASE}-02 — see 05-VALIDATION.md task 5-02-01')`. Docstring naming the contract: "B_smooth=-10, f_wv=1.20 → delta_wv=+2.0 (scarcity = moins négatif). B_smooth=-10, f_wv=0.80 → delta_wv=-2.0 (abundance = plus négatif). Vs legacy multiplicatif: 1.20 × -10 = -12 (sémantique inversée)."

       d. **`test_assembler_delta_additive` (STUBBED-SKIPPED, NEG-03)** — body: `pytest.skip(f'populated in Plan {_PHASE}-02 — see 05-VALIDATION.md task 5-02-02')`. Docstring naming the contract: "assembler.build() consumes compute_delta_wv additively: P = B × f_H × f_W + delta_wv (no f_wv multiplicative). INFO log emits 'WV delta_wv: min=… max=… mean=… €/MWh, sign(B) flips=…'."

       e. **`test_cascading_spread_signed_base` (STUBBED-SKIPPED, NEG-04)** — body: `pytest.skip(f'populated in Plan {_PHASE}-03 — see 05-VALIDATION.md task 5-03-01')`. Docstring naming the contract: "cascader.peak_base_spreads_ = {m: 5.0}, base={'2027': -10.0}, allow_negative_peak=True → result['2027-Peak']=-5.0 (NOT -10.5 du legacy ratio=1.05)."

       f. **`test_fit_peak_ratios_deprecated` (STUBBED-SKIPPED, NEG-04)** — body: `pytest.skip(f'populated in Plan {_PHASE}-03 — see 05-VALIDATION.md task 5-03-02')`. Docstring (W3 RESOLVED via RESEARCH §Pattern 6 — propagated here so the stub is consistent with the post-research plan): `"""RESOLVED in RESEARCH §Pattern 6: transparent shim — fit_peak_ratios emits DeprecationWarning and delegates to fit_peak_spreads. CONTEXT.md D-A4-2's NotImplementedError approach was overridden after the callsite audit confirmed both production callers (production_phases.py:344, :644) have spot_history available. Plan 05-03 implements the shim. This test asserts: pytest.warns(DeprecationWarning) + peak_base_spreads_ populated identical to direct fit_peak_spreads call."""`

       g. **`test_master_flag_audit_log` (STUBBED-SKIPPED)** — body: `pytest.skip(f'populated in Plan {_PHASE}-03 — see 05-VALIDATION.md task 5-03-03')`. Docstring naming the contract: "PFCAssembler.__init__ reads PFC_LT_ALLOW_NEGATIVE_PRICES once, emits INFO 'PFC_LT_ALLOW_NEGATIVE_PRICES={state}, floors_disabled={msfc:..., af:..., wv:..., cascading:...}'."

       h. **`test_phase05_summer_bowl_negative_acceptance` (STUBBED-SKIPPED, SC #2 ROADMAP)** — body: `pytest.skip(f'populated in Plan {_PHASE}-03 — see 05-VALIDATION.md task 5-03-04')`. Docstring naming the gating: "Gated by 5bis-B bowl marker (tests/fixtures/baseline_pfc_seed42_bowl.parquet). If marker absent, skip with message 'Phase 5 SC #2 requires 5bis-B bowl deepening calibrated first'."

       i. **`test_phase05_baseline_regression` (STUBBED-SKIPPED, SC #5 ROADMAP)** — body: `pytest.skip(f'populated in Plan {_PHASE}-03 — see 05-VALIDATION.md task 5-03-05')`. Docstring naming the contract: "assert_frame_equal(build(forwards_phase05_seed42), baseline_pfc_seed42_phase05, atol=1e-12, rtol=0, check_exact=False) + identical columns/dtypes/index/sort order (5bis-A REVIEWS addendum)."

       j. **`test_phase05_baseline_5bisA_via_enforce_true` (STUBBED-SKIPPED, SC #5 legacy backward-compat)** — body: `pytest.skip(f'populated in Plan {_PHASE}-03 — see 05-VALIDATION.md task 5-03-06')`. Docstring naming the contract: "Build with `enforce_positivity=True, enforce_m_factor_floor=True, enforce_floor=True, allow_negative_peak=False` reproduces 5bis-A baseline_pfc_seed42.parquet atol=1e-12 rtol=0 (rollback path testable via D-A2-3)."

    5. Each populated test (a, b) must include in its docstring: the test ID (e.g., "5-01-02"), the requirement IDs covered (e.g., "NEG-01, NEG-05"), and a reference to 05-VALIDATION.md.

    6. Each stubbed test must include a docstring with: the test ID, the owning plan, a one-sentence description of the contract (for `pytest --collect-only -q` output readability), and a `pytest.skip(...)` call with a stable message containing the owner plan ID.

    **Part B — Add inline documentation comment in `tests/conftest.py`:**

    The autouse fixture `_pfc_lt_env_hygiene` already snapshots all `PFC_LT_*` env-var keys by prefix match. No code change is required — `PFC_LT_ALLOW_NEGATIVE_PRICES` (introduced in Plan 05-03) is hygiene-protected automatically.

    Add a one-line inline comment just below the existing `_pfc_lt_env_hygiene` docstring (around current line 28) explicitly listing both flags:
    ```
    # Known PFC_LT_* keys covered by the prefix snapshot:
    # - PFC_LT_USE_SEASONAL_HOURLY_SHAPE (5bis-A D-06, ShapeHourly)
    # - PFC_LT_ALLOW_NEGATIVE_PRICES (Phase 5 D-A2-2, PFCAssembler — introduced in Plan 05-03)
    ```

    Do NOT modify the fixture logic. The two-line comment is the only conftest.py touch.

    **Acceptance: full Phase 5 test stub file imports cleanly and pytest collects all 10 tests.**
  </action>
  <verify>
    <automated>pytest tests/test_phase05_negative_prices.py --collect-only -q 2>&1 | tail -15 && pytest tests/test_phase05_negative_prices.py::test_msfc_signed_monthly_repricing tests/test_phase05_negative_prices.py::test_arbitrage_free_signed_target -x -v 2>&1 | tail -10 && pytest tests/ -q --tb=short 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/test_phase05_negative_prices.py` exits 0.
    - `pytest tests/test_phase05_negative_prices.py --collect-only -q` lists exactly 10 test functions in this order: `test_msfc_signed_monthly_repricing`, `test_arbitrage_free_signed_target`, `test_water_value_delta_sign_invariant`, `test_assembler_delta_additive`, `test_cascading_spread_signed_base`, `test_fit_peak_ratios_deprecated`, `test_master_flag_audit_log`, `test_phase05_summer_bowl_negative_acceptance`, `test_phase05_baseline_regression`, `test_phase05_baseline_5bisA_via_enforce_true`.
    - `pytest tests/test_phase05_negative_prices.py::test_msfc_signed_monthly_repricing -x` exits 0 (NEG-01 + NEG-05 covered).
    - `pytest tests/test_phase05_negative_prices.py::test_arbitrage_free_signed_target -x` exits 0 (NEG-02 covered).
    - `pytest tests/test_phase05_negative_prices.py -q --tb=short` exits 0 reporting `2 passed, 8 skipped` for this file alone.
    - `grep -c "populated in Plan 05-02\|populated in Plan 05-03" tests/test_phase05_negative_prices.py` returns 8 (8 stubs with stable skip messages).
    - `grep -q "5-01-02\|5-01-03" tests/test_phase05_negative_prices.py` exits 0 (test-ID traceability matches 05-VALIDATION.md §Per-Task Verification Map).
    - `grep -q "PFC_LT_ALLOW_NEGATIVE_PRICES" tests/conftest.py` exits 0 (inline doc comment landed).
    - `grep -q "PFC_LT_USE_SEASONAL_HOURLY_SHAPE" tests/conftest.py` exits 0 (5bis-A reference preserved).
    - `pytest tests/ -q --tb=short` exits 0 reporting `260 passed, 11 skipped` (full suite: 258 prior + 2 new populated = 260 passed ; 3 prior skipped + 8 stubbed = 11 skipped).
  </acceptance_criteria>
  <done>Phase 5 test scaffold landed with all 10 tests collectible; 2 populated and green (NEG-01, NEG-02, NEG-05); 8 stubbed-skipped with stable cross-plan owner messages; conftest documentation comment added.</done>
</task>

</tasks>

<verification>
After all four tasks complete, run the full validation gate (sampling rate per 05-VALIDATION.md):

```bash
# Quick gate (per-task feedback)
pytest tests/test_phase05_negative_prices.py -x -q

# Full suite gate (per-wave merge)
pytest tests/ -q --tb=short

# Tolerance contract gate — already enforced by 5bis-A regression tests (atol=1e-12, rtol=0)
pytest tests/test_shape_hourly_infra.py::test_baseline_regression -x -q
```

Expected outcomes:
- `pytest tests/test_phase05_negative_prices.py -q` → `2 passed, 8 skipped` (NEG-01 + NEG-02 + NEG-05 populated; NEG-03 + NEG-04 + acceptance + regressions stubbed for plans 02/03).
- `pytest tests/ -q` → `260 passed, 11 skipped` (full suite baseline preserved).
- `pytest tests/test_shape_hourly_infra.py::test_baseline_regression -x -q` → `2 passed` (5bis-A regression untouched — this plan does NOT modify the MSFC behavior under positive-only inputs because the existing callsites do not pass `enforce_positivity=True`; the 5bis-A baseline was generated with the legacy floor behavior, and `enforce_positivity=False` (default) on positive-only inputs produces the same numerical output since `B_smooth.min() >= 1.0` would have been clipped by the floor but never reaches below 1.0 in the 5bis-A fixture).

Doc gate:
```bash
grep -c "monthly forward négatif" .planning/REQUIREMENTS.md   # → 1
grep -c "Cal'27 forward coté -10" .planning/REQUIREMENTS.md   # → 0 (old wording removed)
```
</verification>

<success_criteria>
This plan is complete when:
- [x] REQUIREMENTS.md NEG-05 reformulated per D-A4-7 (old "Cal'27=-10" wording removed; new "monthly forward négatif July M-07'27 = -2 €/MWh" wording in place with D-A4-7 + 2026-05-19 provenance inline).
- [x] `pfc_shaping/lt/model/msfc_spline.py::smooth_base_prices` exposes `enforce_positivity: bool = False` (default OFF = negative-ready), propagates the kwarg to `_enforce_mean_constraints`, and both floors at l.131 (smooth_base_prices) and l.203 (_enforce_mean_constraints) are conditional under the same flag. The signed-aware clamp at l.120 is in place with `margin = 0.5 * np.ptp(y_knots)`.
- [x] `pfc_shaping/calibration/arbitrage_free.py::ArbitrageFreeCalibrator.__init__` accepts `enforce_m_factor_floor: bool = False` (default OFF). The m_factor floor at l.517 is conditional. When the clip actually mutates m_factor under `enforce_m_factor_floor=True`, `converged=False` is propagated (NEG-02 literal) and a WARN log emitted with the clipped-timestamp count.
- [x] `tests/test_phase05_negative_prices.py` exists with 10 tests: 2 populated and green (`test_msfc_signed_monthly_repricing`, `test_arbitrage_free_signed_target`) covering NEG-01, NEG-02, NEG-05; 8 stubbed-skipped with stable cross-plan owner messages (`populated in Plan 05-02` × 2, `populated in Plan 05-03` × 6).
- [x] `tests/conftest.py` gains an inline doc comment naming both `PFC_LT_USE_SEASONAL_HOURLY_SHAPE` and `PFC_LT_ALLOW_NEGATIVE_PRICES` under the prefix-based hygiene fixture (no code change).
- [x] Pre-Phase-5 test suite (258 passed, 3 skipped) preserved; full suite ends at `260 passed, 11 skipped`.
- [x] No callsite legacy regression — `pfc_shaping/pipeline/autoresearch.py:234` and `pfc_shaping/pipeline/production_phases.py` (ArbitrageFreeCalibrator instantiations) still work without passing the new kwargs (they pick up the default OFF / negative-ready behavior, which is the Phase 5 intended math change).
- [x] RESEARCH §Open Question #1 (mode "multiplicative" assumption A3) closed: SUMMARY records grep result for `ArbitrageFreeCalibrator(` callsites and confirms `mode=` argument used at each.
</success_criteria>

<output>
Create `.planning/phases/05-msfc-log-prix-retire-silent-floors/05-01-SUMMARY.md` when done. Include:
- The four file diffs (REQUIREMENTS.md, msfc_spline.py, arbitrage_free.py, test_phase05_negative_prices.py) summary by line count.
- The grep result for `ArbitrageFreeCalibrator(` mode= audit (RESEARCH §Open Question #1 resolution).
- Test count before/after (`258 passed, 3 skipped` → `260 passed, 11 skipped`).
- Decisions / pitfalls / assumptions encountered (any deviation from RESEARCH §Pattern 1, 2 must be flagged).
- Cross-plan handoff notes: Plan 05-02 will populate `test_water_value_delta_sign_invariant` + `test_assembler_delta_additive`; Plan 05-03 will populate the remaining 6 stubs.
</output>