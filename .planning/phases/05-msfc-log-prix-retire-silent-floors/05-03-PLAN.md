---
phase: 05-msfc-log-prix-retire-silent-floors
plan: 03
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified:
  - pfc_shaping/calibration/cascading.py
  - pfc_shaping/lt/model/assembler.py
  - pfc_shaping/pipeline/autoresearch.py
  - pfc_shaping/pipeline/production_phases.py
  - pfc_shaping/pipeline/rolling_update.py
  - tests/fixtures/_generate_phase05_fixture.py
  - tests/fixtures/forwards_phase05_seed42.parquet
  - tests/fixtures/baseline_pfc_seed42_phase05.parquet
  - tests/test_phase05_negative_prices.py
  - .planning/PROJECT.md
autonomous: true
requirements:
  - NEG-04
  - NEG-05
requirements_addressed:
  - NEG-04
  - NEG-05
must_haves:
  truths:
    - "`pfc_shaping/calibration/cascading.py::ContractCascader` exposes a new public method `fit_peak_spreads(spot_history: pd.DataFrame) -> 'ContractCascader'` (D-A4-1) that aggregates monthly `peak_avg - base_avg` per month from `spot_history` and persists `self.peak_base_spreads_: dict[int, float]` (keys 1..12, values in €/MWh). Also adds `allow_negative_peak: bool = True` to `ContractCascader.__init__` (D-A2-1, NEG-04)."
    - "`synthesize_peak_prices` uses `result[peak_key] = base_price + self.peak_base_spreads_.get(month, default_spread)` when `self.allow_negative_peak=True` (default) and `peak_base_spreads_` is available — sign-invariant: a negative `base_price=-10` with `spread=+5` produces `peak=-5` (NOT the legacy multiplicative `ratio × -10 = -10.5` which inverts the spread semantic in the signed regime). When `peak_base_spreads_` is absent (caller never called `fit_peak_spreads`), a default spread `5.0 €/MWh` is used with `logger.warning('No fitted peak spreads — using default 5.0 €/MWh')` (RESEARCH Pitfall 4)."
    - "Legacy backward-compat preserved per D-A4-3: when `self.allow_negative_peak=False` (operator rollback), `synthesize_peak_prices` keeps the historical multiplicative `result[peak_key] = base_price * ratio` + the implicit `Peak >= Base` clip (the '≥1 ratio'). Existing fitted `peak_base_ratios_` cache is honored."
    - "`fit_peak_ratios` is DEPRECATED per D-A4-2 with the UNIFIED shim contract per codex action #2 (2026-05-19, 05-REVIEWS.md): `fit_peak_ratios(spot_history)` emits `warnings.warn(..., DeprecationWarning, stacklevel=2)` with migration message pointing to `fit_peak_spreads`, then ALWAYS delegates to `self.fit_peak_spreads(spot_history)`, AND ALSO populates `self.peak_base_ratios_: dict[int, float]` derived from `peak_base_spreads_` during the deprecation window. The derivation formula per codex action #2 suggestion: `peak_base_ratios_ = {m: 1.0 + spread / max(base_price_m, 1.0) for m, spread in peak_base_spreads_.items()}` where `base_price_m` is the mean spot price for that month (computed during the same `fit_peak_spreads` call and cached). This ensures legacy callers that read `peak_base_ratios_` directly continue to work transparently during the deprecation window."
    - "`pfc_shaping/lt/model/assembler.py::PFCAssembler.__init__` reads `PFC_LT_ALLOW_NEGATIVE_PRICES` env-var ONCE at init via a `_resolve_allow_negative` helper transposed from the 5bis-A `_resolve_flag` pattern (D-A2-2, D-A2-4 resolved in RESEARCH §Pattern 5). The resolved boolean is stored on `self._allow_negative_prices`. The flag is INFO-AUDIT-LOG ONLY (not an override): `logger.info('PFC_LT_ALLOW_NEGATIVE_PRICES=%s, floors_disabled={msfc:enforce_positivity=%s, af:m_factor_floor=%s, wv:floor=%s, cascading:allow_neg_peak=%s}', ...)`."
    - "**B1/B4 Approach B (checker resolution 2026-05-19):** `PFCAssembler.__init__` ALSO accepts four explicit floor kwargs that map 1:1 to the audit log fields and constitute the authoritative D-A2-3 rollback API: `enforce_positivity: bool = False`, `enforce_m_factor_floor: bool = False`, `enforce_floor: bool = False`, `allow_negative_peak: bool = True`. Defaults are negative-ready (D-A2-1). Forwarding: (a) `enforce_positivity` is passed to `smooth_base_prices(..., enforce_positivity=self.enforce_positivity)` at the MSFC callsite verified at assembler.py:397 — propagates to `_enforce_mean_constraints` per Plan 05-01 RESEARCH Pitfall 1; (b) `enforce_m_factor_floor` overrides `self.calibrator.enforce_m_factor_floor` at `__init__` time when True; (c) `enforce_floor` overrides `self.wv.enforce_floor` at `__init__` time when True; (d) `allow_negative_peak=False` overrides `self.cascader.allow_negative_peak` at `__init__` time. The audit log resolves each floor state via `or` semantics so it reports the maximal floor state (PFCAssembler-level kwarg OR sub-component attribute) — guarantees the log never under-reports an active floor."
    - "No new sidecar parquet is created for `PFC_LT_ALLOW_NEGATIVE_PRICES` (D-A2-4 resolved in RESEARCH §Pattern 5 §Décision sidecar): the flag is audit-trail INFO-LOG only, not a persisted calibration parameter. The four ctor args of the sub-components are the persistence surface (each sub-component owns its own sidecar if any — e.g. ShapeHourly's `shape_hourly.meta.parquet` from 5bis-A)."
    - "`tests/fixtures/_generate_phase05_fixture.py` is a committed reproducible generator (5bis-A `_generate_baseline.py` / 5bis-B `_generate_bowl_fixture.py` pattern). Produces `tests/fixtures/forwards_phase05_seed42.parquet` with: Cal'27=30 €/MWh, July M-07'27=20 €/MWh (dépressé), autres months positifs typiques EEX (Cal'26=28, Cal'28=32, Q1'27=35, Q3'27=22, M-01'27=33, etc.), seed=42, deterministic. The exact set of contracts mirrors the EEX product taxonomy used in `_generate_baseline.py` (Cal, Q, M for at least 2 years horizon)."
    - "`tests/fixtures/forwards_phase05_seed42.parquet` is committed (binary). Re-running the generator produces a byte-identical parquet (determinism check via `sha256sum`)."
    - "`tests/fixtures/baseline_pfc_seed42_phase05.parquet` is committed as the new canonical Phase-5 baseline. Generated AFTER all Phase 5 math has shipped (Plans 05-01, 05-02, this plan): `PFCAssembler.build(forwards_phase05_seed42, seed=42, PFC_LT_USE_SEASONAL_HOURLY_SHAPE=1, PFC_LT_ALLOW_NEGATIVE_PRICES=1)` with default ctor args (enforce_positivity=False, enforce_m_factor_floor=False, enforce_floor=False, allow_negative_peak=True). Convention D-A4-9 5bis-B 'new baseline per math change atomique'. Test `test_phase05_baseline_regression` asserts `assert_frame_equal(build(...), baseline, atol=1e-12, rtol=0, check_exact=False)` + identical columns/dtypes/index/sort order."
    - "Seven stubbed tests from Plan 05-01 (the 6 originals + the codex action #7 fallback-spread stub `test_fit_peak_spreads_empty_spot_history`) are promoted to populated and green in this plan: `test_cascading_spread_signed_base` (NEG-04 unit), `test_fit_peak_ratios_deprecated` (NEG-04 unit, asserts the UNIFIED shim contract per codex action #2 — peak_base_spreads_ AND peak_base_ratios_ both populated), `test_master_flag_audit_log` (unit), `test_phase05_summer_bowl_negative_acceptance` (SC #2 ROADMAP acceptance, gated by 5bis-B bowl marker, with EXPLICIT UTC→Europe/Zurich conversion per codex action #4), `test_phase05_baseline_regression` (SC #5 regression), `test_phase05_baseline_5bisA_via_enforce_true` (SC #5 legacy backward-compat), and the NEW `test_fit_peak_spreads_empty_spot_history` (codex action #7 fallback spread + warning)."
    - "SC #2 acceptance test gating per D-A4-5 + codex action #4: `test_phase05_summer_bowl_negative_acceptance` is SKIPPED with message `'Phase 5 SC #2 requires 5bis-B bowl deepening calibrated first'` when the 5bis-B bowl marker `tests/fixtures/baseline_pfc_seed42_bowl.parquet` is absent. When present, the test loads forwards_phase05_seed42, sets BOTH `PFC_LT_USE_SEASONAL_HOURLY_SHAPE=1` and `PFC_LT_ALLOW_NEGATIVE_PRICES=1`, builds the PFC, AND performs an EXPLICIT `idx_local = pfc.index.tz_convert('Europe/Zurich')` conversion BEFORE applying Sunday/h13 masks (`idx_local.day_name() == 'Sunday'` and `idx_local.hour == 13`) — codex action #4 (05-REVIEWS.md lines 98-99) flagged that the PFC index is UTC but 'h13 Sunday' is local-time semantics, so direct UTC-index filtering would shift the Sunday/h13 window by 1-2 hours and either miss the bowl trough or include adjacent non-bowl hours. Asserts `pfc[mask].mean() < -20.0 €/MWh` (SC #2 ROADMAP literal threshold)."
    - "`pfc_shaping/pipeline/autoresearch.py:234` and `pfc_shaping/pipeline/production_phases.py:344,644` and `pfc_shaping/pipeline/rolling_update.py:365` (4 production callsites identified in RESEARCH §Runtime State Inventory) adopt the Phase 5 negative-ready defaults at the PFCAssembler construction sites: NO explicit `enforce_*=True` / `allow_negative_peak=False` kwarg is added at these production callsites (the PFCAssembler ctor defaults from B1/B4 Approach B handle it — `enforce_positivity=False, enforce_m_factor_floor=False, enforce_floor=False, allow_negative_peak=True`). Operator rollback per D-A2-3 = pass the four kwargs explicitly at the PFCAssembler construction site (not at the sub-component construction site — both work because of the `or` semantics in the audit log, but the PFCAssembler-level kwargs are the documented API). Migration from `fit_peak_ratios(spot_history)` to `fit_peak_spreads(spot_history)` is done explicitly at production_phases.py:344 and production_phases.py:644 (the two confirmed callsites in RESEARCH §Dry-Run §callsite audit) — this avoids relying on the deprecation shim long-term while preserving it as a safety net."
    - "`.planning/PROJECT.md` `## Key Decisions` gains an entry `D-FLIP-2: Phase 5 livré 2026-05-19, defaults negative-ready, master flag PFC_LT_ALLOW_NEGATIVE_PRICES audit-trail INFO only (D-A2-2). Rollback opérateur = construire PFCAssembler(..., enforce_positivity=True, enforce_m_factor_floor=True, enforce_floor=True, allow_negative_peak=False) au callsite production — les quatre kwargs sont sur PFCAssembler.__init__ par B1/B4 Approach B (D-A2-3).` per D-A5-1 final-plan obligation (CONTEXT.md §decisions §Plan decomposition)."
    - "Cross-cutting truth (appears in all 3 plans): sign-invariance is BY CONSTRUCTION — spread additif Peak adds €/MWh so `peak = base + spread` has the correct sign in both regimes (B<0: peak=-10+5=-5; B>0: peak=80+5=85). Versus the LEGACY multiplicative `base × ratio` (ratio>=1) which inverts the spread semantic when base<0 (the peak ratio would have to be <1 for the peak to be less negative than the base, but the existing fit constrains ratio >= 1)."
    - "Cross-cutting truth (appears in all 3 plans): tolerance contract atol=1e-12 rtol=0 identical columns/dtypes/index/sort order — applied to BOTH `baseline_pfc_seed42_phase05.parquet` (new canonical) and the rollback-path 5bis-A test `test_phase05_baseline_5bisA_via_enforce_true`."
    - "Cross-cutting truth (appears in all 3 plans): master flag is NOT an override — the four ctor args are the API. Plan 05-03 makes this explicit by documenting the operator rollback path in PROJECT.md."
    - "Codex action #5 (commit boundary split per 05-REVIEWS.md lines 100, 119, 2026-05-19): the 8 tasks of this plan land as TWO distinct commits within the same wave (no plan restructuring, files_modified preserved, depends_on preserved). Commit 1: `refactor(05): cascading spread-additive + production callsites migration` covers Tasks 1 + 4 (cascading.py changes + production_phases.py + autoresearch.py + rolling_update.py callsite doc + 2 fit_peak_ratios→fit_peak_spreads migrations). Commit 2: `feat(05): master flag + fixtures + phase05 tests + docs` covers Tasks 2 + 3 + 5 + 6 + 7 + 8 (assembler.py master flag + 4 ctor args, _generate_phase05_fixture.py + parquet, baseline parquet, all 7 stub→populated, PROJECT.md D-FLIP-2). The commit boundary helps reviewer triage and provides a clean rollback point if the master flag work needs revision while the cascading + production migrations stay shipped."
    - "Plan 05-03 ships 8 tasks/10 files in a single wave per D-A6-1 (scope-cap exception); commit boundary surfaces via Task 4 (`refactor(05): cascading spread-additive + production callsites migration`) and Task 8 (`feat(05): master flag + fixtures + phase05 tests + docs`)."
  artifacts:
    - path: "pfc_shaping/calibration/cascading.py"
      provides: "`ContractCascader(..., allow_negative_peak: bool = True)` ctor kwarg; new public method `fit_peak_spreads(spot_history) -> ContractCascader` persisting `peak_base_spreads_: dict[int, float]`; `synthesize_peak_prices` spread-additive when allow_negative_peak=True; `fit_peak_ratios` DEPRECATED with UNIFIED shim contract per codex action #2 — transparent shim + DeprecationWarning + derived `peak_base_ratios_` populated during deprecation window."
      contains: "fit_peak_spreads"
    - path: "pfc_shaping/lt/model/assembler.py"
      provides: "`PFCAssembler.__init__` reads PFC_LT_ALLOW_NEGATIVE_PRICES once via `_resolve_allow_negative`, emits one INFO audit log with the 4 floor states. No new sidecar. ALSO accepts four explicit floor kwargs `enforce_positivity`, `enforce_m_factor_floor`, `enforce_floor`, `allow_negative_peak` (B1/B4 Approach B)."
      contains: "PFC_LT_ALLOW_NEGATIVE_PRICES"
    - path: "pfc_shaping/pipeline/autoresearch.py"
      provides: "Negative-ready defaults preserved by NOT passing enforce_*=True — Phase 5 default is already negative-ready. Inline comment names the operator rollback path."
      contains: "Phase 5 default"
    - path: "pfc_shaping/pipeline/production_phases.py"
      provides: "Migration of two callsites from fit_peak_ratios → fit_peak_spreads (lines 344, 644 per RESEARCH §Dry-Run §callsite audit)."
      contains: "fit_peak_spreads"
    - path: "pfc_shaping/pipeline/rolling_update.py"
      provides: "Inline comment naming the Phase 5 default at line 365 (negative-ready). No code change needed since no explicit enforce_*=True is passed."
      contains: "Phase 5"
    - path: "tests/fixtures/_generate_phase05_fixture.py"
      provides: "Deterministic Phase-5 forwards fixture generator (seed=42)."
      contains: "build_phase05_forwards"
    - path: "tests/fixtures/forwards_phase05_seed42.parquet"
      provides: "Committed fixture: Cal'27=30, July M-07'27=20 dépressé, autres months positifs typiques EEX."
    - path: "tests/fixtures/baseline_pfc_seed42_phase05.parquet"
      provides: "Committed canonical Phase-5 baseline (post-floors-off, post-delta-additif WV, post-spread-additif Peak)."
    - path: "tests/test_phase05_negative_prices.py"
      provides: "Seven stubbed tests promoted to populated-and-green (or gated-skip for SC #2 with explicit UTC→Europe/Zurich conversion per codex action #4)."
      contains: "test_phase05_summer_bowl_negative_acceptance"
    - path: ".planning/PROJECT.md"
      provides: "D-FLIP-2 entry documenting Phase 5 ship + defaults + operator rollback."
      contains: "D-FLIP-2"
  key_links:
    - from: "pfc_shaping/calibration/cascading.py::ContractCascader.synthesize_peak_prices"
      to: "pfc_shaping/calibration/cascading.py::ContractCascader.peak_base_spreads_"
      via: "spread-additive lookup `result[peak_key] = base_price + self.peak_base_spreads_.get(month, default_spread)` when allow_negative_peak=True"
      pattern: "peak_base_spreads_"
    - from: "pfc_shaping/calibration/cascading.py::ContractCascader.fit_peak_ratios"
      to: "pfc_shaping/calibration/cascading.py::ContractCascader.fit_peak_spreads"
      via: "UNIFIED shim contract per codex action #2: transparent shim — DeprecationWarning emitted then `return self.fit_peak_spreads(spot_history)` AND derived `peak_base_ratios_` populated during deprecation window via `{m: 1.0 + spread / max(base_price_m, 1.0)}`. Legacy attribute readers see a populated dict, not AttributeError."
      pattern: "DeprecationWarning"
    - from: "pfc_shaping/lt/model/assembler.py::PFCAssembler.__init__"
      to: "os.environ['PFC_LT_ALLOW_NEGATIVE_PRICES']"
      via: "single env read via `_resolve_allow_negative` helper (5bis-A `_resolve_flag` transposed pattern), result stored on self._allow_negative_prices, INFO log emitted once"
      pattern: "_resolve_allow_negative"
    - from: "tests/test_phase05_negative_prices.py::test_phase05_baseline_regression"
      to: "tests/fixtures/baseline_pfc_seed42_phase05.parquet"
      via: "assert_frame_equal(build(forwards_phase05_seed42), baseline, atol=1e-12, rtol=0, check_exact=False) + identical columns/dtypes/index/sort order"
      pattern: "baseline_pfc_seed42_phase05"
    - from: "tests/test_phase05_negative_prices.py::test_phase05_baseline_5bisA_via_enforce_true"
      to: "tests/fixtures/baseline_pfc_seed42.parquet"
      via: "build with enforce_positivity=True, enforce_m_factor_floor=True, enforce_floor=True, allow_negative_peak=False AND assert_frame_equal atol=1e-12 rtol=0 against the 5bis-A baseline"
      pattern: "baseline_pfc_seed42.parquet"
    - from: "tests/test_phase05_negative_prices.py::test_phase05_summer_bowl_negative_acceptance"
      to: "tests/fixtures/baseline_pfc_seed42_bowl.parquet (5bis-B marker, gate only — NOT compared to)"
      via: "if not _BOWL_MARKER_PATH.exists(): pytest.skip('Phase 5 SC #2 requires 5bis-B bowl deepening calibrated first') ; ELSE `idx_local = pfc.index.tz_convert('Europe/Zurich')` THEN mask = (idx_local.day_name() == 'Sunday') & (idx_local.hour == 13) & (idx_local.month == 7) — codex action #4 explicit local-time conversion"
      pattern: "Europe/Zurich"
    - from: "tests/test_phase05_negative_prices.py::test_fit_peak_spreads_empty_spot_history"
      to: "pfc_shaping/calibration/cascading.py::ContractCascader.fit_peak_spreads"
      via: "calls `cascader.fit_peak_spreads(pd.DataFrame())` and asserts `pytest.warns(UserWarning|RuntimeWarning) and cascader.peak_base_spreads_ falls back to {m: 5.0 for m in 1..12}` — codex action #7 fallback-spread regression"
      pattern: "test_fit_peak_spreads_empty_spot_history"
---

## Revision Log (--reviews iteration 1)

Surgical revisions applied 2026-05-19 to incorporate codex review action items
#2, #4, #5, and #7 from `.planning/phases/05-msfc-log-prix-retire-silent-floors/05-REVIEWS.md`
lines 113-122. Plan structure (wave, depends_on, files_modified, autonomous)
preserved. The 8 existing task IDs are stable; action #5 is a commit-boundary
split applied during execution (no new task — commit-message mapping documented
in the must_haves table above and in the verification section below).

| Action # | Priority | Change | Where |
|----------|----------|--------|-------|
| 2 | HIGH | Unify `fit_peak_ratios` shim contract: ALWAYS redirect to `fit_peak_spreads` AND populate `peak_base_ratios_` derived during deprecation window via `{m: 1.0 + spread / max(base_price_m, 1.0)}`. Update RESEARCH.md §Pattern 6 cross-reference + Task 1 Edit C + Task 6 test_fit_peak_ratios_deprecated assertions. | Task 1 Edit C + Task 6 Edit B + acceptance criteria |
| 4 | MEDIUM | Document EXPLICIT `idx_local = pfc.index.tz_convert('Europe/Zurich')` conversion in `test_phase05_summer_bowl_negative_acceptance` BEFORE applying Sunday/h13/July masks. The PFC index is UTC; 'h13 Sunday' is local-time semantics. | Task 6 Edit D + acceptance criteria |
| 5 | MEDIUM | Split wave 3 execution into TWO commits (no plan restructuring): Commit 1 `refactor(05): cascading spread-additive + production callsites migration` covers Tasks 1 + 4. Commit 2 `feat(05): master flag + fixtures + phase05 tests + docs` covers Tasks 2 + 3 + 5 + 6 + 7 + 8. | Commit-boundary mapping in this Revision Log + verification section + must_haves table |
| 7 | LOW | Add NEW test `test_fit_peak_spreads_empty_spot_history` populated and green — exercises the fallback spread + warning path when `spot_history` is empty / insufficient. (Plan 05-01 scaffold ALREADY stubs this entry at position 14; this plan populates it.) | Task 6 Edit F (new sub-edit) + acceptance criteria |

Counts updated end-of-plan:
- Plan 05-02 ends at: `266 passed, 10 skipped` (15 tests in file: 8 populated + 7 stubs).
- This plan flips 7 stubs to populated (6 originals from Plan 05-01 + 1 codex action #7 from Plan 05-01 scaffold).
- Test file final size: 15 tests (unchanged from end-of-Plan-05-02; this plan does not add new test functions, only populates the 7 remaining stubs).
- Populated count end-of-plan: 8 (post-Plan-05-02) + 7 (this plan's promotions) = 15.
- Skip-stubbed count end-of-plan: 7 - 7 = 0 (no stubs remain in the file).
- Best-case full suite: `266 + 7 = 273 passed, 10 - 7 = 3 skipped` (5bis-B bowl marker present, all 15 phase05 tests green) — corrected counts from the pre-revision values.
- SC #2 gated-skip variant: `266 + 6 = 272 passed, 3 + 1 = 4 skipped` (5bis-B bowl marker absent, SC #2 gated-skip — acceptable per D-A4-5).

Commit-boundary mapping for action #5:
| Commit message | Tasks | Files modified |
|----------------|-------|----------------|
| `refactor(05): cascading spread-additive + production callsites migration` | 1, 4 | cascading.py + autoresearch.py + production_phases.py + rolling_update.py |
| `feat(05): master flag + fixtures + phase05 tests + docs` | 2, 3, 5, 6, 7, 8 | assembler.py + _generate_phase05_fixture.py + forwards_phase05_seed42.parquet + baseline_pfc_seed42_phase05.parquet + test_phase05_negative_prices.py + PROJECT.md |

## Scope-Cap Exception (D-A6-1)

Plan 05-03 ships 8 tasks across 10 files in a single wave — exceeding the gsd planner Dimension 5 task cap (5 tasks/plan). The exception is locked in `05-CONTEXT.md` §`<decisions>` §`D-A6-1` (added during revision iteration 2).

**Why the exception exists:** Codex review action #5 (`05-REVIEWS.md` line 119) explicitly mandates "Split wave 3 into two commits — **No plan restructuring**, commit boundary only." Splitting Plan 05-03 into 05-03a / 05-03b would directly contradict the upstream reviewer's directive.

**How the audit trail is preserved:** The executor processes the 8 tasks as a single wave (PLAN.md is loaded once), but two distinct git commits are emitted per the codex-action-#5 commit-boundary mapping documented in the iteration 1 Revision Log above:
- Commit 1 (after Task 4): `refactor(05): cascading spread-additive + production callsites migration` — Tasks 1 + 4.
- Commit 2 (after Task 8): `feat(05): master flag + fixtures + phase05 tests + docs` — Tasks 2, 3, 5, 6, 7, 8.

**Reviewer entry point:** the two commits provide reviewer triage and a clean rollback point even though the plan is loaded as one. Per the Plan-Checker iteration 2 finding, this is the operator-visible justification for the cap exception.

## Revision Log (--reviews iteration 2)

Surgical edits applied 2026-05-19 to address plan-checker iteration 2 BLOCKERs and WARNINGs (no plan restructuring):

| Issue | Severity | Change | Where |
|-------|----------|--------|-------|
| BLOCKER 2 | HIGH | Document scope-cap exception (8 tasks / 10 files in W3). Three coordinated edits: (a) new locked decision D-A6-1 added to `05-CONTEXT.md` §`<decisions>` §Area 6; (b) new `## Scope-Cap Exception (D-A6-1)` section in this plan (immediately above this log) summarising why splitting would contradict codex action #5; (c) new `must_haves.truths[18]` in the frontmatter naming the commit-boundary mechanism. **No plan restructuring, commit-boundary mapping preserved.** | CONTEXT.md D-A6-1 + this plan's `## Scope-Cap Exception` section + `must_haves.truths[18]` |
| WARNING 2 | MEDIUM | Task 4 `<verify>` block rewritten to use explicit `if grep ...; then echo FAIL ...; exit 1; fi` form. The previous compound `! grep -nrE ... | grep -v ... | grep -v ... && grep -nE ... | wc -l && ...` was fragile: a leading `!` applied to a pipeline plus the `grep -v` returning 1 could flip the negation to success and mask actual hits. The replacement writes active hits to `/tmp/active_callsites.txt`, fails loudly when the file is non-empty, and decouples the audit (negative assertion) from the verification (positive assertions on `fit_peak_spreads` and `Phase 5 D-A2-1`). | Task 4 `<verify>` block |
| WARNING 3 | MEDIUM | Task 4 audit grep extended from `pfc_shaping/pipeline/` to the whole `pfc_shaping/` subtree (covers any unforeseen callsite outside pipeline/). Added an acceptance-criteria note documenting that `notebooks/` and `scripts/` outside the `pfc_shaping/` package are tolerated via the DeprecationWarning shim safety net (the unified codex-action-#2 shim contract from Task 1 Edit C is the long-term escape hatch). | Task 4 `<verify>` audit scope + acceptance criteria narrative |
| WARNING 4 | MEDIUM | Task 7 acceptance criterion `grep -c "monkeypatch\." tests/test_phase05_negative_prices.py | awk '$1 == 0 \|\| exit 1'` replaced with pure bash `! grep -q 'monkeypatch\.' tests/test_phase05_negative_prices.py`. The `awk '$1 == 0 \|\| exit 1'` idiom parses inconsistently across BSD vs GNU awk; the bash replacement is portable and exits 0 when no match is found. | Task 7 acceptance criteria |

All four issues are surgical edits — no task IDs were renumbered, no new tasks added, no `files_modified` entries changed, no `depends_on` changed. The iteration 1 codex-action-#5 commit-boundary mapping is preserved verbatim.

<!-- W2 task count rationale (accepted with note): 8 tasks bundle cascading math (D-A4-1), master flag audit (D-A2-2), production callsite audit (RESEARCH §Callsite Audit), fixture+baseline generation (D-A4-5/D-A4-6), and 7 tests + 1 PROJECT.md update. These are tightly coupled by the math → assembler → callsite → fixture → test → docs causal chain. Splitting would force fixture/baseline duplication or out-of-order test stubs. Per the gsd planner's <scope_estimation> §Split Signals review (revision 2026-05-19): the 8-task aggregation is justified by single-concern coupling (Phase 5 closure) and inter-task ordering dependencies; no FILE split is recommended. Codex action #5 (2026-05-19) asks for a COMMIT-boundary split within the same plan — implemented in the Revision Log mapping above. -->

<objective>
Implement the LAST Phase 5 plan — Area 4 (BlockCascading spread additif) + the master flag audit-trail + the Phase 5 canonical fixture and baseline + the remaining seven tests (1 NEG-04 unit + 1 deprecation unit per codex action #2 unified contract + 1 master-flag unit + 1 SC #2 acceptance with codex action #4 explicit UTC→Europe/Zurich conversion + 2 regressions + 1 codex action #7 fallback-spread). Also lands the 4-callsite production audit and the PROJECT.md D-FLIP-2 entry. The 8 tasks land as TWO distinct commits per codex action #5. Concretely:

1. **Cascading spread additif (NEG-04, D-A4-1, D-A4-2, D-A4-3, codex action #2)**: Add `allow_negative_peak: bool = True` to `ContractCascader.__init__` (default ON per D-A2-1 = negative-ready). Add public method `fit_peak_spreads(spot_history)` that aggregates `peak_avg - base_avg` per month and persists `self.peak_base_spreads_: dict[int, float]`. Refactor `synthesize_peak_prices` to use spread additif (`result[peak_key] = base_price + spread`) when `allow_negative_peak=True`. Keep the legacy multiplicative path under `allow_negative_peak=False`. `fit_peak_ratios` becomes DEPRECATED with the UNIFIED SHIM CONTRACT per codex action #2: ALWAYS redirect to `fit_peak_spreads` AND populate `peak_base_ratios_` derived as `{m: 1.0 + spread / max(base_price_m, 1.0)}` during the deprecation window so legacy attribute readers continue to work.

2. **Master flag audit-trail (D-A2-2, D-A2-4 resolved RESEARCH §Pattern 5)**: Add a `_resolve_allow_negative(explicit)` helper to `assembler.py` modeled on the 5bis-A `_resolve_flag` pattern. Add `allow_negative_prices: bool | None = None` PLUS the four explicit floor kwargs (`enforce_positivity`, `enforce_m_factor_floor`, `enforce_floor`, `allow_negative_peak`) to `PFCAssembler.__init__` signature (B1/B4 Approach B). In `__init__`, resolve the flag once via `self._allow_negative_prices = _resolve_allow_negative(allow_negative_prices)` and emit ONE INFO audit log with the four floor states introspected from the four sub-components. Forward the four floor kwargs to the sub-components per the B1/B4 Approach B forwarding rules (Task 2 details). No sidecar persistence — the flag is audit-trail INFO only per D-A2-2 / RESEARCH decision.

3. **Production callsite audit (RESEARCH §Runtime State Inventory)**: In `pfc_shaping/pipeline/autoresearch.py:234`, `pfc_shaping/pipeline/production_phases.py:344, 644`, and `pfc_shaping/pipeline/rolling_update.py:365`, add inline comments documenting that the Phase 5 defaults are negative-ready (`enforce_*=False`, `allow_negative_peak=True`). At the two `production_phases.py` callsites that call `fit_peak_ratios(spot_history)`, MIGRATE explicitly to `fit_peak_spreads(spot_history)` (per RESEARCH §Dry-Run §callsite audit — both have `spot_history` available). The DeprecationWarning shim with the codex-action-#2 unified contract stays as a safety net but is not relied upon by production code post-migration.

4. **Fixture + baseline**: Create `tests/fixtures/_generate_phase05_fixture.py` (5bis-A / 5bis-B pattern) producing `tests/fixtures/forwards_phase05_seed42.parquet` with Cal'27=30, July M-07'27=20 dépressé, autres months positifs typiques EEX, seed=42. After all four production edits are in place AND the Phase 5 default math is stable, generate the canonical Phase-5 baseline `tests/fixtures/baseline_pfc_seed42_phase05.parquet` by running `assembler.build(forwards_phase05_seed42, seed=42, PFC_LT_USE_SEASONAL_HOURLY_SHAPE=1, PFC_LT_ALLOW_NEGATIVE_PRICES=1)` with default ctor args.

5. **Tests (7 stubs → populated)**: Promote the seven remaining stubs in `tests/test_phase05_negative_prices.py` to populated and green (or gated-skip for SC #2). The full test count goes from `266 passed, 10 skipped` (post-Plan-05-02) to `273 passed, 3 skipped` best case (or `272 passed, 4 skipped` if SC #2 is gated-skipped because the 5bis-B bowl marker is absent in this dev environment — both outcomes are acceptable per D-A4-5).

6. **PROJECT.md D-FLIP-2 entry**: Document Phase 5 ship + defaults + operator rollback per D-A5-1.

7. **Codex review action items #2, #4, #5, #7 fully closed in this plan:**
   - **#2** — Task 1 Edit C ships the unified shim contract; Task 6 Edit B asserts `peak_base_ratios_` is populated post-`fit_peak_ratios()` call.
   - **#4** — Task 6 Edit D wires the explicit `idx_local = pfc.index.tz_convert('Europe/Zurich')` conversion in `test_phase05_summer_bowl_negative_acceptance` before applying Sunday/h13/July masks.
   - **#5** — Commit-boundary mapping (Revision Log table above): two commits emitted instead of one, files split by the cascading/master-flag boundary, no plan restructuring.
   - **#7** — Task 6 Edit F populates `test_fit_peak_spreads_empty_spot_history` (the codex-action-#7 stub from Plan 05-01 scaffold position 14).

Purpose: This plan delivers the LAST math change of Phase 5 (cascading spread additif) AND the system-level surface (master flag audit-trail, callsite migrations, canonical baseline). After this plan ships, Phase 5 is COMPLETE and the pipeline produces signed PFC prices by default. Plan 05-03 is intentionally bigger than 05-01/05-02 because it bundles: math (cascading), system (master flag), audit (callsites), fixture/baseline (committed binaries), and tests (7 promotions). RESEARCH §Architectural Responsibility Map confirms this bundling is correct — cascading is a single tier, the master flag belongs to PFCAssembler (the entry point), the fixture/baseline lives in `tests/fixtures/`, and the callsite audit is cross-cutting plumbing. The codex-action-#5 commit-boundary split mitigates the integration density risk identified in 05-REVIEWS.md.

Output: 4 modified production files (`cascading.py`, `assembler.py`, `autoresearch.py`, `rolling_update.py`), 1 modified pipeline file with 2 callsite migrations (`production_phases.py`), 2 new fixture/script files (`_generate_phase05_fixture.py`, `forwards_phase05_seed42.parquet`), 1 new baseline parquet (`baseline_pfc_seed42_phase05.parquet`), 1 test file with 7 tests promoted to populated, 1 docs file update (`PROJECT.md`). Test suite ends at `273 passed, 3 skipped` (or `272 passed, 4 skipped` if SC #2 is gated-skipped). Two commits emitted per codex action #5.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-msfc-log-prix-retire-silent-floors/05-CONTEXT.md
@.planning/phases/05-msfc-log-prix-retire-silent-floors/05-RESEARCH.md
@.planning/phases/05-msfc-log-prix-retire-silent-floors/05-VALIDATION.md
@.planning/phases/05-msfc-log-prix-retire-silent-floors/05-REVIEWS.md
@.planning/phases/05-msfc-log-prix-retire-silent-floors/05-01-SUMMARY.md
@.planning/phases/05-msfc-log-prix-retire-silent-floors/05-02-SUMMARY.md
@.planning/phases/05B-shape-hourly-infrastructure-flag-no-op-refactor/05B-CONTEXT.md
@.planning/phases/05C-shape-hourly-bowl-deepening/05C-CONTEXT.md
@pfc_shaping/calibration/cascading.py
@pfc_shaping/lt/model/assembler.py
@pfc_shaping/lt/model/shape_hourly.py
@pfc_shaping/pipeline/autoresearch.py
@pfc_shaping/pipeline/production_phases.py
@pfc_shaping/pipeline/rolling_update.py
@tests/fixtures/_generate_baseline.py
@tests/fixtures/_generate_bowl_fixture.py
@tests/fixtures/baseline_pfc_seed42.parquet
@tests/test_phase05_negative_prices.py

<interfaces>
Key contracts already established (NOT to re-explore):

From `pfc_shaping/calibration/cascading.py` (current state, pre-Phase-5):
```
class ContractCascader:
    def __init__(self, ...) -> None:                            # line 257 — no allow_negative_peak yet
        ...
        # Phase 5 will add: allow_negative_peak: bool = True

    def fit_peak_ratios(
        self,
        spot_history: pd.DataFrame,
    ) -> "ContractCascader":                                    # line 279 — becomes DEPRECATED shim
        ...
        self.peak_base_ratios_: dict[int, float] = {...}        # legacy ratios per month
        return self

    def synthesize_peak_prices(
        self,
        base_prices: dict[str, float],
        ...
    ) -> dict[str, float]:                                       # line 342
        ...
        # Currently: result[peak_key] = base_price * ratio (with ratio >= 1.0 implicit floor)
```

From `pfc_shaping/lt/model/shape_hourly.py` (5bis-A pattern to transpose to assembler.py):
```
_FLAG_ENV_VAR = "PFC_LT_USE_SEASONAL_HOURLY_SHAPE"             # line 89

def _resolve_flag(explicit: bool | None) -> bool:               # line 92
    """Resolve the flag from explicit arg OR env-var, freeze at init."""
    if explicit is not None:
        return bool(explicit)
    raw = os.getenv(_FLAG_ENV_VAR, "0")
    if raw == "1":
        return True
    if raw == "0":
        return False
    logger.warning("%s=%r invalide — traité comme False", _FLAG_ENV_VAR, raw)
    return False
```
Phase 5 transposes this to `_ALLOW_NEG_ENV_VAR = "PFC_LT_ALLOW_NEGATIVE_PRICES"` + `_resolve_allow_negative(explicit)` in `assembler.py`.

From `pfc_shaping/lt/model/assembler.py` (post-Plan-05-02 state):
```
class PFCAssembler:
    def __init__(
        self,
        shape_hourly: ShapeHourly,
        shape_intraday: ShapeIntraday,
        uncertainty=None,
        water_value=None,
        cascader=None,
        calibrator=None,
        calibration_fallback_to_raw: bool = True,
        peak_source_policy: str = "same_first",
        confidence_thresholds: dict[str, float] | None = None,
    ) -> None:                                                   # line 170
        ...
        # Phase 5 Plan 05-03 will add: allow_negative_prices: bool | None = None kwarg
        # + enforce_positivity / enforce_m_factor_floor / enforce_floor / allow_negative_peak kwargs
        # + _resolve_allow_negative + INFO audit log + forwarding to sub-components
```

From `tests/fixtures/_generate_baseline.py` and `tests/fixtures/_generate_bowl_fixture.py` (5bis-A and 5bis-B patterns — model templates for `_generate_phase05_fixture.py`):
- `def build_pfc(seed: int = 42) -> pd.DataFrame: ...` (5bis-A)
- `def build_bowl_fixture(seed: int = 42) -> tuple[pd.DataFrame, pd.DataFrame]: ...` (5bis-B)
- `def main() -> None: ...` (both) — writes parquet via `to_parquet(_OUT_PATH, index=True)`.

From `tests/test_phase05_negative_prices.py` (post-Plan-05-02 state — INCLUDING the revision-iteration-1 15-test scaffold):
- 15 tests collected. 8 populated and green (Plan 05-01 ×5 + Plan 05-02 ×3 including the new `test_compute_delta_wv_index_alignment`). 7 skip-stubbed with `populated in Plan 05-03` messages (the 6 originals + codex action #7 stub `test_fit_peak_spreads_empty_spot_history`).
- This plan promotes the 7 stubs to populated. The final test count assumes all 7 land green; if SC #2 is gated-skipped (5bis-B bowl marker absent), the count is `272 passed, 4 skipped` instead of `273 passed, 3 skipped`.

Test count at start of this plan: `266 passed, 10 skipped`. This plan must end at one of:
- `273 passed, 3 skipped` (all 7 stubs promoted and green — 5bis-B bowl marker present in CI).
- `272 passed, 4 skipped` (6 stubs green + 1 gated-skip — 5bis-B bowl marker absent — acceptable per D-A4-5).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor `ContractCascader` for spread-additif Peak + DEPRECATED fit_peak_ratios UNIFIED SHIM contract (codex action #2) + allow_negative_peak ctor kwarg (NEG-04) — COMMIT 1 per codex action #5</name>
  <files>pfc_shaping/calibration/cascading.py</files>
  <read_first>
    - pfc_shaping/calibration/cascading.py:250-300 (`ContractCascader.__init__` at line 257 + `fit_peak_ratios` at line 279)
    - pfc_shaping/calibration/cascading.py:340-410 (`synthesize_peak_prices` at line 342 — current multiplicative formula)
    - .planning/phases/05-msfc-log-prix-retire-silent-floors/05-CONTEXT.md §decisions D-A2-1, D-A4-1, D-A4-2, D-A4-3 (ctor arg default True, fit_peak_spreads contract, fit_peak_ratios deprecation, legacy backward-compat under allow_negative_peak=False)
    - .planning/phases/05-msfc-log-prix-retire-silent-floors/05-RESEARCH.md §Architecture Patterns §Pattern 4 §Spread additif ContractCascader (canonical patch shape), §Pattern 6 §fit_peak_ratios backward-compat shim (RESOLVED: transparent shim with DeprecationWarning), §Common Pitfalls §Pitfall 4 (missing peak_base_spreads_ fallback default_spread)
    - .planning/phases/05-msfc-log-prix-retire-silent-floors/05-REVIEWS.md lines 96-97, 113-116 (codex action #2: UNIFIED shim contract — ALWAYS redirect + populate peak_base_ratios_ derived during deprecation window)
    - .planning/phases/05-msfc-log-prix-retire-silent-floors/05-VALIDATION.md §Per-Task Verification Map task 5-03-01 (D-A4-4 canonical test contract)
  </read_first>
  <action>
    Modify `pfc_shaping/calibration/cascading.py` with four surgical edits. This task is the first of TWO commits per codex action #5 (commit message: `refactor(05): cascading spread-additive + production callsites migration`).

    **Edit A — Extend `ContractCascader.__init__` signature (around line 257):**

    Add `allow_negative_peak: bool = True` as a new kwarg (default ON per D-A2-1 = negative-ready). Store `self.allow_negative_peak = bool(allow_negative_peak)`. Update the class docstring with a Phase 5 paragraph naming D-A4-1 / NEG-04: "When True (default, Phase 5 negative-ready), `synthesize_peak_prices` uses the spread-additive formula `peak = base + spread` (sign-invariant). When False (legacy rollback per D-A2-3), uses the historical multiplicative `peak = base × ratio` with implicit `ratio >= 1` (i.e. `Peak >= Base`). The `fit_peak_spreads(spot_history)` public method must be called to populate `peak_base_spreads_` before `synthesize_peak_prices` is invoked on the spread-additive path; absent that, a default spread of 5.0 €/MWh is used with a WARN log (RESEARCH Pitfall 4). The legacy `fit_peak_ratios` method is DEPRECATED with the codex-action-#2 unified shim contract: it always delegates to `fit_peak_spreads` AND populates `peak_base_ratios_` derived as `{m: 1.0 + spread / max(base_price_m, 1.0)}` during the deprecation window — legacy attribute readers continue to work."

    **Edit B — Add public method `fit_peak_spreads` (NEW, place AFTER `fit_peak_ratios`) — cache `base_price_m` for codex action #2 derivation:**

    Signature:
    ```
    def fit_peak_spreads(
        self,
        spot_history: pd.DataFrame,
    ) -> "ContractCascader":
        """Calibrate per-month peak-vs-base spreads in €/MWh (Phase 5 D-A4-1, NEG-04).

        For each month m in 1..12, computes:
            spread_m = mean_t(spot_history[t] | t in peak hours of month m)
                     - mean_t(spot_history[t] | t in base hours of month m)
            base_price_m = mean_t(spot_history[t] | t in base hours of month m)  # cached for codex action #2 derivation

        Stores `self.peak_base_spreads_: dict[int, float]` AND
        `self._base_price_per_month_: dict[int, float]` (the latter is a
        package-private cache used by the `fit_peak_ratios` deprecation shim
        per codex action #2 to derive `peak_base_ratios_` during the
        deprecation window — see fit_peak_ratios docstring).

        Sign-invariant: when consumed by `synthesize_peak_prices` under
        `allow_negative_peak=True`, the formula `peak = base + spread` produces
        the correct peak even when `base < 0` (negative monthly forward),
        because the spread itself is computed in €/MWh, not as a ratio.

        Parameters
        ----------
        spot_history : pd.DataFrame
            Historical EPEX-style spot prices (15-min or hourly). Must have a
            DatetimeIndex and a column named `price_eur_mwh` or similar
            (the exact column resolution mirrors `fit_peak_ratios`).

        Returns
        -------
        ContractCascader
            self, for chaining (matches `fit_peak_ratios` API).
        """
    ```

    Body: reuse the existing peak-mask logic from `fit_peak_ratios` (EEX peak hours 08:00-20:00 weekday excluding country holidays — `_is_peak_timestamp` from assembler.py is the canonical mask; if `cascading.py` already has an equivalent helper, reuse that). Group by month, compute `(peak_avg - base_avg)` per month AND `base_avg` per month (the latter cached on `self._base_price_per_month_` for the deprecation shim per codex action #2). Persist `self.peak_base_spreads_: dict[int, float] = {1: ..., 2: ..., ..., 12: ...}` AND `self._base_price_per_month_: dict[int, float] = {1: ..., ..., 12: ...}`. Months with no historical data fall back to `default_spread = 5.0` (per RESEARCH Pitfall 4) AND `default_base = 50.0` (so the deprecation-shim derivation does not divide by zero or produce wild ratios from very-low base prices).

    **Codex action #7 fallback behavior:** when `spot_history` is empty (`.empty == True`) OR has fewer than 100 rows (insufficient data), the method emits a `warnings.warn("fit_peak_spreads: spot_history is empty/insufficient — using default 5.0 €/MWh for all months", UserWarning, stacklevel=2)` AND populates `self.peak_base_spreads_ = {m: 5.0 for m in range(1, 13)}` and `self._base_price_per_month_ = {m: 50.0 for m in range(1, 13)}`. This is what `test_fit_peak_spreads_empty_spot_history` (Task 6 Edit F) asserts. Document the threshold (`< 100 rows = insufficient`) in the docstring rationale.

    Verify with `python -c "from pfc_shaping.calibration.cascading import ContractCascader; assert hasattr(ContractCascader, 'fit_peak_spreads'); print('OK')"`.

    **Edit C — DEPRECATE `fit_peak_ratios` with UNIFIED SHIM contract (codex action #2 — supersedes RESEARCH §Pattern 6 transparent-shim-only proposal):**

    Replace the existing `fit_peak_ratios` body (lines 279-…) with the unified shim that emits DeprecationWarning, delegates to `fit_peak_spreads`, AND populates `peak_base_ratios_` derived from `peak_base_spreads_` per codex action #2 suggestion. The derivation formula uses the cached `_base_price_per_month_` from `fit_peak_spreads`:
    ```
    def fit_peak_ratios(
        self,
        spot_history: pd.DataFrame,
    ) -> "ContractCascader":
        """DEPRECATED — use ``fit_peak_spreads`` (Phase 5 D-A4-2, NEG-04).

        UNIFIED SHIM CONTRACT per codex review action #2 (2026-05-19,
        05-REVIEWS.md lines 96-97, 113-116): this method ALWAYS does TWO things:

          1. Emit DeprecationWarning pointing migration target.
          2. Delegate to ``fit_peak_spreads(spot_history)`` so
             ``peak_base_spreads_`` is populated.
          3. Derive ``peak_base_ratios_`` from ``peak_base_spreads_`` and
             ``self._base_price_per_month_`` (cached by ``fit_peak_spreads``)
             via ``{m: 1.0 + spread / max(base_price_m, 1.0)}``. This ensures
             legacy callers that read ``self.peak_base_ratios_`` directly
             (e.g. tests, dashboards) continue to work during the deprecation
             window — they do NOT see ``AttributeError``.

        Codex action #2 motivation: the original CONTEXT.md D-A4-2 proposed
        ``NotImplementedError`` if ``spot_history`` was absent; RESEARCH §Pattern 6
        revised to a transparent shim WITHOUT the ratios derivation. The
        codex review noted that the two earlier proposals were inconsistent
        and that LEGACY ATTRIBUTE READERS would silently break without the
        ratios derivation. This UNIFIED SHIM is the authoritative
        deprecation-window behavior.

        Migration: replace ``cascader.fit_peak_ratios(spot)`` with
        ``cascader.fit_peak_spreads(spot)`` and rely on
        ``peak_base_spreads_`` (€/MWh) instead of ``peak_base_ratios_``
        (dimensionless multiplier).

        RESEARCH §Dry-Run §callsite audit identified two callsites in
        ``pfc_shaping/pipeline/production_phases.py`` (lines 344, 644). Both
        are migrated in Plan 05-03 Task 4 (this plan). This shim is the
        safety net for any callsite that has not yet been migrated.
        """
        import warnings
        warnings.warn(
            "ContractCascader.fit_peak_ratios() is deprecated (Phase 5 D-A4-2). "
            "Use fit_peak_spreads(spot_history) which calibrates spreads in "
            "€/MWh (sign-invariant for negative forwards). Migration: replace "
            "fit_peak_ratios(spot) with fit_peak_spreads(spot). The legacy "
            "peak_base_ratios_ attribute is still populated during the "
            "deprecation window (codex action #2 unified shim contract).",
            DeprecationWarning,
            stacklevel=2,
        )
        # Step 1+2: delegate transparently (populates peak_base_spreads_ AND _base_price_per_month_)
        self.fit_peak_spreads(spot_history)
        # Step 3 (codex action #2): derive peak_base_ratios_ during deprecation window
        # so legacy attribute readers do not see AttributeError.
        # Formula: ratio_m = 1.0 + spread_m / max(base_price_m, 1.0). The max(., 1.0)
        # avoids division by very small or zero base prices (which would produce
        # unbounded ratios). For typical EEX scales (10-100 €/MWh base), this is a
        # near-no-op safety floor.
        self.peak_base_ratios_: dict[int, float] = {
            m: 1.0 + spread / max(self._base_price_per_month_.get(m, 50.0), 1.0)
            for m, spread in self.peak_base_spreads_.items()
        }
        return self
    ```

    **Edit D — Refactor `synthesize_peak_prices` to branch on `allow_negative_peak` (around line 342):**

    The current body computes `result[peak_key] = base_price * ratio` (or equivalent). Replace with a branch:
    ```
    def synthesize_peak_prices(
        self,
        base_prices: dict[str, float],
        ...
    ) -> dict[str, float]:
        ...
        if self.allow_negative_peak:
            # Phase 5 D-A4-1 spread-additive path (default, negative-ready).
            spreads = getattr(self, "peak_base_spreads_", None)
            if not spreads:
                # RESEARCH Pitfall 4 — fall back to default 5.0 €/MWh per month.
                logger.warning(
                    "ContractCascader.synthesize_peak_prices: no fitted "
                    "peak_base_spreads_ — using default 5.0 €/MWh for all months. "
                    "Call fit_peak_spreads(spot_history) before synthesize to "
                    "calibrate per-month spreads."
                )
                spreads = {m: 5.0 for m in range(1, 13)}
            for source_key, base_price in base_prices.items():
                # Determine month from source_key (existing logic — preserve)
                month = ...   # extract from source_key (M-07, Q3, etc.)
                spread = spreads.get(month, 5.0)
                result[f"{source_key}-Peak"] = base_price + spread
        else:
            # Legacy multiplicative path (operator rollback per D-A4-3).
            # Current code path preserved verbatim — `result[peak_key] = base_price * ratio`
            # with implicit ratio >= 1 (Peak >= Base).
            ...   # existing logic
    ```

    The exact source_key → month extraction (`M-07` → 7, `Q3` → mean of [7,8,9] or first month, etc.) MUST mirror the existing logic in `fit_peak_ratios`. If the existing logic uses a helper like `_resolve_month(source_key)`, reuse it. If not, extract the existing inline parsing into a local helper at the top of this method to avoid duplication between the two branches.

    Add a docstring update for `synthesize_peak_prices` naming D-A4-1, D-A4-3, NEG-04, RESEARCH Pattern 4, codex action #2 unified shim contract.
  </action>
  <verify>
    <automated>python -c "
from pfc_shaping.calibration.cascading import ContractCascader
import inspect, warnings, pandas as pd

# Ctor signature
sig = inspect.signature(ContractCascader.__init__)
assert 'allow_negative_peak' in sig.parameters, list(sig.parameters)
assert sig.parameters['allow_negative_peak'].default is True

# Instances
c_default = ContractCascader()
assert c_default.allow_negative_peak is True

c_legacy = ContractCascader(allow_negative_peak=False)
assert c_legacy.allow_negative_peak is False

# fit_peak_spreads exists
assert hasattr(ContractCascader, 'fit_peak_spreads')

# fit_peak_ratios emits DeprecationWarning AND delegates AND populates peak_base_ratios_ (codex action #2)
c_test = ContractCascader()
# Build minimal spot_history for the delegate (peak / base mask need timestamps)
idx = pd.date_range('2025-01-01', '2025-12-31 23:00', freq='h', tz='UTC')
spot = pd.DataFrame({'price_eur_mwh': 50.0 + 10.0 * (idx.hour >= 8) * (idx.hour < 20)}, index=idx)
with warnings.catch_warnings(record=True) as w:
    warnings.simplefilter('always')
    c_test.fit_peak_ratios(spot)
    dep_warnings = [x for x in w if issubclass(x.category, DeprecationWarning)]
    assert len(dep_warnings) >= 1, f'expected DeprecationWarning, got: {[str(x.message) for x in w]}'
    assert any('fit_peak_spreads' in str(x.message) for x in dep_warnings), 'migration message missing'
assert hasattr(c_test, 'peak_base_spreads_'), 'shim must populate peak_base_spreads_'
# Codex action #2: peak_base_ratios_ MUST also be populated during deprecation window
assert hasattr(c_test, 'peak_base_ratios_'), 'codex action #2: peak_base_ratios_ MUST be populated by the unified shim'
assert isinstance(c_test.peak_base_ratios_, dict), type(c_test.peak_base_ratios_)
assert set(c_test.peak_base_ratios_.keys()) == set(range(1, 13)), c_test.peak_base_ratios_.keys()
assert all(isinstance(v, float) for v in c_test.peak_base_ratios_.values())
# All ratios > 1.0 (since spread > 0 / base > 0 in the synthetic example)
assert all(v >= 1.0 for v in c_test.peak_base_ratios_.values()), c_test.peak_base_ratios_
print(f'OK cascading shim + spreads + ctor + codex action #2 unified ratios derivation')
print(f'   peak_base_spreads_[1]={c_test.peak_base_spreads_[1]:.2f}, peak_base_ratios_[1]={c_test.peak_base_ratios_[1]:.4f}')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "allow_negative_peak: bool = True" pfc_shaping/calibration/cascading.py` returns 1 match (ctor signature).
    - `grep -n "self.allow_negative_peak" pfc_shaping/calibration/cascading.py` returns ≥ 2 matches (init store + synthesize branch).
    - `grep -n "def fit_peak_spreads" pfc_shaping/calibration/cascading.py` returns 1 match (new method).
    - `grep -n "peak_base_spreads_" pfc_shaping/calibration/cascading.py` returns ≥ 3 matches (fit, init, synthesize).
    - `grep -n "DeprecationWarning" pfc_shaping/calibration/cascading.py` returns ≥ 1 match (fit_peak_ratios shim).
    - **Codex action #2 — UNIFIED shim contract (NEW):** `grep -n "self.peak_base_ratios_" pfc_shaping/calibration/cascading.py` returns ≥ 1 match in the `fit_peak_ratios` body (NOT just legacy code — the derivation assignment must be inside the shim function). Verify the derivation formula is present: `grep -n "1.0 + spread / max" pfc_shaping/calibration/cascading.py` returns 1 match (the codex-action-#2 formula).
    - **Codex action #2 — cached base_price_per_month (NEW):** `grep -n "self._base_price_per_month_" pfc_shaping/calibration/cascading.py` returns ≥ 2 matches (cache populated by `fit_peak_spreads`, consumed by the `fit_peak_ratios` shim).
    - `grep -q "D-A4-1\|D-A4-2\|D-A4-3\|NEG-04" pfc_shaping/calibration/cascading.py` exits 0 (decision-ID / requirement-ID traceability).
    - **Codex action #2 traceability (NEW):** `grep -q "codex action #2\|codex review action #2\|UNIFIED SHIM" pfc_shaping/calibration/cascading.py` exits 0 (review provenance inline).
    - `grep -q "RESEARCH Pitfall 4" pfc_shaping/calibration/cascading.py` exits 0 (pitfall surfaced inline).
    - **Codex action #7 — fallback warning (NEW):** `grep -nE "warnings.warn\(.*empty/insufficient" pfc_shaping/calibration/cascading.py` returns 1 match (the `fit_peak_spreads` empty-history fallback emits a UserWarning).
    - `grep -v '^[[:space:]]*#' pfc_shaping/calibration/cascading.py | grep -c "base_price \* ratio\|base_price\*ratio"` returns ≥ 1 (legacy multiplicative formula still present in the `else` branch of synthesize_peak_prices — NOT removed).
    - `grep -v '^[[:space:]]*#' pfc_shaping/calibration/cascading.py | grep -c "base_price + spread\|base_price+spread"` returns ≥ 1 (new spread-additive formula present in the `if self.allow_negative_peak:` branch).
    - The verify command above prints `OK cascading shim + spreads + ctor + codex action #2 unified ratios derivation`.
    - `pytest tests/ -q -k "cascading or cascader or peak" --tb=short` exits 0 — existing cascading tests still green under Phase 5 defaults.
    - `pytest tests/ -q --tb=short` exits 0 with `266 passed, 10 skipped` — full suite unchanged (the new ctor arg + new method + deprecated shim are additive surface; existing callers continue to work).
  </acceptance_criteria>
  <done>`ContractCascader` exposes `allow_negative_peak: bool = True` (default ON, negative-ready), new `fit_peak_spreads(spot_history)` public method with codex-action-#7 empty-history fallback, UNIFIED SHIM contract for `fit_peak_ratios` (codex action #2): DeprecationWarning + delegate to `fit_peak_spreads` + populate `peak_base_ratios_` derived during deprecation window, and a branched `synthesize_peak_prices` (spread-additive default, multiplicative legacy under allow_negative_peak=False).</done>
</task>

<task type="auto">
  <name>Task 2: Add `PFC_LT_ALLOW_NEGATIVE_PRICES` master flag audit-trail + four explicit floor kwargs on `PFCAssembler.__init__` (D-A2-2 + B1/B4 Approach B: 4 ctor kwargs forwarded to sub-components and to smooth_base_prices) — COMMIT 2 per codex action #5</name>
  <files>pfc_shaping/lt/model/assembler.py</files>
  <read_first>
    - pfc_shaping/lt/model/assembler.py:155-205 (PFCAssembler.__init__ — primary modification target)
    - pfc_shaping/lt/model/shape_hourly.py:50-119 (`_FLAG_ENV_VAR` + `_resolve_flag` — 5bis-A pattern to transpose)
    - tests/conftest.py (post-Plan-05-01 state — confirms PFC_LT_ALLOW_NEGATIVE_PRICES is hygiene-protected by prefix and documented inline)
    - .planning/phases/05-msfc-log-prix-retire-silent-floors/05-CONTEXT.md §decisions D-A2-2, D-A2-3, D-A2-4 (master flag audit-trail format + rollback path + sidecar decision)
    - .planning/phases/05-msfc-log-prix-retire-silent-floors/05-RESEARCH.md §Architecture Patterns §Pattern 5 §Master flag audit-trail (canonical `_resolve_allow_negative` helper + sidecar D-A2-4 RESOLVED: no new sidecar)
    - .planning/phases/05-msfc-log-prix-retire-silent-floors/05-VALIDATION.md §Per-Task Verification Map task 5-03-03 (test_master_flag_audit_log contract)
  </read_first>
  <action>
    Modify `pfc_shaping/lt/model/assembler.py` with three surgical edits.

    **Edit A — Add module-level constant and helper (top of file, near other module constants):**

    ```
    _ALLOW_NEG_ENV_VAR = "PFC_LT_ALLOW_NEGATIVE_PRICES"


    def _resolve_allow_negative(explicit: bool | None) -> bool:
        """Resolve PFC_LT_ALLOW_NEGATIVE_PRICES — explicit arg wins, else env-var, else False (Phase 5 D-A2-2).

        Mirrors the 5bis-A ``_resolve_flag`` helper in ``shape_hourly.py`` for
        consistency. The flag is freeze-at-init: ``PFCAssembler.__init__`` reads
        the env-var exactly once and stores the resolved value on
        ``self._allow_negative_prices``. Subsequent env-var changes do not
        affect the running assembler.

        Audit-trail-only: this flag does NOT override the four ctor args of
        the sub-components (`enforce_positivity`, `enforce_m_factor_floor`,
        `enforce_floor`, `allow_negative_peak`). It is logged INFO once at
        init for operator traceability (D-A2-2).
        """
        if explicit is not None:
            return bool(explicit)
        raw = os.getenv(_ALLOW_NEG_ENV_VAR, "0")
        if raw == "1":
            return True
        if raw == "0":
            return False
        logger.warning(
            "%s=%r invalide — traité comme False (Phase 5 D-A2-2)",
            _ALLOW_NEG_ENV_VAR, raw,
        )
        return False
    ```

    Add `_ALLOW_NEG_ENV_VAR` and `_resolve_allow_negative` to `__all__` if the module has one (RESEARCH §Pattern 5 — exported for tests so they can monkeypatch without hardcoding the env-var name).

    **Edit B — Extend `PFCAssembler.__init__` signature with the audit flag AND four explicit floor kwargs (B1/B4 RESOLUTION — Approach B per the checker feedback 2026-05-19; supersedes the original "introspect from sub-components" sketch):**

    Append FIVE new kwargs to `__init__` (after `confidence_thresholds`), in this exact order:

    1. `allow_negative_prices: bool | None = None`  ← master flag audit-trail (D-A2-2).
    2. `enforce_positivity: bool = False`  ← forwarded to `smooth_base_prices(...)` at the MSFC callsite inside `build()` (default OFF = Phase 5 negative-ready).
    3. `enforce_m_factor_floor: bool = False`  ← forwarded to `self.calibrator` (default OFF).
    4. `enforce_floor: bool = False`  ← forwarded to `self.wv` (default OFF).
    5. `allow_negative_peak: bool = True`  ← forwarded to `self.cascader` (default ON = negative-ready).

    All four floor kwargs match the per-component ctor arg pattern of D-A2-1 and map 1:1 to the audit log fields (D-A2-2). Defaults match D-A2-1 (Phase 5 negative-ready). Operator rollback per D-A2-3 = construct `PFCAssembler(..., enforce_positivity=True, enforce_m_factor_floor=True, enforce_floor=True, allow_negative_peak=False)`.

    In the body, AFTER the existing sub-component assignments (`self.sh`, `self.si`, `self.unc`, `self.wv`, `self.cascader`, `self.calibrator`) and AFTER the capability-check log line at line 197-201, store the resolved floor states and emit the audit log:

    ```python
    # Phase 5 — store the four explicit floor kwargs as attributes (B1/B4 Approach B).
    # These are the authoritative API surface for floor enforcement; sub-component
    # ctor args (e.g. wv.enforce_floor) are honored when explicitly set by the
    # caller, but PFCAssembler-level kwargs take precedence on its own forwarding
    # paths (the MSFC callsite at `build()` and the diagnostic audit log here).
    self.enforce_positivity: bool = bool(enforce_positivity)
    self.enforce_m_factor_floor: bool = bool(enforce_m_factor_floor)
    self.enforce_floor: bool = bool(enforce_floor)
    self.allow_negative_peak: bool = bool(allow_negative_peak)

    # Phase 5 D-A2-2 master flag — audit-trail INFO only, NOT an override.
    # The four ctor args above are the actual API surface — operator rollback
    # per D-A2-3 = pass enforce_*=True / allow_negative_peak=False explicitly.
    self._allow_negative_prices: bool = _resolve_allow_negative(allow_negative_prices)

    # The audit log resolves each floor state from the PFCAssembler-level
    # kwarg first (authoritative), falling back to the sub-component attribute
    # if explicit forwarding has not been wired for that floor yet. This
    # keeps the log accurate even if a caller bypasses PFCAssembler kwargs
    # and constructs `WaterValueCorrection(enforce_floor=True)` directly.
    msfc_floor_on = self.enforce_positivity
    af_floor_on = self.enforce_m_factor_floor or bool(getattr(self.calibrator, "enforce_m_factor_floor", False))
    wv_floor_on = self.enforce_floor or bool(getattr(self.wv, "enforce_floor", False))
    cascading_neg_peak_on = self.allow_negative_peak and bool(getattr(self.cascader, "allow_negative_peak", True))

    logger.info(
        "PFC_LT_ALLOW_NEGATIVE_PRICES=%s, floors_disabled={msfc:enforce_positivity=%s, "
        "af:m_factor_floor=%s, wv:floor=%s, cascading:allow_neg_peak=%s}",
        self._allow_negative_prices,
        not msfc_floor_on,  # "floors_disabled" semantic: True when floor is OFF
        not af_floor_on,
        not wv_floor_on,
        cascading_neg_peak_on,  # allow_negative_peak=True == floor OFF, no negation
    )
    ```

    **Forward the four kwargs from `__init__` to the active code paths inside `build()`:**

    1. **MSFC forwarding (assembler.py:397 `smooth_base_prices(idx, base_prices, B)` callsite):** Locate the call site inside `build()` (verified at assembler.py:397 during plan revision — the `try: ... from pfc_shaping.lt.model.msfc_spline import smooth_base_prices; B = smooth_base_prices(idx, base_prices, B)` block). Update the call to forward the kwarg:
       ```python
       B = smooth_base_prices(idx, base_prices, B, enforce_positivity=self.enforce_positivity)
       ```
       The kwarg propagates to `_enforce_mean_constraints` per Plan 05-01 Task 2 (RESEARCH Pitfall 1 — TWO floors gated by the same flag).

    2. **ArbitrageFreeCalibrator forwarding:** if `self.calibrator is not None and self.enforce_m_factor_floor`, the executor MUST ensure the calibrator instance has `enforce_m_factor_floor=True`. The cleanest implementation: re-set the attribute at `__init__` time when the kwarg is True and the sub-component was constructed with the default (False):
       ```python
       if self.calibrator is not None and self.enforce_m_factor_floor:
           self.calibrator.enforce_m_factor_floor = True  # override sub-component default per D-A2-3 rollback
       ```
       This is safe because `ArbitrageFreeCalibrator.calibrate()` reads `self.enforce_m_factor_floor` at call time (Plan 05-01 Task 3 contract). The reverse direction (caller constructs `ArbitrageFreeCalibrator(enforce_m_factor_floor=True)` and passes `PFCAssembler(enforce_m_factor_floor=False)`) is not supported — the audit log uses `or` semantics so it reports the maximal floor state.

    3. **WaterValueCorrection forwarding:** identical pattern to AF:
       ```python
       if self.wv is not None and self.enforce_floor:
           self.wv.enforce_floor = True  # override sub-component default per D-A2-3 rollback
       ```
       This is safe because `WaterValueCorrection.apply()` and `WaterValueCorrection.compute_delta_wv()` both read `self.enforce_floor` at call time (Plan 05-02 Task 1 contract).

    4. **ContractCascader forwarding:** identical pattern, but with reversed default polarity (`allow_negative_peak=True` is the negative-ready default; setting `allow_negative_peak=False` is the rollback):
       ```python
       if self.cascader is not None and not self.allow_negative_peak:
           self.cascader.allow_negative_peak = False  # override sub-component default per D-A2-3 rollback
       ```
       This is safe because `ContractCascader.synthesize_peak_prices()` reads `self.allow_negative_peak` at call time (Task 1 contract in this plan).

    The exact log line format MUST contain the substring `PFC_LT_ALLOW_NEGATIVE_PRICES=` AND `floors_disabled={` AND each of the four sub-component status substrings (`msfc:`, `af:`, `wv:`, `cascading:`). These substrings are what `test_master_flag_audit_log` asserts.

    **Edit C — Document the rollback path in the class docstring (updated for B1/B4 Approach B):**

    Extend the existing class docstring (around line 145-160 — before the `__init__`) with a new paragraph:

    ```
    Phase 5 D-A2-2 / D-A2-3 — master flag PFC_LT_ALLOW_NEGATIVE_PRICES is an
    audit-trail INFO log only, NOT an override. The four EXPLICIT FLOOR KWARGS
    on PFCAssembler.__init__ are the authoritative API surface:
      - enforce_positivity: bool = False
          (forwarded to smooth_base_prices(...) at the MSFC callsite in build())
      - enforce_m_factor_floor: bool = False
          (forwarded to self.calibrator.enforce_m_factor_floor)
      - enforce_floor: bool = False
          (forwarded to self.wv.enforce_floor)
      - allow_negative_peak: bool = True
          (forwarded to self.cascader.allow_negative_peak)

    Defaults are negative-ready (D-A2-1). To roll back to legacy positive-only
    behavior, construct:
        PFCAssembler(
            ...,
            enforce_positivity=True,
            enforce_m_factor_floor=True,
            enforce_floor=True,
            allow_negative_peak=False,
        )
    This is the canonical D-A2-3 operator rollback path — testable via
    `tests/test_phase05_negative_prices.py::test_phase05_baseline_5bisA_via_enforce_true`
    (Task 7), which uses these four explicit ctor args (no monkeypatching).
    ```

    No sidecar is created for `PFC_LT_ALLOW_NEGATIVE_PRICES` per D-A2-4 resolved in RESEARCH §Pattern 5 §Décision sidecar.
  </action>
  <verify>
    <automated>python -c "
import os, logging, inspect
from pfc_shaping.lt.model.assembler import PFCAssembler, _ALLOW_NEG_ENV_VAR, _resolve_allow_negative

# Module constant + helper exposed
assert _ALLOW_NEG_ENV_VAR == 'PFC_LT_ALLOW_NEGATIVE_PRICES'

# Resolution precedence
assert _resolve_allow_negative(None) is False  # default OFF (no env var)
assert _resolve_allow_negative(True) is True
assert _resolve_allow_negative(False) is False

os.environ['PFC_LT_ALLOW_NEGATIVE_PRICES'] = '1'
assert _resolve_allow_negative(None) is True
os.environ['PFC_LT_ALLOW_NEGATIVE_PRICES'] = '0'
assert _resolve_allow_negative(None) is False
del os.environ['PFC_LT_ALLOW_NEGATIVE_PRICES']

# PFCAssembler.__init__ signature: master flag + four explicit floor kwargs (B1/B4 Approach B)
sig = inspect.signature(PFCAssembler.__init__)
for name, expected_default in [
    ('allow_negative_prices', None),
    ('enforce_positivity', False),
    ('enforce_m_factor_floor', False),
    ('enforce_floor', False),
    ('allow_negative_peak', True),
]:
    assert name in sig.parameters, (name, list(sig.parameters))
    assert sig.parameters[name].default == expected_default, (name, sig.parameters[name].default, expected_default)
print('OK assembler master flag helper + 5 floor kwargs (Approach B)')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "_ALLOW_NEG_ENV_VAR = \"PFC_LT_ALLOW_NEGATIVE_PRICES\"" pfc_shaping/lt/model/assembler.py` returns 1 match.
    - `grep -n "def _resolve_allow_negative" pfc_shaping/lt/model/assembler.py` returns 1 match.
    - `grep -n "allow_negative_prices: bool | None = None" pfc_shaping/lt/model/assembler.py` returns 1 match (ctor signature — master flag).
    - **B1/B4 Approach B floor kwargs (5 acceptance lines):**
      - `grep -n "enforce_positivity: bool = False" pfc_shaping/lt/model/assembler.py` returns 1 match (PFCAssembler.__init__ signature — distinct from the msfc_spline.py occurrences from Plan 05-01).
      - `grep -n "enforce_m_factor_floor: bool = False" pfc_shaping/lt/model/assembler.py` returns 1 match.
      - `grep -n "enforce_floor: bool = False" pfc_shaping/lt/model/assembler.py` returns 1 match.
      - `grep -n "allow_negative_peak: bool = True" pfc_shaping/lt/model/assembler.py` returns 1 match (default ON = negative-ready).
      - `grep -n "smooth_base_prices(idx, base_prices, B, enforce_positivity=self.enforce_positivity)" pfc_shaping/lt/model/assembler.py` returns 1 match (MSFC kwarg forwarding wired at the callsite verified at assembler.py:397).
    - `grep -n "self._allow_negative_prices" pfc_shaping/lt/model/assembler.py` returns ≥ 2 matches (init store + log line).
    - `grep -n "PFC_LT_ALLOW_NEGATIVE_PRICES=" pfc_shaping/lt/model/assembler.py` returns ≥ 1 match (log format string).
    - `grep -n "floors_disabled={" pfc_shaping/lt/model/assembler.py` returns ≥ 1 match (log format string).
    - `grep -q "D-A2-2\|D-A2-3\|D-A2-4" pfc_shaping/lt/model/assembler.py` exits 0 (decision-ID traceability).
    - The verify command above prints `OK assembler master flag helper + 5 floor kwargs (Approach B)`.
    - `pytest tests/ -q -k "assembler or pfc" --tb=short` exits 0 — existing tests still green (all 5 new kwargs are optional; defaults preserve Phase 5 negative-ready behavior).
    - `pytest tests/ -q --tb=short` exits 0 with `266 passed, 10 skipped` — full suite unchanged at this point (test_master_flag_audit_log lands in Task 6).
  </acceptance_criteria>
  <done>`PFCAssembler.__init__` accepts `allow_negative_prices: bool | None = None` (master flag) AND four explicit floor kwargs `enforce_positivity`, `enforce_m_factor_floor`, `enforce_floor`, `allow_negative_peak` (B1/B4 Approach B). The master flag is resolved once via `_resolve_allow_negative` and emits one INFO audit log. The four floor kwargs are forwarded to (1) `smooth_base_prices()` at assembler.py:397 via explicit kwarg, (2) `self.calibrator.enforce_m_factor_floor` attribute override on rollback, (3) `self.wv.enforce_floor` attribute override on rollback, (4) `self.cascader.allow_negative_peak` attribute override on rollback. No new sidecar per D-A2-4 resolution.</done>
</task>

<task type="auto">
  <name>Task 3: Create `tests/fixtures/_generate_phase05_fixture.py` + commit `tests/fixtures/forwards_phase05_seed42.parquet`</name>
  <files>tests/fixtures/_generate_phase05_fixture.py, tests/fixtures/forwards_phase05_seed42.parquet</files>
  <read_first>
    - tests/fixtures/_generate_baseline.py (5bis-A pattern — `build_pfc()` + `main()` + `to_parquet(_OUT_PATH, index=True)` convention)
    - tests/fixtures/_generate_bowl_fixture.py (5bis-B pattern — `build_bowl_fixture()` returns tuple; deterministic seed=42)
    - .planning/phases/05-msfc-log-prix-retire-silent-floors/05-CONTEXT.md §domain §In scope (fixture contract: Cal'27=30, July M-07'27=20 dépressé, autres months positifs typiques EEX)
    - .planning/phases/05-msfc-log-prix-retire-silent-floors/05-RESEARCH.md §Architecture Patterns §Pattern 7 §`_generate_phase05_fixture.py` (synthetic contract list), §Common Pitfalls §Pitfall 5 (July=-2 vs July=20 — fixture is the acceptance variant)
  </read_first>
  <action>
    Create `tests/fixtures/_generate_phase05_fixture.py` modeled on the existing fixture generators.

    Required structure:
    1. Module docstring: "Deterministic Phase-5 forwards-fixture generator. Produces `tests/fixtures/forwards_phase05_seed42.parquet` (synthetic EEX-like forward quotes with Cal'27=30 €/MWh, July M-07'27=20 €/MWh dépressé, autres months positifs typiques EEX). Used by `tests/test_phase05_negative_prices.py::test_phase05_summer_bowl_negative_acceptance` (SC #2 ROADMAP) and `test_phase05_baseline_regression` (SC #5 ROADMAP). Phase 5 D-A4-5 / D-A4-6."
    2. Imports: from __future__, numpy, pandas, sys, Path bootstrap.
    3. Module constants: `_OUT_PATH = Path(__file__).parent / "forwards_phase05_seed42.parquet"`, `_REFERENCE_DATE = pd.Timestamp('2026-12-15', tz='UTC')` (year-end of N-1 = anchor for Cal'27 Y+1).
    4. Function `build_phase05_forwards(seed: int = 42) -> dict[str, float]` returning the forward-quotes dict. Structure (use the project's existing key convention — Y01/Q0N/M0N — which 5bis-A `_generate_baseline.py` builds for Cal'27; this fixture extends to span at least Cal'27 + Cal'26 + Cal'28 + 4 quarters of 2027 + 12 months of 2027):
       - Cal'26 (Y01_2026): 28.0
       - Cal'27 (Y01_2027): 30.0
       - Cal'28 (Y01_2028): 32.0
       - Q1'27 (Q01_2027): 35.0
       - Q2'27 (Q02_2027): 23.0
       - Q3'27 (Q03_2027): 22.0  (summer quarter, dépressé)
       - Q4'27 (Q04_2027): 33.0
       - M-01'27 (M01_2027): 33.0
       - M-02'27 (M02_2027): 31.0
       - M-03'27 (M03_2027): 28.0
       - M-04'27 (M04_2027): 25.0
       - M-05'27 (M05_2027): 24.0
       - M-06'27 (M06_2027): 22.0
       - M-07'27 (M07_2027): 20.0  ← THE July dépressé month (RESEARCH Pitfall 5 — fixture uses 20 €/MWh positif; the bowl deepening 5bis-B + Phase 5 floors-off pushes h13 Sunday < -20 €/MWh per SC #2)
       - M-08'27 (M08_2027): 23.0
       - M-09'27 (M09_2027): 25.0
       - M-10'27 (M10_2027): 28.0
       - M-11'27 (M11_2027): 32.0
       - M-12'27 (M12_2027): 36.0
       The Peak counterparts (Cal'27-Peak, Q3'27-Peak, M07_2027-Peak) are derived ALSO with EEX-typical spread (Peak ≈ Base + 5-8 €/MWh for normal months, smaller spread for solar-depressed months). Use the existing 5bis-A `_generate_baseline.py` peak-spread heuristic if it exists; otherwise hard-code Peak = Base + 6.0 with random N(0, 0.5) jitter under seed=42.

       Use `rng = np.random.default_rng(seed)` for any jitter so the fixture is byte-stable across re-runs.

    5. Function `main() -> None`:
       - Build the dict: `forwards = build_phase05_forwards(seed=42)`.
       - Convert to a DataFrame: long-format `pd.DataFrame({'contract_key': list(forwards), 'price_eur_mwh': list(forwards.values())})` (or wide-format if 5bis-A baseline uses wide — match the format used by `_generate_baseline.py` exactly so `read_parquet` consumers do not need format-detection logic).
       - Write to `_OUT_PATH` via `to_parquet(_OUT_PATH, index=True)`.
       - Print confirmation: `print(f"Wave 0 fixture: rows={len(df)} path={_OUT_PATH.relative_to(REPO_ROOT)}")`.

    6. `if __name__ == "__main__": main()` block.

    Run the generator once via `python tests/fixtures/_generate_phase05_fixture.py`. Verify the parquet is `≤ 50 KB` (small — only ~20 contract keys).

    DO NOT modify any other file in this task.
  </action>
  <verify>
    <automated>python tests/fixtures/_generate_phase05_fixture.py && python -c "
import pandas as pd, os, hashlib
p = 'tests/fixtures/forwards_phase05_seed42.parquet'
df = pd.read_parquet(p)
# Schema sanity
assert df.shape[0] >= 15, f'expected ≥15 contracts, got {df.shape}'
# Critical contract presence
keys_concat = ' '.join(df.values.flatten().astype(str))
assert any('2027' in s for s in df.values.flatten().astype(str)), 'Cal/Q/M_2027 contracts missing'
# Size sanity
size_kb = os.path.getsize(p) / 1024
assert size_kb < 100, f'fixture too large: {size_kb:.1f} KB'
# Determinism (sha256 stable across re-runs — recomputed in acceptance below)
sha = hashlib.sha256(open(p, 'rb').read()).hexdigest()
print(f'OK rows={df.shape[0]} size={size_kb:.1f}KB sha256={sha[:16]}...')
"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/fixtures/_generate_phase05_fixture.py` exits 0.
    - `test -f tests/fixtures/forwards_phase05_seed42.parquet` exits 0.
    - `grep -q "build_phase05_forwards" tests/fixtures/_generate_phase05_fixture.py` exits 0.
    - `grep -q "D-A4-5\|D-A4-6" tests/fixtures/_generate_phase05_fixture.py` exits 0 (decision-ID traceability).
    - `grep -q "seed=42\|seed: int = 42\|np.random.default_rng(42)" tests/fixtures/_generate_phase05_fixture.py` exits 0.
    - The verify command above prints `OK rows=... size=...KB sha256=...`.
    - Re-running `python tests/fixtures/_generate_phase05_fixture.py` produces a byte-identical parquet (sha256 stable): capture sha256 before re-run and compare via `python -c "import hashlib; print(hashlib.sha256(open('tests/fixtures/forwards_phase05_seed42.parquet', 'rb').read()).hexdigest())"`.
    - The two critical contracts Cal'27 (Y01_2027) = 30.0 and M-07'27 (M07_2027) = 20.0 are present in the parquet content (`python -c "import pandas as pd; df = pd.read_parquet('tests/fixtures/forwards_phase05_seed42.parquet'); print(df)"` shows these values).
    - `pytest tests/ -x -q` exits 0 reporting `266 passed, 10 skipped` (no new test consumer yet — Tasks 6/7 wire it in).
  </acceptance_criteria>
  <done>Phase-5 forwards fixture generator + parquet committed. Cal'27=30, July M-07'27=20 dépressé, autres months positifs typiques EEX. Byte-stable across re-runs (seed=42 deterministic).</done>
</task>

<task type="auto">
  <name>Task 4: Migrate two callsites in `production_phases.py` from `fit_peak_ratios` → `fit_peak_spreads` + add Phase 5 default doc comments in 4 production callsites — COMMIT 1 per codex action #5</name>
  <files>pfc_shaping/pipeline/autoresearch.py, pfc_shaping/pipeline/production_phases.py, pfc_shaping/pipeline/rolling_update.py</files>
  <read_first>
    - pfc_shaping/pipeline/autoresearch.py:225-245 (line 234 callsite — typically a `ShapeHourly(...)` or `WaterValueCorrection(...)` construction; verify)
    - pfc_shaping/pipeline/production_phases.py:340-355 (line 344 — `cascader_ch.fit_peak_ratios(inputs.epex_ch)` per RESEARCH §Dry-Run §callsite audit)
    - pfc_shaping/pipeline/production_phases.py:640-655 (line 644 — `cascader.fit_peak_ratios(spec.epex_df)` per RESEARCH §Dry-Run §callsite audit)
    - pfc_shaping/pipeline/rolling_update.py:355-380 (line 365 callsite — typically a ShapeHourly or related construction)
    - .planning/phases/05-msfc-log-prix-retire-silent-floors/05-CONTEXT.md §canonical_refs §Callsites legacy à préserver (4-callsite audit list)
    - .planning/phases/05-msfc-log-prix-retire-silent-floors/05-RESEARCH.md §Dry-Run Results §D-A4-2 callsite audit (the two migrations)
  </read_first>
  <action>
    **Edit A — Migrate `production_phases.py:344`:**

    Locate the line `cascader_ch.fit_peak_ratios(inputs.epex_ch)` (or equivalent — confirm via grep before editing). Replace with:
    ```
    # Phase 5 D-A4-2 migration (NEG-04): fit_peak_spreads calibrates spreads
    # in €/MWh (sign-invariant for negative forwards). The deprecation shim
    # still routes fit_peak_ratios() → fit_peak_spreads() but explicit migration
    # avoids the runtime DeprecationWarning.
    cascader_ch.fit_peak_spreads(inputs.epex_ch)
    ```

    **Edit B — Migrate `production_phases.py:644`:**

    Locate the line `cascader.fit_peak_ratios(spec.epex_df)` (or equivalent — confirm via grep). Replace with:
    ```
    # Phase 5 D-A4-2 migration (NEG-04) — see comment at the analogous callsite above.
    cascader.fit_peak_spreads(spec.epex_df)
    ```

    **Edit C — Phase 5 default doc comments in `autoresearch.py:234`, `production_phases.py` (both ContractCascader constructions if visible), `rolling_update.py:365`:**

    At each of the four production callsites where a `ShapeHourly`, `WaterValueCorrection`, `ContractCascader`, or `ArbitrageFreeCalibrator` is constructed (grep `ShapeHourly(\|WaterValueCorrection(\|ContractCascader(\|ArbitrageFreeCalibrator(` across `pfc_shaping/pipeline/*.py` and identify the 4 relevant lines per RESEARCH §canonical_refs §Callsites legacy à préserver), ADD a one-line inline comment ABOVE the construction:
    ```
    # Phase 5 D-A2-1 default (negative-ready): no explicit enforce_* kwargs.
    # Legacy rollback per D-A2-3: pass enforce_positivity=True (smooth_base_prices),
    # enforce_m_factor_floor=True (ArbitrageFreeCalibrator), enforce_floor=True
    # (WaterValueCorrection), allow_negative_peak=False (ContractCascader) explicitly.
    ```

    Variants per file (for readability, abbreviate the comment to the line that names the relevant ctor arg):
    - `autoresearch.py:234`: if it constructs e.g. `WaterValueCorrection()`, comment names `enforce_floor=True` as the legacy override.
    - `production_phases.py` ContractCascader construction (line TBD by grep): comment names `allow_negative_peak=False` as the legacy override.
    - `rolling_update.py:365`: confirm via read which class is constructed, name the corresponding kwarg in the comment.

    DO NOT change any code behavior. These edits are PURE DOCUMENTATION (comments only). The behavioral migration is only at the two `fit_peak_ratios → fit_peak_spreads` callsites in `production_phases.py`.

    **Edit D — Audit-trail SUMMARY entry:**

    In the SUMMARY of this plan, record the EXACT line numbers and file paths of all 4 inline-comment additions and the 2 `fit_peak_ratios → fit_peak_spreads` migrations. This is the cross-AI audit trail per RESEARCH §canonical_refs.
  </action>
  <verify>
    <automated>if grep -nrE 'fit_peak_ratios\(' pfc_shaping/ | grep -v 'fit_peak_ratios()' | grep -vE '^\s*#' > /tmp/active_callsites.txt && [ -s /tmp/active_callsites.txt ]; then echo "FAIL: active fit_peak_ratios callsite remains:"; cat /tmp/active_callsites.txt; exit 1; fi && grep -cE 'fit_peak_spreads\(' pfc_shaping/pipeline/production_phases.py | awk '{ if ($1 < 2) { print "FAIL: fewer than 2 fit_peak_spreads callsites in production_phases.py"; exit 1 } else print "OK fit_peak_spreads count=" $1 }' && grep -c 'Phase 5 D-A2-1 default' pfc_shaping/pipeline/autoresearch.py pfc_shaping/pipeline/production_phases.py pfc_shaping/pipeline/rolling_update.py 2>&1 && pytest tests/ -q --tb=short 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rE "fit_peak_ratios\\(" pfc_shaping/ | grep -v '^\\s*#'` returns 0 matches (scope extended in revision iteration 2 from `pfc_shaping/pipeline/` to the whole `pfc_shaping/` subtree per WARNING 3 — covers any callsite outside `pipeline/`). NOTE: `notebooks/` and `scripts/` outside the `pfc_shaping/` package are intentionally NOT audited here — they are tolerated via the DeprecationWarning shim safety net (the codex-action-#2 unified shim contract from Task 1 Edit C transparently delegates to `fit_peak_spreads` and populates both `peak_base_spreads_` and `peak_base_ratios_`, so external callers continue to work for the deprecation window).
    - `grep -E "fit_peak_spreads\\(" pfc_shaping/pipeline/production_phases.py` returns ≥ 2 matches (the two new callsites).
    - `grep -c "Phase 5 D-A2-1" pfc_shaping/pipeline/autoresearch.py pfc_shaping/pipeline/production_phases.py pfc_shaping/pipeline/rolling_update.py` returns ≥ 3 (one inline comment per file minimum).
    - `grep -q "D-A4-2" pfc_shaping/pipeline/production_phases.py` exits 0 (migration traceability).
    - `pytest tests/ -q --tb=short` exits 0 with `266 passed, 10 skipped` — production pipeline runs the new migrated callsites; no test failure.
    - The SUMMARY records the exact line numbers of all 4 doc-comment additions + the 2 migration edits + the result of the `grep -rE "fit_peak_ratios\\("` audit (should be 0 active callsites after this task).
    - **Codex action #5 — Commit 1 finalization:** AFTER Task 4 completes, emit the FIRST commit per the boundary mapping in the Revision Log: `git commit -m "refactor(05): cascading spread-additive + production callsites migration"` covering files modified in Tasks 1 + 4 (cascading.py + autoresearch.py + production_phases.py + rolling_update.py). Tasks 2, 3, 5, 6, 7, 8 land in the SECOND commit at the END of the plan.
  </acceptance_criteria>
  <done>Two `fit_peak_ratios` callsites in `production_phases.py` migrated to `fit_peak_spreads`; Phase 5 default doc comments added at the 4 production callsites identified in RESEARCH §canonical_refs. **COMMIT 1 of codex action #5 emitted at end of this task** with message `refactor(05): cascading spread-additive + production callsites migration` covering Tasks 1 + 4 outputs.</done>
</task>

<task type="auto">
  <name>Task 5: Generate the canonical Phase-5 baseline `tests/fixtures/baseline_pfc_seed42_phase05.parquet`</name>
  <files>tests/fixtures/baseline_pfc_seed42_phase05.parquet</files>
  <read_first>
    - tests/fixtures/_generate_baseline.py (5bis-A `build_pfc(seed=42)` pattern — the wrapper that constructs a fitted PFCAssembler and calls build())
    - tests/fixtures/_generate_phase05_fixture.py (post-Task-3 state — provides the forwards dict via `build_phase05_forwards()`)
    - tests/fixtures/forwards_phase05_seed42.parquet (post-Task-3 — committed binary)
    - pfc_shaping/lt/model/assembler.py (post-Task-2 state — confirms PFC_LT_ALLOW_NEGATIVE_PRICES env-read at init)
    - .planning/phases/05-msfc-log-prix-retire-silent-floors/05-CONTEXT.md §decisions D-A4-6 (canonical baseline contract)
    - .planning/phases/05-msfc-log-prix-retire-silent-floors/05-RESEARCH.md §Dry-Run Results §D-A3-3 (multiplicatif vs additif diff 0.2-1.4 €/MWh — confirms new baseline obligatory)
  </read_first>
  <action>
    Generate the canonical Phase-5 baseline parquet by running `PFCAssembler.build()` with:
    - Inputs: `forwards_phase05_seed42` loaded from the committed parquet (Task 3).
    - Env: `PFC_LT_USE_SEASONAL_HOURLY_SHAPE=1` (5bis-B bowl gated by this flag — needed for SC #2 acceptance test to see deep bowl), `PFC_LT_ALLOW_NEGATIVE_PRICES=1` (Phase 5 audit-trail INFO log only).
    - Sub-component ctor args: ALL DEFAULTS (negative-ready) — `enforce_positivity=False`, `enforce_m_factor_floor=False`, `enforce_floor=False`, `allow_negative_peak=True`.
    - Seed: 42 (deterministic).

    Implementation:
    1. Create a small one-off generation snippet at the top of `tests/fixtures/_generate_phase05_fixture.py` OR add a `--generate-baseline` CLI flag to `_generate_phase05_fixture.py` — both are acceptable. The recommended approach: add a separate function `build_phase05_baseline_pfc(seed: int = 42) -> pd.DataFrame` inside `_generate_phase05_fixture.py` that returns the assembled PFC DataFrame.
    2. The function loads `tests/fixtures/forwards_phase05_seed42.parquet`, builds calendar via `enrich_15min_index(...)` (5bis-A pattern), fits the sub-components on a synthetic EPEX history (seed=42) using the same helper that 5bis-A `_generate_baseline.py` uses, sets `os.environ['PFC_LT_USE_SEASONAL_HOURLY_SHAPE'] = '1'` AND `os.environ['PFC_LT_ALLOW_NEGATIVE_PRICES'] = '1'` BEFORE constructing PFCAssembler (so the flags are picked up at init).
    3. Calls `PFCAssembler.build(base_prices=forwards_dict, seed=42)` and returns the resulting DataFrame.
    4. Extend `main()` (or add a new `generate_baseline()` sub-function) to write the output to `_BASELINE_OUT_PATH = Path(__file__).parent / "baseline_pfc_seed42_phase05.parquet"` via `df.to_parquet(_BASELINE_OUT_PATH, index=True)`.
    5. Run the script: `python tests/fixtures/_generate_phase05_fixture.py --generate-baseline` (or invoke `build_phase05_baseline_pfc()` directly if the CLI flag is not implemented).

    Verify the baseline is non-trivial: `python -c "import pandas as pd; df = pd.read_parquet('tests/fixtures/baseline_pfc_seed42_phase05.parquet'); print(df.shape, df.dtypes, df.columns.tolist()); print('min=%.2f max=%.2f mean=%.2f' % (df['price_shape'].min(), df['price_shape'].max(), df['price_shape'].mean()))"`.

    Sanity bounds: the baseline must contain at least some negative prices on the dépressé summer Sunday h13 region (otherwise the SC #2 acceptance test in Task 6 will fail). If the min is positive, investigate: either the bowl was not activated (env var not picked up at init), the floors were not bypassed (default ctor args not honored at the construction site), or the dépressé July=20 forward is not deep enough alone (without 5bis-B bowl) to push prices below zero. The RESEARCH expectation per SC #2 is `pfc[Sunday, h13, July 2027].mean() < -20.0 €/MWh`.

    Commit the baseline parquet to git.

    Restore the env-var state at end of the script (delete `PFC_LT_*` keys that the script set) so re-runs are idempotent.
  </action>
  <verify>
    <automated>python tests/fixtures/_generate_phase05_fixture.py --generate-baseline 2>&1 | tail -3 || python -c "
import os
os.environ['PFC_LT_USE_SEASONAL_HOURLY_SHAPE'] = '1'
os.environ['PFC_LT_ALLOW_NEGATIVE_PRICES'] = '1'
from tests.fixtures._generate_phase05_fixture import build_phase05_baseline_pfc
df = build_phase05_baseline_pfc(seed=42)
out = 'tests/fixtures/baseline_pfc_seed42_phase05.parquet'
df.to_parquet(out, index=True)
" && python -c "
import pandas as pd, os, hashlib
p = 'tests/fixtures/baseline_pfc_seed42_phase05.parquet'
df = pd.read_parquet(p)
assert df.shape[0] > 100, f'baseline too small: {df.shape}'
assert 'price_shape' in df.columns or any('price' in c for c in df.columns), f'no price column: {df.columns.tolist()}'
size_kb = os.path.getsize(p) / 1024
sha = hashlib.sha256(open(p, 'rb').read()).hexdigest()
price_col = 'price_shape' if 'price_shape' in df.columns else [c for c in df.columns if 'price' in c][0]
print(f'OK shape={df.shape} size={size_kb:.1f}KB min={df[price_col].min():.2f} max={df[price_col].max():.2f} mean={df[price_col].mean():.2f} sha256={sha[:16]}...')
"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/fixtures/baseline_pfc_seed42_phase05.parquet` exits 0.
    - The verify command above prints `OK shape=... size=... min=... max=... mean=...`.
    - `python -c "import pandas as pd; df = pd.read_parquet('tests/fixtures/baseline_pfc_seed42_phase05.parquet'); print(df.shape)"` returns a shape with rows > 100 (real PFC over a 1+ year horizon).
    - The baseline parquet contains at least some negative prices (verifies that the floors are bypassed and the bowl is active): `python -c "import pandas as pd; df = pd.read_parquet('tests/fixtures/baseline_pfc_seed42_phase05.parquet'); price_col = 'price_shape' if 'price_shape' in df.columns else [c for c in df.columns if 'price' in c][0]; print(f'min={df[price_col].min():.2f}'); assert df[price_col].min() < 0.0, f'baseline should contain negative prices, min={df[price_col].min()}'"`.
    - Re-running the generation produces a byte-identical parquet (sha256 stable across re-runs). Compare sha256 before and after.
    - `pytest tests/ -x -q` exits 0 with `266 passed, 10 skipped` — no new test consumes the baseline yet (Task 6 wires it in).
  </acceptance_criteria>
  <done>Canonical Phase-5 baseline `tests/fixtures/baseline_pfc_seed42_phase05.parquet` generated, contains negative prices (proves floors-off + bowl-on), byte-stable across re-runs.</done>
</task>

<task type="auto">
  <name>Task 6: Promote 6 of the 7 remaining stubs to populated (NEG-04 unit ×2 with codex action #2 unified shim assertion + master flag + SC #2 acceptance with codex action #4 explicit UTC→Europe/Zurich + baseline regression + codex action #7 fallback-spread) in `tests/test_phase05_negative_prices.py`</name>
  <files>tests/test_phase05_negative_prices.py</files>
  <read_first>
    - tests/test_phase05_negative_prices.py (post-Plan-05-02 state — 15 tests, 8 populated, 7 skip-stubs)
    - pfc_shaping/calibration/cascading.py (post-Task-1 state — fit_peak_spreads, UNIFIED SHIM contract per codex action #2, ctor kwarg, codex action #7 empty-history fallback)
    - pfc_shaping/lt/model/assembler.py (post-Task-2 state — _resolve_allow_negative, INFO audit log format)
    - tests/fixtures/forwards_phase05_seed42.parquet (post-Task-3 — committed)
    - tests/fixtures/baseline_pfc_seed42_phase05.parquet (post-Task-5 — committed)
    - tests/fixtures/baseline_pfc_seed42_bowl.parquet (5bis-B marker — presence/absence GATES the SC #2 acceptance test per D-A4-5)
    - .planning/phases/05-msfc-log-prix-retire-silent-floors/05-CONTEXT.md §decisions D-A4-4, D-A4-5, D-A4-6 (canonical contracts for the five tests promoted here)
    - .planning/phases/05-msfc-log-prix-retire-silent-floors/05-VALIDATION.md §Per-Task Verification Map (test contracts 5-03-01 through 5-03-05)
    - .planning/phases/05B-shape-hourly-infrastructure-flag-no-op-refactor/05B-REVIEWS.md (tolerance contract `atol=1e-12 rtol=0` — applied to test_phase05_baseline_regression)
    - .planning/phases/05-msfc-log-prix-retire-silent-floors/05-REVIEWS.md lines 96-104 + 113-122 (codex actions #2, #4, #7)
  </read_first>
  <action>
    Promote SIX of the seven remaining stubs to populated. The seventh (`test_phase05_baseline_5bisA_via_enforce_true`) is owned by Task 7 separately for clarity.

    **Edit A — `test_cascading_spread_signed_base` (NEG-04 unit, 5-03-01):**

    Replace the skip body with the canonical D-A4-4 contract:
    1. Instantiate `cascader = ContractCascader(allow_negative_peak=True)` (default).
    2. Manually populate `cascader.peak_base_spreads_ = {m: 5.0 for m in range(1, 13)}` (avoid the fit step to keep the test focused on the spread-additive formula).
    3. Build `base_prices = {'2027': -10.0, '2027-07': -15.0}` (mix of Cal and monthly negatives).
    4. Call `result = cascader.synthesize_peak_prices(base_prices)`.
    5. Assert `result['2027-Peak'] == pytest.approx(-10.0 + 5.0)` (= -5.0) AND `result['2027-07-Peak'] == pytest.approx(-15.0 + 5.0)` (= -10.0).
    6. Negative control (legacy path): `cascader_legacy = ContractCascader(allow_negative_peak=False)`. Populate `peak_base_ratios_ = {m: 1.05 for m in range(1, 13)}` if the legacy path requires it. Call `synthesize_peak_prices({'2027': -10.0})` and assert the result differs from the spread-additive value (legacy multiplicative `1.05 × -10 = -10.5`).

    Docstring: test ID `5-03-01`, requirement `NEG-04`, reference 05-VALIDATION.md.

    **Edit B — `test_fit_peak_ratios_deprecated` (NEG-04 unit, 5-03-02) — codex action #2 UNIFIED SHIM contract assertion:**

    Replace the skip body with:
    1. Build a minimal `spot_history = pd.DataFrame({'price_eur_mwh': [50.0] * (24*365)}, index=pd.date_range('2025-01-01', periods=24*365, freq='h', tz='UTC'))`.
    2. Instantiate `cascader = ContractCascader()`.
    3. Use `pytest.warns(DeprecationWarning, match='fit_peak_spreads')` context to call `cascader.fit_peak_ratios(spot_history)`.
    4. Assert that the deprecation actually fired (the `pytest.warns` context handles the assertion).
    5. **Codex action #2 — UNIFIED SHIM contract assertions (NEW):** Assert post-call BOTH of these attributes are populated:
       - `cascader.peak_base_spreads_` is a dict with keys 1..12 and float values (the shim delegated transparently per RESEARCH §Pattern 6).
       - `cascader.peak_base_ratios_` is ALSO populated (a dict with keys 1..12 and float values ≥ 1.0) — this is the codex action #2 derivation during the deprecation window so legacy attribute readers do not see AttributeError.
       - For each month m, assert `cascader.peak_base_ratios_[m] == pytest.approx(1.0 + cascader.peak_base_spreads_[m] / max(cascader._base_price_per_month_[m], 1.0))` (the codex action #2 derivation formula).
    6. Negative control: instantiate a SEPARATE `cascader_direct = ContractCascader()`, call `fit_peak_spreads(spot_history)` directly (no deprecation warning). Assert `cascader_direct.peak_base_spreads_` is populated AND `cascader_direct._base_price_per_month_` is populated BUT `cascader_direct.peak_base_ratios_` is NOT auto-populated (only the deprecation shim path populates ratios — this proves the derivation is shim-bound, not silently always-derived).

    Note: CONTEXT.md D-A4-2 originally proposed `raise NotImplementedError`. RESEARCH §Pattern 6 RESOLVED in favor of the transparent shim approach. Codex review action #2 (05-REVIEWS.md, 2026-05-19) UNIFIED the contract by also requiring the derived `peak_base_ratios_` to be populated during the deprecation window. This test asserts the unified-shim resolved behavior, NOT the original CONTEXT proposal. Document this divergence inline in the test docstring.

    Docstring: test ID `5-03-02`, requirement `NEG-04`, reference 05-VALIDATION.md + RESEARCH §Pattern 6 + codex action #2 unified shim contract.

    **Edit C — `test_master_flag_audit_log` (5-03-03):**

    Replace the skip body with:
    1. Build minimal sub-components: `sh = ShapeHourly()` (5bis-A default), `si = ShapeIntraday()` (existing class — minimal fit on synthetic data per 5bis-A `_generate_baseline.py` workaround), `wv = WaterValueCorrection()` (Plan 05-02 default = enforce_floor=False), `cas = ContractCascader()` (Task 1 default = allow_negative_peak=True), `calb = ArbitrageFreeCalibrator()` (Plan 05-01 default = enforce_m_factor_floor=False).
    2. Use `caplog.at_level(logging.INFO, logger='pfc_shaping.lt.model.assembler')` (or the appropriate logger name) to capture init logs.
    3. Set `os.environ['PFC_LT_ALLOW_NEGATIVE_PRICES'] = '1'` BEFORE constructing the assembler.
    4. Construct: `asm = PFCAssembler(shape_hourly=sh, shape_intraday=si, water_value=wv, cascader=cas, calibrator=calb)`.
    5. Assert at least ONE captured record's message contains the substring `"PFC_LT_ALLOW_NEGATIVE_PRICES=True"`.
    6. Assert the message contains `"floors_disabled={"` AND `"msfc:"` AND `"af:"` AND `"wv:"` AND `"cascading:"`.
    7. Negative-control: separately, construct with `os.environ['PFC_LT_ALLOW_NEGATIVE_PRICES'] = '0'`. Assert the captured message contains `"PFC_LT_ALLOW_NEGATIVE_PRICES=False"`.

    The `os.environ` mutation is safe because conftest.py's autouse `_pfc_lt_env_hygiene` snapshots and restores PFC_LT_* keys (5bis-A D-12).

    Docstring: test ID `5-03-03`, references D-A2-2, 05-VALIDATION.md.

    **Edit D — `test_phase05_summer_bowl_negative_acceptance` (SC #2 ROADMAP, 5-03-04, GATED-SKIP per D-A4-5 + codex action #4 explicit UTC→Europe/Zurich conversion):**

    Replace the skip body with:
    1. AT THE TOP OF THE TEST: `if not _BOWL_MARKER_PATH.exists(): pytest.skip('Phase 5 SC #2 requires 5bis-B bowl deepening calibrated first')`. This implements the D-A4-5 gate.
    2. Build the same way Task 5 builds the canonical baseline: load `forwards_phase05_seed42.parquet`, set BOTH env-vars, construct PFCAssembler with defaults, call `assembler.build()`.
    3. **Codex action #4 (NEW, 05-REVIEWS.md lines 98-99) — EXPLICIT UTC→Europe/Zurich conversion BEFORE applying Sunday/h13/July masks:** The PFC index is UTC; 'h13 Sunday' is local-time semantics. Without the explicit conversion, the masks would shift the Sunday/h13 window by 1-2 hours (depending on DST) and either miss the bowl trough entirely or include adjacent non-bowl hours. Wire the conversion exactly as suggested by codex review:
       ```python
       # codex action #4 (2026-05-19, 05-REVIEWS.md lines 98-99):
       # PFC index is UTC, but SC #2 ROADMAP semantics are local-time
       # (h13 Sunday in Europe/Zurich). Convert BEFORE masking.
       idx_local = pfc.index.tz_convert('Europe/Zurich')
       sunday_mask = idx_local.day_name() == 'Sunday'
       h13_mask = idx_local.hour == 13
       july_mask = idx_local.month == 7
       year_mask = idx_local.year == 2027
       subset = pfc.loc[sunday_mask & h13_mask & july_mask & year_mask]
       ```
       Document inline in the test that the masks are derived from `idx_local` (Europe/Zurich), NOT the raw UTC index — operator readability matters because the magnitude of the assertion (`< -20 €/MWh`) is sensitive to which hours are included in the subset.
    4. If the column conventions differ (e.g., `type_jour` is `dow == 6`, `heure` is `index.hour`), adapt accordingly — match what's actually in the baseline parquet committed in Task 5. The codex-action-#4 conversion idiom is the SAME regardless of whether the test masks `type_jour` (string) or `dow` (int) — what matters is that the index is converted to Europe/Zurich BEFORE the comparison.
    5. Assert `subset['price_shape'].mean() < -20.0` (the literal SC #2 ROADMAP threshold).
    6. If the assertion fails, the diagnostic message must report the actual value: e.g. `f"SC #2 not met: pfc[Sunday, h13 Europe/Zurich, July 2027].mean() = {subset.mean():.2f} €/MWh, expected < -20.0 (5bis-B bowl + Phase 5 floors-off may not be deep enough — check 5bis-B sigma_on, hydro_weight_sigma_on calibration); the UTC→Europe/Zurich conversion per codex action #4 was applied"`.

    Docstring: test ID `5-03-04`, references SC #2 ROADMAP, D-A4-5 gating logic, codex action #4 timezone-conversion documentation.

    **Edit E — `test_phase05_baseline_regression` (SC #5 regression, 5-03-05):**

    Replace the skip body with:
    1. AT THE TOP: `if not _BASELINE_PATH.exists(): pytest.fail('Phase 5 canonical baseline missing — re-run Task 5 generator')` (HARD-FAIL: this baseline IS committed in Task 5, its absence is a bug).
    2. Load the baseline: `baseline = pd.read_parquet(_BASELINE_PATH)`.
    3. Set env-vars (PFC_LT_USE_SEASONAL_HOURLY_SHAPE=1, PFC_LT_ALLOW_NEGATIVE_PRICES=1).
    4. Rebuild via the SAME pipeline used to generate the baseline: load forwards_phase05_seed42, construct PFCAssembler with defaults, call build().
    5. Assert `pd.testing.assert_frame_equal(rebuild, baseline, check_exact=False, atol=1e-12, rtol=0, check_dtype=True, check_column_type=True, check_index_type=True, check_like=False)` — the 5bis-A REVIEWS tolerance contract.

    Docstring: test ID `5-03-05`, references D-A4-6, 5bis-A REVIEWS tolerance contract.

    **Edit F — `test_fit_peak_spreads_empty_spot_history` (codex action #7, 5-03-07) — NEW populated test (was stubbed by Plan 05-01 scaffold at position 14):**

    Replace the `pytest.skip(...)` body with the canonical codex-action-#7 contract:
    1. Instantiate `cascader = ContractCascader()`.
    2. CASE 1 — empty DataFrame: `empty_spot = pd.DataFrame(columns=['price_eur_mwh'])`. Use `pytest.warns(UserWarning, match='empty/insufficient')` to call `cascader.fit_peak_spreads(empty_spot)`. Post-call, assert `cascader.peak_base_spreads_ == {m: 5.0 for m in range(1, 13)}` (default fallback per Task 1 Edit B codex-action-#7 logic).
    3. CASE 2 — insufficient rows: build a 50-row DataFrame (< 100 row threshold). Use `pytest.warns(UserWarning)` to call `fit_peak_spreads`. Assert fallback dict is populated.
    4. CASE 3 — positive control: build a 200-row sufficient DataFrame with realistic prices. Call `fit_peak_spreads(spot)` WITHOUT a warnings context — assert no UserWarning fires (the sufficient-data path doesn't trigger the fallback warning) AND `peak_base_spreads_` contains computed (not default) values.
    5. Assert `cascader._base_price_per_month_` (the cache for codex action #2) is also populated in all three cases.

    Docstring: test ID `5-03-07`, codex action #7 (05-REVIEWS.md line 122), references 05-VALIDATION.md task 5-03-07 (NEW, this revision adds the entry).

    **The remaining stub `test_phase05_baseline_5bisA_via_enforce_true` stays SKIPPED in this task — Task 7 owns it.**
  </action>
  <verify>
    <automated>pytest tests/test_phase05_negative_prices.py::test_cascading_spread_signed_base tests/test_phase05_negative_prices.py::test_fit_peak_ratios_deprecated tests/test_phase05_negative_prices.py::test_master_flag_audit_log tests/test_phase05_negative_prices.py::test_phase05_baseline_regression tests/test_phase05_negative_prices.py::test_fit_peak_spreads_empty_spot_history -x -v 2>&1 | tail -12 && pytest tests/test_phase05_negative_prices.py::test_phase05_summer_bowl_negative_acceptance -v 2>&1 | tail -5 && pytest tests/test_phase05_negative_prices.py -q --tb=short 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_phase05_negative_prices.py::test_cascading_spread_signed_base -x` exits 0 (NEG-04 spread-additive verified).
    - `pytest tests/test_phase05_negative_prices.py::test_fit_peak_ratios_deprecated -x` exits 0 (DeprecationWarning + shim delegation + codex action #2 unified shim derivation of `peak_base_ratios_` all verified).
    - `pytest tests/test_phase05_negative_prices.py::test_master_flag_audit_log -x` exits 0 (INFO log format verified).
    - `pytest tests/test_phase05_negative_prices.py::test_phase05_baseline_regression -x` exits 0 (baseline regression matches at atol=1e-12 rtol=0).
    - **Codex action #7 (NEW):** `pytest tests/test_phase05_negative_prices.py::test_fit_peak_spreads_empty_spot_history -x` exits 0 (fallback spread + UserWarning verified).
    - `pytest tests/test_phase05_negative_prices.py::test_phase05_summer_bowl_negative_acceptance -v` exits 0 (test ran without ERROR; outcome is either PASS or SKIP — both acceptable per D-A4-5 gating).
    - **Codex action #4 traceability in test file (NEW):** `grep -q "tz_convert('Europe/Zurich')\|tz_convert(\"Europe/Zurich\")" tests/test_phase05_negative_prices.py` exits 0 (the explicit UTC→Zurich conversion is wired in the SC #2 test).
    - **Codex action #2 traceability in test file (NEW):** `grep -q "codex action #2\|UNIFIED SHIM\|peak_base_ratios_" tests/test_phase05_negative_prices.py` exits 0 (the unified shim assertions are wired in test_fit_peak_ratios_deprecated).
    - `pytest tests/test_phase05_negative_prices.py -q --tb=short` exits 0 reporting one of:
      - `14 passed, 1 skipped` (SC #2 ran and passed — 5bis-B bowl marker present, full Phase 5 chain works end-to-end).
      - `13 passed, 2 skipped` (SC #2 gated-skip due to 5bis-B bowl marker absent — acceptable per D-A4-5).
      In both cases, `test_phase05_baseline_5bisA_via_enforce_true` remains skipped (owned by Task 7).
    - `grep -c "populated in Plan 05-03" tests/test_phase05_negative_prices.py` returns 1 (only `test_phase05_baseline_5bisA_via_enforce_true` retains the "populated in Plan 05-03" skip marker, awaiting Task 7).
    - `grep -q "5-03-01\|5-03-02\|5-03-03\|5-03-04\|5-03-05\|5-03-07" tests/test_phase05_negative_prices.py` exits 0 (6 test IDs traceable to 05-VALIDATION.md, including the NEW 5-03-07 from codex action #7).
    - `pytest tests/ -q --tb=short` exits 0 reporting `272 passed, 4 skipped` (or `271 passed, 5 skipped` if SC #2 gated-skipped).
  </acceptance_criteria>
  <done>Six of seven remaining stubs promoted to populated: NEG-04 spread-additive + DeprecationWarning shim with codex-action-#2 unified ratio derivation + master flag INFO log + SC #2 acceptance with codex-action-#4 explicit Europe/Zurich conversion (gated by 5bis-B bowl marker) + canonical baseline regression + codex-action-#7 fallback-spread. Test suite advances from `266 passed, 10 skipped` to `272 passed, 4 skipped` (best case `273 passed, 3 skipped` if SC #2 ran green).</done>
</task>

<task type="auto">
  <name>Task 7: Populate the last stub `test_phase05_baseline_5bisA_via_enforce_true` (SC #5 legacy backward-compat regression at atol=1e-12)</name>
  <files>tests/test_phase05_negative_prices.py</files>
  <read_first>
    - tests/test_phase05_negative_prices.py (post-Task-6 state — 6 of 7 remaining stubs promoted; this is the last one)
    - tests/fixtures/baseline_pfc_seed42.parquet (5bis-A frozen canonical baseline — generated under the legacy multiplicative + F_WV_FLOOR + m_factor floor + Peak ratio path)
    - tests/fixtures/_generate_baseline.py (5bis-A pattern — provides `build_pfc(seed=42)` which is the canonical build that produced baseline_pfc_seed42.parquet on `main@28dfd65`)
    - .planning/phases/05-msfc-log-prix-retire-silent-floors/05-CONTEXT.md §decisions D-A2-3, D-A2-5 (operator rollback path; baseline 5bis-A should remain green via enforce_*=True)
    - .planning/phases/05-msfc-log-prix-retire-silent-floors/05-RESEARCH.md §Architecture Patterns §Pattern 1 (ctor arg behavior), §Dry-Run §D-A3-3 (the rollback path restores the multiplicative WV behavior bit-pour-bit when enforce_floor=True triggers the legacy clip-+-multiplicative branch)
    - .planning/phases/05B-shape-hourly-infrastructure-flag-no-op-refactor/05B-REVIEWS.md §1 (tolerance contract atol=1e-12 rtol=0 + identical schema)
  </read_first>
  <action>
    Promote the FINAL stub `test_phase05_baseline_5bisA_via_enforce_true`. This test proves the OPERATOR ROLLBACK PATH (D-A2-3): when all four sub-components are constructed with their LEGACY behavior explicitly enabled, the Phase 5 pipeline reproduces the 5bis-A baseline `baseline_pfc_seed42.parquet` bit-pour-bit at `atol=1e-12, rtol=0`.

    Required test body (B1/B4 RESOLUTION — Approach B per checker feedback 2026-05-19, supersedes the original monkeypatching sketch):

    1. Load the 5bis-A baseline: `baseline_5bisA = pd.read_parquet(_BASELINE_5BISA_PATH)`. The constant `_BASELINE_5BISA_PATH = Path("tests/fixtures/baseline_pfc_seed42.parquet")` was defined in Plan 05-01 module-level constants.
    2. Verify the baseline file exists; HARD-FAIL with a clear diagnostic if missing.
    3. Construct the 5bis-A inputs IDENTICALLY to how `tests/fixtures/_generate_baseline.py` constructs them — same forwards dict, same seed=42, same epex/hydro synthetic histories. Reuse the helper if exposed (`from tests.fixtures._generate_baseline import build_pfc`); otherwise inline the construction.
    4. Construct each sub-component with its DEFAULT Phase 5 ctor args (no need to override at sub-component level — the four PFCAssembler kwargs forward to them per Task 2 Edit B):
       - `ShapeHourly(use_seasonal_hourly=False)` (5bis-A default; flag=OFF reproduces baseline 5bis-A — this is OUT OF SCOPE of the four Phase 5 floor kwargs; ShapeHourly's flag is unrelated to negative-prices floors).
       - `WaterValueCorrection()` (default; PFCAssembler will override `wv.enforce_floor = True` via the kwarg forwarding wired in Task 2 Edit B).
       - `ContractCascader()` (default; PFCAssembler will override `cascader.allow_negative_peak = False` via the kwarg forwarding wired in Task 2 Edit B). Note: the legacy multiplicative `synthesize_peak_prices` path (Task 1 Edit D `else:` branch) requires `peak_base_ratios_` populated. Call `cascader.fit_peak_ratios(spot_history)` BEFORE constructing the assembler — this emits a DeprecationWarning (acceptable in this test; `pytest.warns(DeprecationWarning)` context wraps the call) AND populates `peak_base_spreads_` PLUS `peak_base_ratios_` via the codex-action-#2 UNIFIED SHIM contract (Task 1 Edit C). The legacy `else:` branch in `synthesize_peak_prices` consumes `peak_base_ratios_` directly — this now works because codex action #2 derives it during the deprecation window.
       - `ArbitrageFreeCalibrator()` (default; PFCAssembler will override `calibrator.enforce_m_factor_floor = True` via the kwarg forwarding wired in Task 2 Edit B).
    5. **B1/B4 RESOLUTION — no monkeypatching needed.** Construct the assembler with the four explicit floor kwargs (D-A2-3 rollback path):
       ```python
       assembler = PFCAssembler(
           shape_hourly=sh,
           shape_intraday=si,
           uncertainty=unc,
           water_value=wv,
           cascader=cascader,
           calibrator=calibrator,
           enforce_positivity=True,          # NEG-01 rollback (MSFC floor restored via assembler.py:397 forwarding)
           enforce_m_factor_floor=True,      # NEG-02 rollback (m_factor floor restored via attribute override in Task 2)
           enforce_floor=True,               # NEG-03 rollback (F_WV_FLOOR clip restored via attribute override in Task 2)
           allow_negative_peak=False,        # NEG-04 rollback (peak ratio>=1 restored via attribute override in Task 2)
       )
       ```
       This is the canonical D-A2-3 operator rollback path. NO `monkeypatch.setattr`, NO `functools.partial`. The four explicit ctor args replace the monkeypatching sketch from the original plan.
    6. Set env-var `PFC_LT_ALLOW_NEGATIVE_PRICES=0` (audit-trail INFO only; the actual behavioral rollback is via the four ctor args above — the env-var has no override semantic per D-A2-2).
    7. Call `result = assembler.build(base_prices=forwards_5bisA, seed=42)`. The MSFC kwarg `enforce_positivity=True` propagates to `smooth_base_prices()` at assembler.py:397 via the call-site forwarding wired in Task 2 Edit B.
    8. Assert `pd.testing.assert_frame_equal(result, baseline_5bisA, check_exact=False, atol=1e-12, rtol=0, check_dtype=True, check_column_type=True, check_index_type=True)`.

    **Important diagnostic guidance for the executor:**

    If this test FAILS at `atol=1e-12` despite all four ctor args set to legacy values:
    1. Capture the divergence: `np.abs(rebuild - baseline_5bisA).max().max()` and the maximum-divergence coordinate.
    2. Check whether the divergence is in the columns produced by WV (the multiplicative-vs-additive path branching in Plan 05-02 might not fully restore bit-pour-bit equality when enforce_floor=True). The legacy multiplicative path is `price_raw = B * f_S * f_W * f_H * f_Q * f_WV * f_bridge` — this should be EXACTLY the formula used pre-Phase-5 when `enforce_floor=True`. If the test still diverges, Plan 05-02 Task 2's "delta_wv_pending=False" branch is not perfectly preserving the legacy path.
    3. Document the divergence in SUMMARY with the exact numerical magnitude. If `< 1e-10`, the residual may be acceptable as a floating-point reordering artifact (e.g., NumPy operation order changed between Phase 5 branches); in that case, loosen the tolerance for THIS specific test to `atol=1e-10` and document the relaxation in the test docstring + SUMMARY. If `>> 1e-10`, the rollback path is BROKEN and Plan 05-02 / Task 1 needs revision before this plan completes.

    Docstring: test ID `5-03-06`, references D-A2-3 operator rollback path, 5bis-A REVIEWS tolerance contract, SC #5 ROADMAP literal "feature flag PFC_LT_ALLOW_NEGATIVE_PRICES=0 permet rollback".
  </action>
  <verify>
    <automated>pytest tests/test_phase05_negative_prices.py::test_phase05_baseline_5bisA_via_enforce_true -x -v 2>&1 | tail -10 && pytest tests/test_phase05_negative_prices.py -q --tb=short 2>&1 | tail -5 && pytest tests/ -q --tb=short 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_phase05_negative_prices.py::test_phase05_baseline_5bisA_via_enforce_true -x` exits 0 (legacy backward-compat rollback path proven bit-pour-bit at atol=1e-12).
    - **B1/B4 Approach B verification (no monkeypatching in the test body):**
      - `! grep -q 'monkeypatch\.' tests/test_phase05_negative_prices.py` exits 0 — the test does NOT use monkeypatch on `smooth_base_prices` (or any other identifier). The four PFCAssembler kwargs replace the monkeypatching pattern. (Bash form portable across BSD/GNU per WARNING 4 fix; the previous `grep -c ... | awk '$1 == 0 || exit 1'` idiom parses inconsistently between awk implementations.)
      - `grep -q "enforce_positivity=True" tests/test_phase05_negative_prices.py` exits 0 — explicit kwarg present in the test body.
      - `grep -q "enforce_m_factor_floor=True" tests/test_phase05_negative_prices.py` exits 0.
      - `grep -q "enforce_floor=True" tests/test_phase05_negative_prices.py` exits 0.
      - `grep -q "allow_negative_peak=False" tests/test_phase05_negative_prices.py` exits 0.
    - `pytest tests/test_phase05_negative_prices.py -q --tb=short` exits 0 reporting one of:
      - `15 passed` (all 15 tests green — best case, SC #2 ran green).
      - `14 passed, 1 skipped` (SC #2 gated-skipped — acceptable per D-A4-5).
    - `grep -c "populated in Plan 05-0" tests/test_phase05_negative_prices.py` returns 0 (no stubs remain).
    - `grep -q "5-03-06" tests/test_phase05_negative_prices.py` exits 0 (test-ID traceability).
    - `pytest tests/ -q --tb=short` exits 0 reporting `273 passed, 3 skipped` (best case) OR `272 passed, 4 skipped` (SC #2 gated-skipped).
    - If the test required tolerance relaxation (atol > 1e-12) due to floating-point reordering across the Phase 5 branches, the SUMMARY records the exact atol used and the divergence magnitude, AND the test docstring contains a TODO marker referencing the residual.
  </acceptance_criteria>
  <done>Last stub populated: `test_phase05_baseline_5bisA_via_enforce_true` proves the operator rollback path D-A2-3 restores 5bis-A baseline at atol=1e-12 rtol=0 (or documents the exact residual if reordering artifacts force a relaxation). Test suite finalized at `273 passed, 3 skipped` (best case) or `272 passed, 4 skipped`.</done>
</task>

<task type="auto">
  <name>Task 8: Add `D-FLIP-2` entry to PROJECT.md per D-A5-1 final-plan obligation + emit COMMIT 2 per codex action #5</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md `## Key Decisions` section (locate the D-FLIP-1 5bis-B flag flip strategy entry — the new entry follows the same format)
    - .planning/phases/05-msfc-log-prix-retire-silent-floors/05-CONTEXT.md §decisions D-A5-1 (Plan 05-03 obligation to update PROJECT.md), D-A2-2, D-A2-3 (master flag audit-trail + rollback path = the content of D-FLIP-2)
    - .planning/phases/05C-shape-hourly-bowl-deepening/05C-CONTEXT.md §decisions D-FLIP-1 (pattern reference — same documentation discipline)
  </read_first>
  <action>
    Append a new entry to `.planning/PROJECT.md` `## Key Decisions` section (or equivalent — locate the existing D-FLIP-1 entry from 5bis-B and place D-FLIP-2 immediately after it for chronological order).

    Required entry content (exact wording, with today's date 2026-05-19):

    ```
    ### D-FLIP-2 — Phase 5 negative prices defaults + operator rollback (2026-05-19)

    **Decision:** Phase 5 (MSFC retire silent floors + PFC peut être négative)
    livrée 2026-05-19. Defaults negative-ready :
    - `smooth_base_prices(enforce_positivity=False)` (MSFC floor l.131 + l.203 désactivés)
    - `ArbitrageFreeCalibrator(enforce_m_factor_floor=False)` (m_factor floor 0.1 désactivé)
    - `WaterValueCorrection(enforce_floor=False)` (F_WV_FLOOR clip désactivé)
    - `ContractCascader(allow_negative_peak=True)` (peak ratio >= 1 désactivé, spread additif)

    Master flag `PFC_LT_ALLOW_NEGATIVE_PRICES` est AUDIT-TRAIL INFO LOG ONLY
    (D-A2-2) — NON un override. Les 4 ctor args ci-dessus sont la véritable
    surface API.

    **Operator rollback (D-A2-3) — B1/B4 Approach B :** construire
    `PFCAssembler(..., enforce_positivity=True, enforce_m_factor_floor=True,
    enforce_floor=True, allow_negative_peak=False)` aux 4 callsites production
    (`autoresearch.py:234`, `production_phases.py:344,644`, `rolling_update.py:365`).
    Les quatre kwargs sont sur `PFCAssembler.__init__` (Plan 05-03 Task 2
    Edit B Approach B). Le forwarding vers les sous-composants se fait
    automatiquement :
    - `enforce_positivity` → passé à `smooth_base_prices(...)` au callsite
      `assembler.py:397` (propagation vers `_enforce_mean_constraints`
      ligne 203 par RESEARCH Pitfall 1).
    - `enforce_m_factor_floor` → écrit sur `self.calibrator.enforce_m_factor_floor`
      à `__init__` quand True.
    - `enforce_floor` → écrit sur `self.wv.enforce_floor` à `__init__` quand True.
    - `allow_negative_peak=False` → écrit sur `self.cascader.allow_negative_peak`
      à `__init__`.

    Régression bit-pour-bit du 5bis-A baseline préservée via cette quadruple
    rollback : `tests/test_phase05_negative_prices.py::test_phase05_baseline_5bisA_via_enforce_true`
    asserte `assert_frame_equal(..., atol=1e-12, rtol=0)` contre
    `tests/fixtures/baseline_pfc_seed42.parquet` (5bis-A frozen baseline).

    **Baseline canonique Phase 5 :** `tests/fixtures/baseline_pfc_seed42_phase05.parquet`
    (post-floors-off, post-delta-additif WV, post-spread-additif Peak, flag
    PFC_LT_USE_SEASONAL_HOURLY_SHAPE=1 et PFC_LT_ALLOW_NEGATIVE_PRICES=1).
    Convention D-A4-9 5bis-B "new baseline per math change atomique" suivie.

    **Tests :** 7 nouveaux tests dans `tests/test_phase05_negative_prices.py` +
    3 tests ajoutés par la révision codex --reviews itération 1 (clamp edge ×2,
    converged-reason, delta_wv index alignment) + 1 test ajouté par codex action #7
    (fallback spread). Total 15 tests Phase 5.

    **Codex review --reviews itération 1 (2026-05-19) — 7 action items closés :**
    Voir `.planning/phases/05-msfc-log-prix-retire-silent-floors/05-REVIEWS.md`
    pour le détail. Tous les items #1 (compute_delta_wv keyword-only +
    assert index alignment), #2 (unified shim contract fit_peak_ratios), #3
    (clamp margin floor + 2 edge tests), #4 (UTC→Europe/Zurich conversion
    in SC #2 test), #5 (commit-boundary split en 2 commits), #6 (reason-tagged
    converged=False logging), #7 (fallback spread + warning) sont implémentés
    dans Plans 05-01 / 05-02 / 05-03 surgically revised.

    **Flip default OFF→ON :** NON applicable — Phase 5 est livrée defaults
    OFF (negative-ready). Le rollback opérateur est un cas exceptionnel. Pas
    de "date de flip" comme D-FLIP-1 5bis-B.

    **Référence :** `.planning/phases/05-msfc-log-prix-retire-silent-floors/05-CONTEXT.md`
    + `05-RESEARCH.md` + `05-VALIDATION.md` + `05-REVIEWS.md`.
    ```

    Do NOT modify any other section of PROJECT.md. The entry MUST be placed in chronological order (after D-FLIP-1 if it exists, or in a "Phase 5" section if PROJECT.md groups by phase).

    Verify the entry is committed and grep-discoverable.

    **Codex action #5 — Commit 2 finalization:** AFTER Task 8 completes, emit the SECOND commit per the boundary mapping in the Revision Log: `git commit -m "feat(05): master flag + fixtures + phase05 tests + docs"` covering files modified in Tasks 2 + 3 + 5 + 6 + 7 + 8 (assembler.py + _generate_phase05_fixture.py + forwards_phase05_seed42.parquet + baseline_pfc_seed42_phase05.parquet + test_phase05_negative_prices.py + PROJECT.md). Commit 1 was emitted at end of Task 4.
  </action>
  <verify>
    <automated>grep -q "D-FLIP-2" .planning/PROJECT.md && grep -q "Phase 5 negative prices defaults" .planning/PROJECT.md && grep -q "audit-trail INFO log only\|AUDIT-TRAIL INFO LOG ONLY\|audit-trail INFO LOG ONLY" .planning/PROJECT.md && grep -q "operator rollback\|rollback opérateur" .planning/PROJECT.md && grep -q "baseline_pfc_seed42_phase05" .planning/PROJECT.md && grep -q "codex review --reviews\|codex action" .planning/PROJECT.md && echo OK PROJECT.md D-FLIP-2</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "D-FLIP-2" .planning/PROJECT.md` returns ≥ 1.
    - `grep -q "Phase 5" .planning/PROJECT.md && grep -q "2026-05-19" .planning/PROJECT.md` exits 0 (entry properly dated).
    - `grep -q "enforce_positivity\|enforce_m_factor_floor\|enforce_floor\|allow_negative_peak" .planning/PROJECT.md` exits 0 (the 4 ctor args documented as the API surface).
    - `grep -q "PFC_LT_ALLOW_NEGATIVE_PRICES" .planning/PROJECT.md` exits 0 (master flag documented).
    - `grep -q "baseline_pfc_seed42_phase05\|baseline_pfc_seed42.parquet" .planning/PROJECT.md` exits 0 (baseline references documented for traceability).
    - **Codex review --reviews iteration 1 traceability in PROJECT.md (NEW):** `grep -q "codex review --reviews\|codex action #\|--reviews itération 1" .planning/PROJECT.md` exits 0 (the 7 action items are summarized for cross-phase memory).
    - The verify command above prints `OK PROJECT.md D-FLIP-2`.
    - `pytest tests/ -q --tb=short` exits 0 (final test count unchanged from Task 7).
    - **Codex action #5 — Commit 2 finalization:** `git log --oneline -2 | grep -cE "^[a-f0-9]+ (refactor\(05\)|feat\(05\)):"` returns 2 (two commits emitted in this plan per the action #5 boundary mapping; first commit was emitted at end of Task 4 with `refactor(05):`, second commit is emitted now with `feat(05):`).
  </acceptance_criteria>
  <done>PROJECT.md gains D-FLIP-2 entry documenting Phase 5 ship, defaults (negative-ready), operator rollback path, canonical baseline, test surface, AND codex review --reviews iteration 1 closure summary. Cross-AI audit trail complete per D-A5-1. **COMMIT 2 of codex action #5 emitted at end of this task** with message `feat(05): master flag + fixtures + phase05 tests + docs` covering Tasks 2 + 3 + 5 + 6 + 7 + 8 outputs. Total of 2 commits emitted in this plan per the codex-action-#5 boundary mapping.</done>
</task>

</tasks>

<verification>
After all 8 tasks complete, run the full validation gate per 05-VALIDATION.md:

```bash
# Quick gate (per-task feedback)
pytest tests/test_phase05_negative_prices.py -x -q

# Full suite gate (per-wave merge / pre /gsd:verify-work)
pytest tests/ -q --tb=short

# Tolerance contract gate
pytest tests/test_phase05_negative_prices.py::test_phase05_baseline_regression -x -q
pytest tests/test_phase05_negative_prices.py::test_phase05_baseline_5bisA_via_enforce_true -x -q

# Deprecation warning audit (should still be silent in production after Task 4 migration)
pytest tests/ -q -W error::DeprecationWarning 2>&1 | tail -5

# Codex action #5 commit boundary verification
git log --oneline -2 | grep -E "^[a-f0-9]+ (refactor\(05\)|feat\(05\)):"
```

Expected outcomes (TWO acceptable variants):
- **Best case** (5bis-B bowl marker present, full Phase 5 chain works end-to-end):
  - `pytest tests/test_phase05_negative_prices.py -q` → `15 passed`.
  - `pytest tests/ -q` → `273 passed, 3 skipped` (full suite final).
- **SC #2 gated-skip** (5bis-B bowl marker absent in this dev environment):
  - `pytest tests/test_phase05_negative_prices.py -q` → `14 passed, 1 skipped`.
  - `pytest tests/ -q` → `272 passed, 4 skipped`.

Both variants are valid Phase 5 completion per D-A4-5 gating logic.

Tolerance contract verification:
- `test_phase05_baseline_regression` (SC #5 canonical) → MUST PASS at atol=1e-12 rtol=0.
- `test_phase05_baseline_5bisA_via_enforce_true` (SC #5 legacy) → MUST PASS at atol=1e-12 rtol=0; if Task 7 required relaxation due to floating-point reordering, the test docstring + SUMMARY record the exact atol used and the divergence magnitude.

Deprecation audit:
- `pytest tests/ -q -W error::DeprecationWarning` exits 0 (no production code path triggers DeprecationWarning after Task 4 migration; the shim is only exercised in the dedicated `test_fit_peak_ratios_deprecated` which uses `pytest.warns` context).

**Codex action #5 — Commit boundary verification:** the `git log --oneline -2` command should show TWO commits in chronological order: `refactor(05): cascading spread-additive + production callsites migration` (emitted at end of Task 4) and `feat(05): master flag + fixtures + phase05 tests + docs` (emitted at end of Task 8). This is the cross-AI audit trail for the codex-action-#5 split.
</verification>

<success_criteria>
This plan is complete when:
- [x] `pfc_shaping/calibration/cascading.py::ContractCascader` exposes `allow_negative_peak: bool = True`, new `fit_peak_spreads(spot_history)` method (with codex-action-#7 empty-history fallback + UserWarning), UNIFIED SHIM contract on `fit_peak_ratios` per codex action #2 (DeprecationWarning + delegate + derive `peak_base_ratios_`), and a branched `synthesize_peak_prices` (spread-additive default, multiplicative legacy under allow_negative_peak=False).
- [x] `pfc_shaping/lt/model/assembler.py::PFCAssembler.__init__` accepts `allow_negative_prices: bool | None = None` AND four explicit floor kwargs (B1/B4 Approach B), reads PFC_LT_ALLOW_NEGATIVE_PRICES once via `_resolve_allow_negative`, emits ONE INFO audit log per construction with the four floor states.
- [x] No new sidecar parquet created for the master flag (D-A2-4 resolution: audit-trail INFO log only).
- [x] `tests/fixtures/_generate_phase05_fixture.py` + `tests/fixtures/forwards_phase05_seed42.parquet` committed; deterministic byte-stable seed=42.
- [x] `tests/fixtures/baseline_pfc_seed42_phase05.parquet` committed; contains negative prices (proves floors-off + bowl-on); byte-stable across re-runs.
- [x] Two `fit_peak_ratios` callsites in `production_phases.py` migrated to `fit_peak_spreads`. Phase 5 default doc comments added at 4 production callsites (autoresearch.py:234, production_phases.py:344, production_phases.py:644, rolling_update.py:365).
- [x] All 15 tests in `tests/test_phase05_negative_prices.py` populated (no skip-stub remains in the "populated in Plan 05-0X" form). Test outcomes: 14-15 passing depending on 5bis-B bowl marker presence (codex action #4 explicit Europe/Zurich conversion wired in SC #2 test).
- [x] PROJECT.md `## Key Decisions` gains a D-FLIP-2 entry per D-A5-1 obligation, documenting Phase 5 ship, defaults, operator rollback path, canonical baseline, AND codex review --reviews iteration 1 closure summary.
- [x] Full test suite ends at `273 passed, 3 skipped` (best case) OR `272 passed, 4 skipped` (SC #2 gated-skipped). Both are valid Phase 5 completion.
- [x] No DeprecationWarning fires from production paths (`pytest -W error::DeprecationWarning` clean — only test_fit_peak_ratios_deprecated triggers the warning under explicit `pytest.warns` context).
- [x] Cross-AI audit trail complete: SUMMARY records (a) the four production-callsite locations and edits, (b) the `fit_peak_ratios` audit grep result, (c) any tolerance relaxation required by Task 7 with magnitude, (d) the test outcome (best case vs SC #2 gated-skip), (e) the two commit SHAs and messages emitted per codex action #5.
- [x] Codex review --reviews iteration 1 — all 4 action items in scope of this plan (#2, #4, #5, #7) fully closed. The three other items (#1, #3, #6) were closed in Plans 05-02 and 05-01 respectively.
</success_criteria>

<output>
Create `.planning/phases/05-msfc-log-prix-retire-silent-floors/05-03-SUMMARY.md` when done. Include:
- The 8 file diffs (cascading.py, assembler.py, autoresearch.py, production_phases.py, rolling_update.py, _generate_phase05_fixture.py, test_phase05_negative_prices.py, PROJECT.md) summary by line count + key signatures.
- The new fixture binaries (`forwards_phase05_seed42.parquet`, `baseline_pfc_seed42_phase05.parquet`) with sha256 and size.
- The two `fit_peak_ratios → fit_peak_spreads` migration line numbers.
- The 4 production-callsite doc comments — exact file:line for each.
- Test outcome: which of `273 passed, 3 skipped` / `272 passed, 4 skipped` was achieved, and if SC #2 ran green, the actual `pfc[Sunday, h13 Europe/Zurich, July 2027].mean()` value AND the timezone conversion idiom used (codex action #4 traceability).
- The Task 7 outcome: did `test_phase05_baseline_5bisA_via_enforce_true` pass at atol=1e-12 rtol=0, or did tolerance relaxation apply? If yes, magnitude.
- **Codex action #5 — Commit boundary outputs:** the TWO commit SHAs and messages (`refactor(05):` and `feat(05):`) emitted in this plan, with the file lists per commit.
- The Phase 5 sign-off statement: "Phase 5 — MSFC retire silent floors + PFC peut être négative — LIVRÉE 2026-05-19. Defaults negative-ready actifs en production. Operator rollback documented in PROJECT.md D-FLIP-2. Codex review --reviews iteration 1 fully closed (7 action items)."
- Pointer to follow-up work (Phase 5ter Monte-Carlo + Phase 10 backtest réel HFC OMPEX — both inheritances de Phase 5).
</output>

## PHASE GOAL

**As a** trader FMV pricing profile deals GRD (bloc nuit 18-9 + bloc solaire WE OP1/OP2 racheté),
**I want to** voir la PFC LT mid-market piquer à -20/-25 €/MWh à h13 dimanche d'été 2027+ (heures structurellement négatives sur PV-excess CH/DE/AT),
**so that** le bloc solaire racheté soit valorisé au juste prix (pas sur-acheté contre une production qui sera vendue spot à -X €/MWh), unblock the SOTA shape depth required for the Phase 10 backtest vs HFC OMPEX.
