---
phase: 10
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
  - tests/conftest.py
  - pfc_shaping/requirements.txt
  - pfc_shaping/validation/__init__.py
  - pfc_shaping/validation/block_masks.py
  - pfc_shaping/validation/scorecard.py
  - tests/test_phase10_infra.py
  - data/epex_15min.parquet
  - data/forwards_history_phase10.parquet
  - .planning/phases/PFC-LT-10-backtest-par-bloc-vs-hfc-ompex/10-01-NOTES.md
autonomous: false
requirements: [BT-01, BT-02, BT-06, BT-07, BT-08, BT-09, BT-10]

must_haves:
  truths:
    - "Les 5 blocs client renommés (D-A2-2) sont implémentés comme classes BlockMask avec apply(idx_utc)->bool ndarray"
    - "Le scorecard harness skeleton expose `AblationConfig`, `list_vintages_2024_2025()`, `build_one()`, `derive_forwards_from_epex_hist()`, `run_scorecard_pillar_1()` sans encore évaluer aucun pillar"
    - "Le cache `data/epex_15min.parquet` couvre 2019-01-01..2025-12-31 (ou décline explicitement avec fallback documenté)"
    - "La question forwards-as-of-vintage (RESEARCH Q2) est tranchée empiriquement : snapshot historique OU dérivation depuis EPEX hist Cal/Q/M means via le helper derive_forwards_from_epex_hist"
    - "Le ratio empirique mean(EPEX|weekend∪VS_holidays)/mean(EPEX|weekday) sur 2019-2023 est mesuré et le threshold Hildmann pour Pillar 1.2 est tranché (RESEARCH Pitfall 1)"
    - "[C2 REVIEWS] La RÈGLE de recalibration Pillar 1.2 (méthode UNIQUE + scope + trigger) est gelée EX-ANTE dans `10-01-NOTES.md` AVANT toute mesure empirique. Aucun changement de seuil ad-hoc autorisé post-mesure : si le ratio empirique sort de [0.65, 0.95], la formule pré-gelée s'applique mécaniquement (pas de décision politique en cours d'exécution)"
    - "[C3 REVIEWS] Le helper `derive_forwards_from_epex_hist` (Q2 fallback) est marqué `forwards_source='fallback_diagnostic'` dans une métadonnée structurée propagée au scorecard parquet — pas seulement un log line ; toute cellule Pillar 1/4 calculée via ce fallback est traçable structurellement"
    - "ROADMAP/REQUIREMENTS/PROJECT.md reflètent le pivot D-A0-2 : Phase 10 = scorecard, BT-03/BT-05 → Phase 10B deferred, BT-04 reformulé DM vs 3 baselines, BT-06..BT-10 créés pour les 5 piliers"
    - "conftest.py snapshot/restore PFC_LT_* couvre les deux flags par préfixe (déjà cas), validé par test ajouté"
  artifacts:
    - path: "pfc_shaping/validation/block_masks.py"
      provides: "5 BlockMask classes + ALL_BLOCKS list per RESEARCH §Pattern 1"
      exports: ["BlockMask", "BlockOvernightWeekday", "BlockMiddayWeekday", "BlockWeekendMidday", "BlockSummerSolarBowl", "BlockWinterEveningPeak", "ALL_BLOCKS"]
    - path: "pfc_shaping/validation/scorecard.py"
      provides: "Skeleton harness: AblationConfig dataclass, list_vintages_2024_2025, build_one, derive_forwards_from_epex_hist (Q2 fallback helper consommé Task 3) + FORWARDS_SOURCE_REAL/_FALLBACK_DIAGNOSTIC constants (C3 REVIEWS), run_scorecard_pillar_1 stub"
      exports: ["AblationConfig", "ABLATION_GRID", "list_vintages_2024_2025", "last_business_day_of_month", "build_one", "derive_forwards_from_epex_hist", "FORWARDS_SOURCE_REAL", "FORWARDS_SOURCE_FALLBACK_DIAGNOSTIC", "run_scorecard_pillar_1"]
    - path: "data/epex_15min.parquet"
      provides: "Cache EPEX 15-min CH 2019-01-01..2025-12-31 (gitignored, bootstrap au premier run)"
      contains: "DatetimeIndex tz=UTC freq=15min + colonne 'price'"
    - path: "tests/test_phase10_infra.py"
      provides: "5+ tests synthétiques validant block_masks tz-safety, vintages count=24, AblationConfig grid integrity, conftest env hygiene couvre les 2 flags, fixture fallback path"
    - path: ".planning/phases/PFC-LT-10-backtest-par-bloc-vs-hfc-ompex/10-01-NOTES.md"
      provides: "Notes empiriques Plan 10-01 : (a) RECALIBRATION METHOD frozen ex-ante (C2 REVIEWS), (b) ratio holiday/weekend mesuré 2019-2023, (c) décision threshold Pillar 1.2 = application mécanique de la formule pré-gelée, (d) décision forwards-as-of-vintage path + fallback_diagnostic marker convention (C3 REVIEWS)"
    - path: ".planning/REQUIREMENTS.md"
      provides: "BT-01 préservé, BT-02 renommé blocs D-A2-2, BT-03/BT-05 retirés vers nouveau groupe BT-10B-01/BT-10B-02, BT-04 reformulé DM vs 3 baselines, BT-06..BT-10 ajoutés (1 par pilier)"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 10 title 'PFC FMV Quality Scorecard (5-pillar SOTA replication)', nouvelle entry Phase 10B 'PFC FMV vs HFC OMPEX (deferred, requires FMV poste H:\\)', mise à jour des Plans listés"
    - path: ".planning/PROJECT.md"
      provides: "Entrée 2026-05-20 Key Decisions documentant le pivot + update wording D-FLIP-1 (gate = SC#1 Hildmann 4/4 PASS Phase 10)"
  key_links:
    - from: "pfc_shaping/validation/scorecard.py::build_one"
      to: "pfc_shaping.lt.model.assembler.PFCAssembler"
      via: "PFCAssembler(sh, si, unc, enforce_positivity=..., enforce_m_factor_floor=..., enforce_floor=..., allow_negative_peak=...).build(base_prices, reference_date=vintage)"
      pattern: "PFCAssembler\\(.*enforce_positivity.*allow_negative_peak"
    - from: "pfc_shaping/validation/block_masks.py::BlockMask.apply"
      to: "pandas.DatetimeIndex.tz_convert('Europe/Zurich')"
      via: "tz-convert UTC→local pour calcul mask, return ndarray aligné sur idx UTC"
      pattern: "tz_convert.*Europe/Zurich"
    - from: "pfc_shaping/validation/scorecard.py::list_vintages_2024_2025"
      to: "pandas.tseries.offsets.BMonthEnd"
      via: "BMonthEnd(0) sur Europe/Zurich + tz_convert UTC + 18:00 local pin"
      pattern: "BMonthEnd\\(0\\)"
    - from: "tests/test_phase10_infra.py"
      to: "pfc_shaping.validation.block_masks.ALL_BLOCKS"
      via: "import ALL_BLOCKS + assert len==5 + assert tz-safety on each"
      pattern: "from pfc_shaping.validation.block_masks import"
---

<objective>
Plan 10-01 (wave 1) — Livrer l'infrastructure de la Phase 10 PFC FMV Quality Scorecard sans encore évaluer aucun des 5 piliers, plus le mini-fixup doc qui acte le pivot D-A0-2 (Phase 10 reframée + Phase 10B deferred).

Purpose : Préparer la machine (block masks tz-aware, vintages calendar, ablation grid skeleton, EPEX cache bootstrap, forwards-as-of-vintage path) sans toucher au code core PFC, et trancher les 2 questions ouvertes du RESEARCH avant Plan 10-02 (Pitfall 1 Hildmann holiday-weekend threshold ; Q2 accès forwards historiques Mac Mini). Ouvrir aussi le human-verify checkpoint statsmodels/matplotlib install (RESEARCH §Package Legitimacy Audit).

**[REVIEWS iter 1 — C2/C3 surgical edits]** :
- **C2 (HIGH)** — La méthode de recalibration du threshold Hildmann Pillar 1.2 est **gelée EX-ANTE** (Sub-step 3a-bis) AVANT toute mesure empirique (Sub-step 3b). Formule unique, scope, trigger, forbidden patterns énumérés ; aucun tweak ad-hoc post-mesure.
- **C3 (MEDIUM)** — Le helper `derive_forwards_from_epex_hist` (fallback Q2) émet un marker structuré `forwards_source = "fallback_diagnostic"` propagé via une colonne du parquet `data/forwards_history_phase10.parquet` ; pas un simple log. Plan 10-04 utilisera ce marker pour annoter "Diagnostic only — not gate-eligible" toute cellule Pillar 1/4 dérivée du fallback ; SC#1 ne peut PAS être satisfait par un run fallback.

Output :
- 2 nouveaux modules `pfc_shaping/validation/{block_masks.py,scorecard.py}` (skeleton only, pas de pillar maths)
- cache `data/epex_15min.parquet` bootstrapé 2019-2025 + snapshot forwards historiques (ou décision fallback documentée) AVEC colonne `forwards_source` obligatoire (C3)
- `tests/test_phase10_infra.py` (5+ tests synthétiques verts)
- mises à jour ROADMAP/REQUIREMENTS/PROJECT.md actant le pivot
- `10-01-NOTES.md` documentant les 2 décisions empiriques (threshold Hildmann + forwards path) + 2 sections REVIEWS (C2 recalibration method frozen ex-ante + C3 forwards_source structured flag convention)
- conftest.py confirmation explicite via test que la couverture préfixe `PFC_LT_*` snapshot les 2 flags
- requirements.txt prêt à recevoir statsmodels + matplotlib (matplotlib déjà commenté ligne 38 — Plan 10-02 le décommente in-place + ajoute statsmodels)
- 2 constants module-level `FORWARDS_SOURCE_REAL` et `FORWARDS_SOURCE_FALLBACK_DIAGNOSTIC` exportés depuis `scorecard.py` (C3)
</objective>

<execution_context>
@/Users/julienbattaglia/Desktop/PFC/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julienbattaglia/Desktop/PFC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/PFC-LT-10-backtest-par-bloc-vs-hfc-ompex/10-CONTEXT.md
@.planning/phases/PFC-LT-10-backtest-par-bloc-vs-hfc-ompex/10-RESEARCH.md
@.planning/phases/05B-shape-hourly-infrastructure-flag-no-op-refactor/05B-CONTEXT.md
@.planning/phases/05-msfc-log-prix-retire-silent-floors/05-CONTEXT.md
@pfc_shaping/lt/model/assembler.py
@pfc_shaping/lt/model/shape_hourly.py
@pfc_shaping/lt/model/uncertainty.py
@pfc_shaping/lt/model/shape_intraday.py
@pfc_shaping/data/ingest_energy_charts.py
@pfc_shaping/data/calendar_ch.py
@pfc_shaping/validation/backtest.py
@pfc_shaping/validation/compare_hfc.py
@pfc_shaping/config.yaml
@tests/conftest.py

<interfaces>
<!-- Contracts extraits du codebase — l'executor n'a pas besoin de re-grep. -->

From pfc_shaping/lt/model/assembler.py (lines 235-281, 388-447):
```python
class PFCAssembler:
    def __init__(
        self,
        shape_hourly: ShapeHourly,
        shape_intraday: ShapeIntraday,
        uncertainty=None,
        water_value=None,
        cascader=None,
        calibrator=None,
        calibration_fallback_to_raw: bool = True,
        peak_source_policy: str = "same_first",
        confidence_thresholds: dict[str, float] | None = None,
        allow_negative_prices: bool | None = None,
        enforce_positivity: bool = False,
        enforce_m_factor_floor: bool = False,
        enforce_floor: bool = False,
        allow_negative_peak: bool = True,
    ) -> None: ...

    def build(
        self,
        base_prices: dict,
        quoted_keys: set[str] | None = None,
        start_date: str | None = None,
        horizon_days: int = HORIZON_DAYS,
        entso_forecast: pd.DataFrame | None = None,
        hydro_forecast: pd.DataFrame | None = None,
        outages_forecast: pd.DataFrame | None = None,
        reference_date: pd.Timestamp | None = None,
        country: str = "CH",
    ) -> pd.DataFrame: ...
```

From pfc_shaping/lt/model/shape_hourly.py:
```python
class ShapeHourly:
    def __init__(self, sigma: float | None = None, sigma_off: float = 0.5,
                 sigma_on: float = 0.25, hydro_weight_sigma: float | None = None,
                 hydro_weight_sigma_off: float = 0.25, hydro_weight_sigma_on: float = ...,
                 use_seasonal_hourly: bool | None = None, ...) -> None: ...
    def fit(self, epex_df, calendar_df, hydro_df=None, ...) -> "ShapeHourly": ...
    def apply(self, timestamps, calendar_df, ...) -> pd.Series: ...
```

From pfc_shaping/lt/model/shape_intraday.py:
```python
class ShapeIntraday:
    def __init__(self, halflife_days: float = 180.0, hydro_weight_sigma: float = 0.25) -> None: ...
    def fit(self, epex_df, hydro_df, calendar_df) -> "ShapeIntraday": ...
```

From pfc_shaping/lt/model/uncertainty.py (lines 51-194, READ FOR IC SUPPORT):
```python
class Uncertainty:
    def __init__(self, n_boot: int = N_BOOT, seed: int = SEED) -> None: ...
    def fit(self, epex_df: pd.DataFrame, calendar_df: pd.DataFrame) -> "Uncertainty": ...
    def compute(self, pfc_df, calendar_df, reference_date=None) -> pd.DataFrame:
        # Returns DataFrame with columns ['p10', 'p90'] ONLY — hardcoded percentiles
        # NO level=/confidence= parameter. IC95 (p2.5/p97.5) NOT SUPPORTED natively.
        # Consequence for Plan 10-03 Pillar 3: only IC80 testable in Phase 10.
        # Extending to IC95 = scope-creep on uncertainty.py → deferred Phase 5ter (CONTEXT D-A3-3).
```

From pfc_shaping/data/ingest_energy_charts.py:
```python
def load_prices(start: str, end: str, bzn: str = "CH") -> pd.DataFrame:
    """Returns DataFrame with tz=UTC DatetimeIndex freq=15min + col 'price' (CC BY 4.0)."""
```

From pfc_shaping/data/calendar_ch.py:
```python
def build_calendar(start: str, end: str, country: str = "CH") -> pd.DataFrame: ...
# enrich_15min_index also exposed for tz-aware index enrichment
```

From tests/conftest.py:
```python
@pytest.fixture(autouse=True)
def _pfc_lt_env_hygiene():
    # Snapshot/restore os.environ keys with prefix "PFC_LT_" — couvre déjà les deux flags
```

From .planning/REQUIREMENTS.md (current Backtest by Block section, to be reformulated):
- BT-01 : harness backtest accepte bloc schema {start_hour, end_hour, dow_filter, month_filter}
- BT-02 : 5 blocs 2024-2025 CH spot (naming actuel mauvais — corrigé D-A2-2)
- BT-03 : compare PFC FMV vs HFC OMPEX (à migrer vers BT-10B-01)
- BT-04 : table markdown DM test p-value (à reformuler vs 3 naive baselines maison)
- BT-05 : Δ MAE ≤ -1.5 €/MWh ≥ 3 blocs (à migrer vers BT-10B-02)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1 — Package legitimacy human-verify pour statsmodels + matplotlib</name>
  <what-built>RESEARCH §Package Legitimacy Audit a flaggé `statsmodels==0.14.6` et `matplotlib>=3.7` comme `[ASSUMED]` (slopcheck indisponible Mac Mini). Per planner protocol, ces deux installs requièrent un human-verify blocking-human AVANT toute écriture dans `pfc_shaping/requirements.txt` Plan 10-02. Aucune install effectuée à ce stade — le but est de pré-autoriser pour Plan 10-02.</what-built>
  <how-to-verify>
    1. Ouvrir https://pypi.org/project/statsmodels/0.14.6/ — confirmer : publisher = "statsmodels" (canonical), homepage = github.com/statsmodels/statsmodels, release date Dec 2025, >10M downloads/mois.
    2. Ouvrir https://pypi.org/project/matplotlib/ — confirmer : maintainer = "Matplotlib Developers" (canonical), homepage = matplotlib.org, version stable >=3.7, ~30M downloads/mois.
    3. Confirmer qu'aucun typosquat-like (`statsmodelz`, `matplot1ib`) n'est suggéré sous-jacent.
    4. Si OK, taper "approved" pour autoriser Plan 10-02 à ajouter ces 2 lignes à requirements.txt.
  </how-to-verify>
  <resume-signal>Type "approved" si pypi canonical confirmé OU "rejected — use alternative" + nom du remplaçant.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2 — block_masks.py + scorecard.py skeleton + tests infra</name>
  <files>pfc_shaping/validation/__init__.py, pfc_shaping/validation/block_masks.py, pfc_shaping/validation/scorecard.py, tests/test_phase10_infra.py, pfc_shaping/requirements.txt, tests/conftest.py</files>
  <read_first>
    - pfc_shaping/validation/backtest.py (existing patterns to mirror, lines 1-50 imports + WalkForwardBacktest class)
    - pfc_shaping/validation/compare_hfc.py (_align_for_comparison line 73-91 = duplicate-defense pattern réutilisable)
    - pfc_shaping/lt/model/assembler.py lines 235-281 (PFCAssembler.__init__ signature pour AblationConfig fields)
    - pfc_shaping/lt/model/shape_hourly.py lines 199-310 (__init__ signature for use_seasonal_hourly arg)
    - pfc_shaping/lt/model/uncertainty.py lines 61-66 (Uncertainty(n_boot, seed) signature)
    - pfc_shaping/data/calendar_ch.py lines 60-90 (build_calendar + enrich_15min_index pattern, tz-safety)
    - .planning/phases/PFC-LT-10-backtest-par-bloc-vs-hfc-ompex/10-RESEARCH.md §Pattern 1 (block_masks canonical impl), §Pattern 5 (build_one + list_vintages canonical impl)
    - tests/conftest.py (autouse env hygiene déjà couvre PFC_LT_* par préfixe)
    - pfc_shaping/requirements.txt (vérifier que ligne 38 contient déjà `# matplotlib>=3.7.0` commenté — ne PAS dupliquer)
  </read_first>
  <behavior>
    - Test : `from pfc_shaping.validation.block_masks import ALL_BLOCKS; assert len(ALL_BLOCKS) == 5` et chaque nom matche `block_overnight_weekday`, `block_midday_weekday`, `block_weekend_midday`, `block_summer_solar_bowl`, `block_winter_evening_peak`
    - Test : pour chaque BlockMask, `apply(pd.date_range('2024-06-01', '2024-06-08', freq='15min', tz='UTC'))` retourne `np.ndarray[bool]` de longueur == longueur de l'index ; `apply(idx_tz_naive)` raise `ValueError("Index must be tz-aware (UTC expected).")`
    - Test : `BlockOvernightWeekday().apply(idx)` couvre h=18..23 ET h=0..8 sur weekday, FALSE le samedi/dimanche — vérifier sur une fenêtre 7 jours avec ~22h/jour weekday inclus
    - Test : `BlockSummerSolarBowl().apply(idx)` est FALSE en avril et septembre, TRUE en juin h11-13
    - Test : `list_vintages_2024_2025()` retourne exactement 24 `pd.Timestamp` tz-aware UTC, le premier == 31 jan 2024 18:00 Europe/Zurich convertit UTC (= 2024-01-31 17:00 UTC en hiver), le dernier == 31 déc 2025 18:00 local
    - Test : `last_business_day_of_month(2024, 3)` == 29 mars 2024 (vendredi, Karfreitag-ignoring per RESEARCH Pitfall 4 documented acceptable)
    - Test : `ABLATION_GRID` contient exactement 4 `AblationConfig` (legacy / 5bisB-only / Phase5-only / target) avec les flags exacts D-A6-1
    - Test : `conftest._pfc_lt_env_hygiene` snapshot/restore prouvable sur `PFC_LT_USE_SEASONAL_HOURLY_SHAPE` ET `PFC_LT_ALLOW_NEGATIVE_PRICES` (set in test → assert restored after) — sanity D-A6-4 hygiene
  </behavior>
  <action>
    Créer `pfc_shaping/validation/__init__.py` (file vide ou avec docstring module + `__all__ = ["block_masks", "scorecard"]`).

    Créer `pfc_shaping/validation/block_masks.py` per RESEARCH §Pattern 1 (lignes 182-265) :
    - `BlockMask` base class avec `name: str` class attr + méthode `apply(idx_utc: pd.DatetimeIndex) -> np.ndarray[bool]` qui raise ValueError sur tz-naive, convertit en Europe/Zurich pour `_mask_local`, retourne array aligné sur `idx_utc`.
    - 5 subclasses : `BlockOvernightWeekday` (hour>=18 | hour<9, weekday<5), `BlockMiddayWeekday` (10<=hour<15, weekday<5), `BlockWeekendMidday` (11<=hour<15, weekday>=5), `BlockSummerSolarBowl` (11<=hour<14, 5<=month<=8), `BlockWinterEveningPeak` (17<=hour<21, weekday<5, month>=11 | month<=2).
    - Module-level constant `ALL_BLOCKS: list[BlockMask] = [BlockOvernightWeekday(), BlockMiddayWeekday(), BlockWeekendMidday(), BlockSummerSolarBowl(), BlockWinterEveningPeak()]`.
    - Type hints stricts, docstrings courts indiquant les heures locales Europe/Zurich.

    Créer `pfc_shaping/validation/scorecard.py` per RESEARCH §Pattern 5 (lignes 474-549) — SKELETON ONLY, pas de pillars maths à ce stade. Le skeleton inclut le helper `derive_forwards_from_epex_hist` qui sera consommé Task 3 sub-step 3c (résolution Q2 forwards fallback Mac Mini).
    - `from __future__ import annotations` ; imports `dataclass`, `pd`, `np`, `PFCAssembler`, `ShapeHourly`, `ShapeIntraday`, `Uncertainty`, `enrich_15min_index`.
    - `@dataclass(frozen=True) class AblationConfig` avec champs : `name: str, use_seasonal_hourly: bool, enforce_positivity: bool, enforce_m_factor_floor: bool, enforce_floor: bool, allow_negative_peak: bool`.
    - Module constant `ABLATION_GRID: list[AblationConfig]` avec les 4 configs exactes D-A6-1 (noms `bowl_off_floors_on`, `bowl_on_floors_on`, `bowl_off_floors_off`, `bowl_on_floors_off`).
    - Helper `last_business_day_of_month(year: int, month: int) -> pd.Timestamp` per RESEARCH §Code Examples (lignes 679-684) : `pd.Timestamp(f"{year}-{month:02d}-01", tz="Europe/Zurich") + BMonthEnd(0)`, replace(hour=18,minute=0), tz_convert("UTC").
    - `list_vintages_2024_2025() -> list[pd.Timestamp]` : list comprehension sur years (2024, 2025) × month (1..12), retourne 24 timestamps.
    - `def build_one(config: AblationConfig, vintage: pd.Timestamp, epex_hist: pd.DataFrame, forwards_asof: dict, with_uncertainty: bool = False) -> pd.DataFrame` per RESEARCH §Pattern 5 lignes 516-548 : fit-from-scratch (`epex_hist.index < vintage` strict, no leakage per Pitfall 2), construit ShapeHourly/Intraday/Uncertainty/Assembler avec config.flags, returns `assembler.build(base_prices=forwards_asof, start_date=vintage.strftime, horizon_days=3*365, reference_date=vintage)`.
    - `def derive_forwards_from_epex_hist(epex_hist: pd.Series, vintage: pd.Timestamp, horizon_days: int = 3*365) -> dict[str, float]` : SKELETON helper (Q2 fallback Mac Mini, consommé Task 3 sub-step 3c). Pour `vintage` donné, filtre `epex_hist` sur `index < vintage`, compute Cal/Q/M means sur les fenêtres futures `[vintage, vintage + horizon_days]` perçues comme "would-be-forwards" via shifting EPEX hist équivalent (pre-vintage windows utilisés comme proxy). Retourne dict `{'YYYY': cal_mean, 'YYYY-QN': quarter_mean, 'YYYY-MM': month_mean}` avec convention parser keys identique à `assembler.build(base_prices=...)`. Skeleton initial peut être un stub retournant `{}` + raise `NotImplementedError("derive_forwards_from_epex_hist body in Task 3 sub-step 3c")` — la signature seule suffit Plan 10-02 pour importer.
    - `def run_scorecard_pillar_1(config_name: str, cache_dir, epex_source: str = "mock") -> dict` : STUB qui raise `NotImplementedError("Pillar 1 wiring deferred to Plan 10-02")` — interface only.

    Étendre `tests/conftest.py` UNIQUEMENT si nécessaire (vérifier d'abord avec read) : la fixture autouse `_pfc_lt_env_hygiene` couvre déjà `PFC_LT_*` par préfixe (ligne 35 : `k.startswith("PFC_LT_")`), donc PFC_LT_USE_SEASONAL_HOURLY_SHAPE + PFC_LT_ALLOW_NEGATIVE_PRICES sont automatiquement gérés. Aucune modification requise — ajouter seulement un commentaire de docstring si le commentaire existant ne mentionne pas explicitement les 2 flags Phase 10 scope.

    Modifier `pfc_shaping/requirements.txt` (PAS encore d'install — juste annotation) :
    - **In-place replace** de la ligne existante `# matplotlib>=3.7.0` (ligne 38) par : `# matplotlib>=3.7.0  # Phase 10 (Plan 10-02 wiring) : scorecard figures PNG, human-verified Plan 10-01 Task 1` — utiliser Edit tool avec `old_string="# matplotlib>=3.7.0"` et `new_string="# matplotlib>=3.7.0  # Phase 10 (Plan 10-02 wiring) : scorecard figures PNG, human-verified Plan 10-01 Task 1"`. Ne PAS ajouter une seconde ligne commentée matplotlib (orphan duplicate).
    - **Ajouter** UNE NOUVELLE ligne juste après la ligne `# matplotlib>=3.7.0` modifiée : `# statsmodels==0.14.6  # Phase 10 (Plan 10-02 wiring) : MZ regression + DM HAC variance (Plan 10-03), human-verified Plan 10-01 Task 1`.
    - Justification : install effectif est Plan 10-02 (pillars 1+2 actual wiring). On documente la décision human-verified ici pour traçabilité. Plan 10-02 Task 1 fera l'in-place decomment sur les 2 lignes (matplotlib existante reformatée + statsmodels nouveau).

    Créer `tests/test_phase10_infra.py` couvrant TOUS les comportements listés en `<behavior>`. Utiliser uniquement données synthétiques deterministes (`pd.date_range`), pas d'I/O parquet, runtime < 5s total.
  </action>
  <verify>
    <automated>python -m pytest tests/test_phase10_infra.py -x -q && python -c "from pfc_shaping.validation.block_masks import ALL_BLOCKS; print(len(ALL_BLOCKS))" | grep -q "^5$" && python -c "from pfc_shaping.validation.scorecard import ABLATION_GRID, list_vintages_2024_2025, derive_forwards_from_epex_hist, FORWARDS_SOURCE_REAL, FORWARDS_SOURCE_FALLBACK_DIAGNOSTIC; assert len(ABLATION_GRID) == 4 and len(list_vintages_2024_2025()) == 24 and FORWARDS_SOURCE_FALLBACK_DIAGNOSTIC == 'fallback_diagnostic'; print('forwards_source constants OK')" && grep -c "^# matplotlib>=3.7.0" pfc_shaping/requirements.txt | tr -d ' ' | grep -q "^1$"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_phase10_infra.py` PASS (≥7 tests verts couvrant behaviors ci-dessus)
    - `python -c "from pfc_shaping.validation.block_masks import ALL_BLOCKS; print(len(ALL_BLOCKS))"` retourne `5`
    - `python -c "from pfc_shaping.validation.scorecard import ABLATION_GRID, list_vintages_2024_2025, derive_forwards_from_epex_hist"` import sans erreur ; `len(ABLATION_GRID)==4` ; `len(list_vintages_2024_2025())==24` ; `derive_forwards_from_epex_hist` est callable
    - `grep -c "block_overnight_weekday\|block_midday_weekday\|block_weekend_midday\|block_summer_solar_bowl\|block_winter_evening_peak" pfc_shaping/validation/block_masks.py` >= 5
    - `grep -c "bowl_off_floors_on\|bowl_on_floors_on\|bowl_off_floors_off\|bowl_on_floors_off" pfc_shaping/validation/scorecard.py` >= 4
    - `grep -c "^# matplotlib>=3.7.0" pfc_shaping/requirements.txt` == 1 (in-place replace, PAS de duplicate orphelin)
    - `grep -c "^# statsmodels==0.14.6" pfc_shaping/requirements.txt` == 1 (nouvelle ligne ajoutée)
    - Suite globale `pytest tests/` reste verte (≥272 passed, comme post-Phase 5)
  </acceptance_criteria>
  <done>Skeleton infra Phase 10 en place ; pillars maths déférés Plan 10-02/03/04 ; conftest validé couvre les 2 flags par préfixe ; pypi check pré-autorisé pour Plan 10-02 ; derive_forwards_from_epex_hist exposé skeleton pour Task 3.</done>
</task>

<task type="auto">
  <name>Task 3 — Bootstrap data caches (EPEX 15-min 2019-2025 + forwards historiques tranchés)</name>
  <files>data/epex_15min.parquet, data/forwards_history_phase10.parquet, .planning/phases/PFC-LT-10-backtest-par-bloc-vs-hfc-ompex/10-01-NOTES.md, .gitignore, pfc_shaping/validation/scorecard.py</files>
  <read_first>
    - pfc_shaping/data/ingest_energy_charts.py lines 80-150 (load_prices signature + retry logic + cache pattern)
    - pfc_shaping/data/ingest_forwards.py (existing forwards XLSX parser — vérifier signature pour audit accès chemin H:\)
    - pfc_shaping/config.yaml lignes 60-86 (paths.epex_parquet + forwards.eex_report_path documentent les chemins)
    - .planning/phases/PFC-LT-10-backtest-par-bloc-vs-hfc-ompex/10-RESEARCH.md §Common Pitfalls Pitfall 1 (recommend mesure empirique 2019-2023 EPEX) + §Runtime State Inventory (data/epex_15min.parquet existe mars 2025, re-bootstrap requis)
    - pfc_shaping/validation/scorecard.py (Task 2 a posé le skeleton de `derive_forwards_from_epex_hist` — Task 3 sub-step 3c implémente le body)
    - .gitignore (vérifier que data/*.parquet est ignoré ou ajouter)
  </read_first>
  <action>
    **Sub-step 3a (EPEX bootstrap) :**

    Run `python -c "from pfc_shaping.data.ingest_energy_charts import load_prices; df = load_prices('2019-01-01', '2025-12-31', bzn='CH'); df.to_parquet('data/epex_15min.parquet'); print(f'rows={len(df)}, range={df.index.min()}..{df.index.max()}')"`.
    Si energy-charts.info répond en erreur ou timeout : fallback documenté = utiliser le SMARD `pfc_shaping.data.ingest_smard.load_prices` avec même range + même schema, sinon ENTSO-E si SMARD échoue aussi. Documenter le fallback effectif utilisé dans `10-01-NOTES.md`.
    Output attendu : DataFrame ~245k rows (7 ans × 35040 quarters/yr), tz=UTC, freq=15min, colonne `price` €/MWh.

    Vérifier que `.gitignore` couvre `data/*.parquet` (ou ajouter `data/epex_15min.parquet` + `data/forwards_history_phase10.parquet` explicitement si la règle globale absente). Le fichier physique vit local-only, pas commit.

    **Sub-step 3a-bis [C2 REVIEWS] — FREEZE RECALIBRATION METHOD EX-ANTE (avant toute mesure empirique) :**

    Codex review (HIGH severity, REVIEWS row 2) : "Pitfall 1 = holiday/weekend ratio politiquement sensible si recalibré ad-hoc post-mesure". Mitigation = figer la règle de recalibration AVANT de regarder les chiffres.

    AVANT d'exécuter Sub-step 3b (mesure empirique), ÉCRIRE dans `10-01-NOTES.md` une section dédiée `## C2 REVIEWS — Recalibration Method (FROZEN EX-ANTE)` contenant :

    - **Formule unique de recalibration** (à appliquer mécaniquement si le ratio empirique sort de [0.65, 0.95]) :
      `IF empirical_ratio ∈ [0.65, 0.95]: threshold := (0.65, 0.95)  # research default confirmed`
      `ELSE: threshold := (max(0.50, P10_monthly_ratios), min(0.99, P90_monthly_ratios))  # data-driven envelope on 5y monthly bootstrap`
      où `P10_monthly_ratios` et `P90_monthly_ratios` = 10th et 90th percentiles des ratios MENSUELS calculés sur 2019-2023 (60 valeurs).
    - **Scope** : applicable UNIQUEMENT à la mesure 2019-2023 holidays_VS ; un nouveau scope nécessite un amendement plan signé par l'utilisateur (pas de "tweak" silencieux).
    - **Trigger condition** : déclenché ssi `empirical_ratio ∉ [0.65, 0.95]`. Aucun autre trigger autorisé (e.g. "le ratio paraît trop bas", "exclude PV hours feels right") — ces décisions deviennent un blocker Plan 10-01 nécessitant ré-ouverture user.
    - **Forbidden patterns** (énumérés explicitement pour éviter les regrets post-hoc) :
      - ❌ "On exclut les heures solaires bowl pour gonfler artificiellement le ratio numérateur" (Option 2 KYOS) — REJECT EX-ANTE
      - ❌ "On élargit à [0.40, 0.99] parce que ça arrange le verdict SC#1" — REJECT EX-ANTE
      - ❌ "On utilise un subset 2021-2023 plus court pour éviter le COVID 2020" — REJECT EX-ANTE
      - Seule la formule ci-dessus s'applique. Si elle donne un threshold "inconfortable", c'est un signal qu'il faut investiguer le bowl OU ré-ouvrir la phase, pas tweaker le seuil.
    - **Audit-trail** : l'application de la formule (cas par cas IF/ELSE) doit être écrite VERBATIM dans Sub-step 3b avec la valeur numérique de `empirical_ratio` ; toute déviation = blocker.

    Cette sous-étape est PUREMENT DOCUMENTAIRE — pas de code, pas de mesure. Le seul output est l'écriture de la section `## C2 REVIEWS — Recalibration Method (FROZEN EX-ANTE)` dans `10-01-NOTES.md` AVANT que Sub-step 3b ne touche aux données EPEX.

    **Sub-step 3b (Pillar 1 holiday/weekend ratio mesure empirique 2019-2023) — résout Pitfall 1 RESEARCH :**

    Mesurer sur l'EPEX bootstrappé : `ratio = mean(price | weekend ∪ holidays_VS) / mean(price | weekday hors holidays)` sur la fenêtre 2019-01-01..2023-12-31 strict (exclut 2024-2025 = période de test Phase 10 elle-même).
    Utiliser `holidays.country_holidays('CH', subdiv='VS', years=range(2019, 2024))`.

    Écrire le résultat dans `10-01-NOTES.md` (créer le fichier) avec format :
    ```
    # Plan 10-01 — Notes empiriques

    ## RESEARCH Pitfall 1 — Hildmann holiday/weekend ratio threshold (Pillar 1.2)

    **Measurement (EPEX CH 15-min, 2019-01-01..2023-12-31, holidays_VS) :**
    - mean(price | weekend ∪ holidays_VS) = X.XX €/MWh
    - mean(price | weekday hors holidays) = Y.YY €/MWh
    - ratio empirique = R.RRR

    **Threshold decision Pillar 1.2 (Plan 10-02 SC#1 gate) — APPLICATION MÉCANIQUE de la formule frozen Sub-step 3a-bis [C2 REVIEWS] :**

    [Application VERBATIM de la formule pré-gelée Sub-step 3a-bis :
       IF R ∈ [0.65, 0.95] → threshold := (0.65, 0.95)  # research default confirmé empiriquement
       ELSE → threshold := (max(0.50, P10_monthly_ratios), min(0.99, P90_monthly_ratios))  # data-driven envelope
              avec P10_monthly_ratios = <valeur numérique>, P90_monthly_ratios = <valeur numérique>
    AUCUNE déviation autorisée vs la formule (cf. Forbidden patterns Sub-step 3a-bis).
    Si le résultat de la formule est "inconfortable" → BLOCKER (re-open user), pas de tweak.]
    ```

    **Sub-step 3c (forwards-as-of-vintage path — résout RESEARCH Q2 + implémente body `derive_forwards_from_epex_hist`) :**

    Tester explicitement : `python -c "import os; p = 'H:\\\\Energy\\\\GeCom\\\\MARCHE & NEGOCE\\\\Prix\\\\EEX - ER\\\\Price_Report_EEX.xlsx'; print('H:\\\\ accessible:', os.path.exists(p))"`. Sur Mac Mini c'est attendu False.

    Si False (cas Mac Mini) : fallback = implémenter le **body** de `derive_forwards_from_epex_hist(epex_hist, vintage, horizon_days=3*365)` dans `pfc_shaping/validation/scorecard.py` (Task 2 a posé le skeleton + signature + NotImplementedError stub). Le body :
    - Filtrer `epex_hist.loc[epex_hist.index < vintage]` (no leakage, Pitfall 2).
    - Construire les fenêtres futures perçues : Cal years = [vintage.year + 1, vintage.year + 2, vintage.year + 3], Quarters = chaque Q sur les 3 années, Months = chaque M sur les 12 mois next.
    - Pour chaque key, calculer un proxy "would-be-forward" = mean(EPEX_hist[same_period_last_N_years]) où N = nombre d'années disponibles pré-vintage (typiquement 5-6 ans).
    - Retourner dict `{'YYYY': cal_proxy, 'YYYY-QN': quarter_proxy, 'YYYY-MM': month_proxy}` avec convention parser identique à `assembler.build(base_prices=...)` (year=YYYY, quarter=YYYY-QN, month=YYYY-MM).
    - Caveat documenté en docstring : c'est un proxy non-leaky pour benchmark Pillar 2 KPIs, PAS un vrai forward EEX — sera substitué par le snapshot XLSX réel si Phase 10B execution depuis FMV poste.

    Cacher le résultat agrégé pour les 24 vintages dans `data/forwards_history_phase10.parquet` (long format : `vintage, key, price, forwards_source`). **[C3 REVIEWS] La colonne `forwards_source` est OBLIGATOIRE** — valeur `FORWARDS_SOURCE_FALLBACK_DIAGNOSTIC = "fallback_diagnostic"` quand le proxy `derive_forwards_from_epex_hist` est utilisé (cas Mac Mini), `FORWARDS_SOURCE_REAL = "real_eex_xlsx"` quand le snapshot XLSX H:\\ est disponible. Itérer : `for vintage in list_vintages_2024_2025(): forwards = derive_forwards_from_epex_hist(epex_hist, vintage); for k, v in forwards.items(): records.append({'vintage': vintage, 'key': k, 'price': v, 'forwards_source': FORWARDS_SOURCE_FALLBACK_DIAGNOSTIC})`. Cette colonne sera lue par Plan 10-04 pour annoter "Diagnostic only — not gate-eligible" toute cellule Pillar 1/4 dont les forwards proviennent du fallback (cf. C3 REVIEWS row 3) ; SC#1 ne peut PAS être satisfait par un run fallback.

    Documenter dans `10-01-NOTES.md` :
    ```
    ## RESEARCH Q2 — Forwards historiques as-of vintage (Mac Mini)

    **Test accès H:\\\\... : <PASS|FAIL>**
    **Path retenu :** [snapshot local H:\\\\ OU fallback derive_forwards_from_epex_hist documenté ici]
    **Spot-check 1-2 quotes vs forwards XLSX (si accessible) :** [diff observé ou "non-testable"]
    **Convention parser keys :** identique assembler.build (year=YYYY, quarter=YYYY-QN, month=YYYY-MM)
    **derive_forwards_from_epex_hist body :** implémenté dans pfc_shaping/validation/scorecard.py (Task 3 sub-step 3c)

    ## C3 REVIEWS — forwards_source structured flag (NOT just a log line)
    **Marker convention :**
    - `FORWARDS_SOURCE_REAL = "real_eex_xlsx"` (constante module scorecard.py, exportée)
    - `FORWARDS_SOURCE_FALLBACK_DIAGNOSTIC = "fallback_diagnostic"` (idem)
    **Propagation :** colonne obligatoire `forwards_source` dans `data/forwards_history_phase10.parquet` ; valeur héritée à chaque appel de `build_one(..., forwards_asof=...)` et propagée dans le scorecard parquet output Plan 10-04 (cellules KPIs Pillar 1/4 portent la métadonnée).
    **Gate impact (Plan 10-04 SC#1 evaluator) :** toute cellule avec `forwards_source == "fallback_diagnostic"` est annotée `"Diagnostic only — not gate-eligible"` dans `10-VERIFICATION.md` (cf. Plan 10-04 Task 2/3) ; SC#1 Hildmann 4/4 PASS ne peut être satisfait QUE par un run avec `forwards_source == "real_eex_xlsx"` agrégé sur Config 4.
    **Path retenu Plan 10-01 (Mac Mini default) :** `fallback_diagnostic` — donc Plan 10-04 SC#1 final run nécessitera soit l'accès H:\\\\ (FMV poste), soit une override user explicit acceptant le statut diagnostic-only (auquel cas D-FLIP-1 reste BLOCKED).
    ```

    Si True (cas FMV poste exécutant Plan 10-01 plus tard) : path retenu = parsing direct XLSX via `ingest_forwards`, snapshot des forwards as-of chaque vintage_date dans `data/forwards_history_phase10.parquet`. `derive_forwards_from_epex_hist` reste implémenté comme fallback secondaire.

    Note : Cette task crée un fichier de cache. Si energy-charts.info est indisponible (network), task échoue explicitement — pas de placeholder vide.
  </action>
  <verify>
    <automated>python -c "import pandas as pd; df = pd.read_parquet('data/epex_15min.parquet'); assert df.index.tz is not None, 'tz-naive'; assert len(df) > 100000, f'too few rows: {len(df)}'; assert df.index.min().year <= 2019 and df.index.max().year >= 2024, f'range: {df.index.min()}..{df.index.max()}'; print('OK', len(df), df.index.min(), df.index.max())" && test -f .planning/phases/PFC-LT-10-backtest-par-bloc-vs-hfc-ompex/10-01-NOTES.md && grep -q "Pitfall 1" .planning/phases/PFC-LT-10-backtest-par-bloc-vs-hfc-ompex/10-01-NOTES.md && grep -q "Q2" .planning/phases/PFC-LT-10-backtest-par-bloc-vs-hfc-ompex/10-01-NOTES.md && grep -q "C2 REVIEWS.*Recalibration Method.*FROZEN EX-ANTE" .planning/phases/PFC-LT-10-backtest-par-bloc-vs-hfc-ompex/10-01-NOTES.md && grep -q "C3 REVIEWS.*forwards_source" .planning/phases/PFC-LT-10-backtest-par-bloc-vs-hfc-ompex/10-01-NOTES.md && python -c "from pfc_shaping.validation.scorecard import derive_forwards_from_epex_hist; import pandas as pd; df = pd.read_parquet('data/epex_15min.parquet'); vintage = pd.Timestamp('2024-06-28', tz='UTC'); fwds = derive_forwards_from_epex_hist(df['price'], vintage); assert len(fwds) > 0, 'derive returned empty'; print('derive OK', len(fwds), 'keys')" && python -c "import pandas as pd; df = pd.read_parquet('data/forwards_history_phase10.parquet'); assert 'forwards_source' in df.columns and df['forwards_source'].nunique() == 1, f'forwards_source missing or multi-valued: cols={df.columns.tolist()}'; print('forwards_source col OK, value =', df['forwards_source'].iloc[0])"</automated>
  </verify>
  <acceptance_criteria>
    - `data/epex_15min.parquet` existe, tz-aware UTC, ≥100k rows, range couvre au moins 2019-01..2024-12
    - `data/forwards_history_phase10.parquet` existe avec ≥24 vintages × ≥4 keys/vintage (Cal + Q1..Q4 minimum)
    - `10-01-NOTES.md` contient : (a) mesure du ratio holiday/weekend 2019-2023 avec valeurs numériques explicites, (b) décision threshold Pillar 1.2 = APPLICATION MÉCANIQUE de la formule frozen Sub-step 3a-bis (PAS de tweak), (c) test H:\ accès + path retenu pour forwards-as-of-vintage, (d) mention que derive_forwards_from_epex_hist body est implémenté Task 3 sub-step 3c
    - **[C2 REVIEWS] `10-01-NOTES.md` contient une section `## C2 REVIEWS — Recalibration Method (FROZEN EX-ANTE)`** avec : formule unique IF/ELSE, scope, trigger condition, forbidden patterns énumérés, audit-trail convention. Cette section est écrite AVANT Sub-step 3b (preuve via git diff order : commit séparé ou commit avec timestamp message)
    - **[C2 REVIEWS] L'application Sub-step 3b cite VERBATIM la formule frozen** (texte identique côté formule + valeurs numériques `empirical_ratio`, `P10_monthly_ratios`, `P90_monthly_ratios` insérées). Aucune mention de "Option 2 KYOS" ou autre déviation non-listée.
    - `derive_forwards_from_epex_hist(epex_hist, vintage)` retourne un dict non-vide avec keys au format `YYYY`, `YYYY-QN`, `YYYY-MM` (callable sans NotImplementedError)
    - **[C3 REVIEWS] `data/forwards_history_phase10.parquet` contient une colonne `forwards_source` avec exactement 1 valeur unique** (`fallback_diagnostic` sur Mac Mini OU `real_eex_xlsx` sur FMV poste) — assertable via `python -c "import pandas as pd; df = pd.read_parquet('data/forwards_history_phase10.parquet'); assert 'forwards_source' in df.columns and df['forwards_source'].nunique() == 1, f'colonne forwards_source absente ou multi-valeurs: {df.columns.tolist()}'"`.
    - **[C3 REVIEWS] `10-01-NOTES.md` section `## C3 REVIEWS — forwards_source structured flag`** présente avec : convention valeurs, propagation, gate impact (Plan 10-04 SC#1 not satisfiable par fallback).
    - `.gitignore` couvre `data/*.parquet` (pas de commit accidentel des caches)
    - `grep -c "Pitfall 1\|Q2\|Threshold decision\|derive_forwards_from_epex_hist\|C2 REVIEWS\|C3 REVIEWS\|forwards_source\|Recalibration Method (FROZEN EX-ANTE)" .planning/phases/PFC-LT-10-backtest-par-bloc-vs-hfc-ompex/10-01-NOTES.md` >= 8
  </acceptance_criteria>
  <done>Cache EPEX prêt pour Plan 10-02 ; threshold Hildmann Pillar 1.2 tranché empiriquement ; forwards-as-of-vintage path tranché ; derive_forwards_from_epex_hist body implémenté ; toutes les questions ouvertes du RESEARCH (Q1 implicit measure + Q2) sont closes.</done>
</task>

<task type="auto">
  <name>Task 4 — Mini-fix doc : ROADMAP + REQUIREMENTS + PROJECT.md (pivot D-A0-2)</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/PROJECT.md</files>
  <read_first>
    - .planning/ROADMAP.md (whole — Phase 10 entry lines 63-67 + 181-194 + Plans listing convention used in 5bis-A/B and 5)
    - .planning/REQUIREMENTS.md (whole — Backtest by Block section lines 50-60 + Traceability table line 114)
    - .planning/PROJECT.md (whole — Key Decisions table starting line 76)
    - .planning/phases/PFC-LT-10-backtest-par-bloc-vs-hfc-ompex/10-CONTEXT.md §D-A0-2 (autoritative reformulation spec)
  </read_first>
  <action>
    **Sub-step 4a — ROADMAP.md :**

    Localiser la section "Active (P0 — successeurs immédiats)" entrée `**Phase 10 (refondu): Backtest par bloc client vs HFC OMPEX 2024-2025**` (~ligne 64) :
    - Renommer en `**Phase 10: PFC FMV Quality Scorecard (5-pillar SOTA replication)**`
    - Réécrire goal : "Démontrer que la PFC FMV est SOTA par construction via 5 piliers (Hildmann structural + KYOS empirical accuracy + Christoffersen unconditional + DM vs 3 naive baselines + peer review SOTA literature). SC#1 unique gate = Hildmann 4/4 PASS sous Config 4 (bowl ON + floors negative-ready)."
    - Préciser : "Depends on: Phase 5bis-B + Phase 5 livrées. Argument interne décisif vs OMPEX (autonome, pas comparatif)."
    - Insérer juste après : nouvelle entrée `**Phase 10B: PFC FMV vs HFC OMPEX block-MAE benchmark (deferred, requires FMV poste H:\\)**` avec goal : "Backtest comparatif PFC FMV vs HFC OMPEX 2024-2025 par bloc. KPI : Δ MAE bloc ≤ -1.5 €/MWh sur ≥ 3 blocs. Réutilise l'infra Phase 10 (block_masks, scorecard harness, structural_tests). Exécutable depuis poste FMV avec accès au chemin `H:\\Energy\\GeCom\\MARCHE & NEGOCE\\Prix\\Analyse HFC\\HFC test\\ER -HFC_OMPEX_15min`. Requirements: BT-10B-01, BT-10B-02 (migrés de BT-03/BT-05)."

    Localiser la section "Phase Details > Phase 10" (~ligne 181) :
    - Mettre à jour title + goal en cohérence avec ci-dessus
    - Requirements line : `BT-01, BT-02, BT-04 (reformulé), BT-06..BT-10` (au lieu de `BT-01 → BT-05`)
    - Success Criteria : remplacer par SC#1 unique gate Hildmann 4/4 PASS + mention que Pillars 2-5 produisent du content informational
    - Plans : remplacer `TBD via /gsd:plan-phase 10` par la liste explicite :
      ```
      Plans:
      - [ ] 10-01-PLAN.md — Infrastructure + block_masks + scorecard skeleton + ROADMAP/REQ/PROJECT updates + EPEX/forwards bootstrap + threshold Hildmann empirique
      - [ ] 10-02-PLAN.md — Pillars 1 (Hildmann SC#1 gate) + 2 (Empirical accuracy MAE/RMSE/MZ)
      - [ ] 10-03-PLAN.md — Pillars 3 (Christoffersen unconditional) + 4 (DM vs 3 naive baselines)
      - [ ] 10-04-PLAN.md — Pillar 5 + final scorecard assembly + 96-build ablation grid run + 10-VERIFICATION.md + D-FLIP-1 flip (gated)
      ```

    Insérer juste après la Phase 10 Details : nouvelle section `### Phase 10B: PFC FMV vs HFC OMPEX block-MAE benchmark (deferred)` avec Goal/Depends/Requirements/Success Criteria/Plans=TBD, mirror de la Phase 10 entry mais comparatif et tagged "deferred — requires FMV poste H:\".

    **Sub-step 4b — REQUIREMENTS.md :**

    Section "Backtest by Block (priorité 1)" :
    - BT-01 : préservé inchangé
    - BT-02 : update wording avec les 5 blocs renommés D-A2-2 : `block_overnight_weekday (18-9 Lun-Ven), block_midday_weekday (10-15 Lun-Ven), block_weekend_midday (11-15 Sam-Dim), block_summer_solar_bowl (11-14 mai-août toutes DOW), block_winter_evening_peak (17-21 Lun-Ven nov-fév)`
    - BT-03 : DELETE de cette section, migrer vers nouvelle section Phase 10B (voir ci-dessous) avec ID `BT-10B-01`
    - BT-04 : REFORMULATE : `**BT-04** : Sortie : tableau markdown avec colonnes \`bloc | baseline | horizon | MAE_PFC | MAE_baseline | Δ MAE | DM-stat | p-value | better_than_baseline (Y/N)\`. Baselines maison = climatology, persistence Y-1, forwards-flat-no-shape (D-A4-1 CONTEXT). DM test avec Newey-West HAC lag=h-1 + HLN correction (D-A4-2 CONTEXT).`
    - BT-05 : DELETE de cette section, migrer vers nouvelle section Phase 10B avec ID `BT-10B-02`

    Ajouter 5 nouveaux items après BT-04 reformulé :
    - `**BT-06** (Pillar 1 — Structural Hildmann) : Sous Config 4 (bowl ON + floors negative-ready), les 4 tests structurels Hildmann PASS : arb-free <0.01 €/MWh sur chaque Cal/Q/M tradé, holiday/weekend ratio ∈ [threshold décidé Plan 10-01 NOTES], seasonal corr >0.85 entre PFC monthly signature et EPEX 2019-2023 monthly signature, continuity max-jump <2 €/MWh aux frontières mensuelles. SC#1 UNIQUE GATE Phase 10.`
    - `**BT-07** (Pillar 2 — Empirical KYOS) : Pour chaque cellule (bloc × horizon ∈ {M+1, M+3, M+6, Y+1, Y+2} × config), MAE, RMSE, bias absolu et Mincer-Zarnowitz régression \`realised = α + β·pred + ε\` avec test joint α=0 & β=1 (Wald via statsmodels.OLS.f_test) reportés dans le scorecard markdown.`
    - `**BT-08** (Pillar 3 — Christoffersen unconditional) : IC80 source = Uncertainty(n_boot=500, seed=42) bootstrap (cols p10/p90). Per (bloc × horizon) sur Config 4 : test binomial LR_uc Christoffersen 1998 (\`H0: observed_freq == nominal\`) reportée dans le scorecard. IC95 (p2.5/p97.5) NON supportée par Uncertainty API actuelle → déférée Phase 5ter (CONTEXT D-A3-3 amendé). Conditional coverage + reliability diagrams aussi déférés Phase 5ter.`
    - `**BT-09** (Pillar 4 — DM vs naive baselines) : 3 baselines maison (climatology = mean(EPEX hist pré-2024) par bloc, persistence Y-1 = mean(EPEX | bloc, vintage-1yr ± 15j), forwards-flat-no-shape = forward EEX Cal/Q/M as-of vintage sans shape). DM test avec loss differential MAE-cohérent et HAC Newey-West lag=h-1 + HLN small-sample correction. Reformulation de BT-04 historique.`
    - `**BT-10** (Pillar 5 — Peer review SOTA) : Table comparative 9×6 (9 features methodology × 6 références PFC FMV/KYOS/Volue/EULER/Benth-Koekebakker/Caldana) + gap analysis 3 paragraphes (~150 mots chacun : où FMV est SOTA / où il y a gap actionnable / où on innove) intégrés dans 10-VERIFICATION.md.`

    Ajouter à la fin de la section "v1 Requirements" une nouvelle sous-section :
    ```
    ### Backtest comparatif HFC OMPEX (Phase 10B, deferred — requires FMV poste)

    - [ ] **BT-10B-01** : Le harness compare PFC FMV vs HFC OMPEX (chemin `H:\Energy\GeCom\MARCHE & NEGOCE\Prix\Analyse HFC\HFC test\ER -HFC_OMPEX_15min`) bloc par bloc. Réutilise toute l'infra Phase 10 — c'est purement un ajout de baseline + un slot column dans la table comparative.
    - [ ] **BT-10B-02** : Cible de validation : Δ MAE PFC FMV vs HFC OMPEX ≤ -1.5 €/MWh sur au moins 3 blocs des 5 testés. Migration depuis BT-05 historique.
    ```

    Update Traceability table (ligne ~109) :
    - Remplacer ligne `BT-01 → BT-05 | Phase 10 (refondu) | parallèle 5bis/5/5ter` par DEUX lignes :
      - `BT-01, BT-02, BT-04, BT-06..BT-10 | Phase 10 | active`
      - `BT-10B-01, BT-10B-02 | Phase 10B | deferred (requires FMV poste H:\)`

    **Sub-step 4c — PROJECT.md :**

    Ajouter une nouvelle entrée dans la table "Key Decisions" (juste après la ligne 2026-05-20 D-FLIP-2) :
    ```
    | 2026-05-20 | **Phase 10 reframed as PFC FMV Quality Scorecard (5-pillar SOTA replication). OMPEX comparative benchmark deferred to new Phase 10B (requires FMV poste H:\\). SC#1 unique gate = Hildmann 4/4 PASS sous Config 4 (bowl ON + floors negative-ready). Pillars 2-5 produisent du content informational mais NE bloquent PAS la phase ni le flip D-FLIP-1.** (D-A0-1..D-A0-4) | User explicit pivot 2026-05-20 lors de /gsd:discuss-phase 10 : "oublier OMPEX, focus qualité absolue PFC FMV state-of-the-art, c'est notre mission... comment font les autres, qu'est-ce qui se fait sur le marché". EPFL principle "build it right by design, validate against principle, not just against competitor". |
    ```

    Update la ligne 2026-05-19 D-FLIP-1 (gate wording) : remplacer le wording actuel "Flip default ON gated par Phase 10 success (Δ MAE bloc ≤ -1.5 EUR/MWh vs HFC OMPEX 2024-2025)" par : "Flip default ON gated par Phase 10 SC#1 PASS : 4 tests structurels Hildmann (arb-free, holiday/weekend, seasonal corr, continuity) PASS aux seuils SOTA-grade sous Config 4 (bowl ON + floors negative-ready) sur l'agrégat des 24 vintages 2024-2025. Le benchmark comparatif Δ MAE vs HFC OMPEX est désormais déféré Phase 10B (post-pivot 2026-05-20)."

    Vérifier que le wording final mentionne explicitement : (a) gate = SC#1 Hildmann 4/4, (b) Config 4 = production target, (c) le D-FLIP-1 d'origine ROADMAP devient cohérent avec la nouvelle phase 10 scope.
  </action>
  <verify>
    <automated>cd /Users/julienbattaglia/Desktop/PFC && grep -q "PFC FMV Quality Scorecard" .planning/ROADMAP.md && grep -q "Phase 10B" .planning/ROADMAP.md && grep -q "BT-06\|BT-07\|BT-08\|BT-09\|BT-10" .planning/REQUIREMENTS.md && grep -q "BT-10B-01" .planning/REQUIREMENTS.md && grep -q "D-A0-1\|D-A0-2\|D-A0-3\|D-A0-4" .planning/PROJECT.md && grep -q "SC#1\|Hildmann 4/4\|Config 4" .planning/PROJECT.md && ! grep -E "^- \[ \] \*\*BT-03\*\*|^- \[ \] \*\*BT-05\*\*" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP.md : Phase 10 title contient "PFC FMV Quality Scorecard", Phase 10B (deferred) ajoutée, Plans listés explicitement (10-01..10-04)
    - REQUIREMENTS.md : BT-03 et BT-05 ne sont PLUS dans la section "Backtest by Block" (migrés vers BT-10B-01/BT-10B-02 sous nouvelle section), BT-04 reformulé DM vs 3 baselines, BT-06..BT-10 ajoutés
    - REQUIREMENTS.md BT-08 wording mentionne explicitement IC80 only + IC95 déférée Phase 5ter (cohérent avec uncertainty.py API actuelle)
    - REQUIREMENTS.md Traceability table reflète les deux groupes (Phase 10 active + Phase 10B deferred)
    - PROJECT.md : nouvelle entrée 2026-05-20 D-A0-* + update wording D-FLIP-1 (gate = SC#1 Hildmann 4/4 PASS Phase 10 Config 4)
    - `grep -c "Phase 10B" .planning/ROADMAP.md` >= 2 (Active + Phase Details sections)
    - Aucune référence orpheline `BT-03` ou `BT-05` (ancien) dans REQUIREMENTS.md (migrés OU mentionnés dans Traceability deferred only)
  </acceptance_criteria>
  <done>Documents planning alignés avec le pivot D-A0-2 ; chaque requirement ID BT-01..BT-10 trace vers la Phase 10 ; BT-10B-* explicitement deferred ; D-FLIP-1 gate cohérent avec SC#1 Hildmann 4/4 PASS ; BT-08 wording acte explicitement IC80-only / IC95-déféré.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| energy-charts.info HTTP→Python | Untrusted JSON parsing into `data/epex_15min.parquet` (network input, CC BY 4.0 licensed, fallback SMARD/ENTSO-E) |
| pypi.org installs (Plan 10-02) | Trust shift : statsmodels + matplotlib downloaded from PyPI — slopcheck unavailable, gated par human-verify Task 1 |
| H:\ network share (Phase 10B only) | Untrusted file path inaccessible Mac Mini ; Plan 10-01 Task 3 explicite le fallback |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | pip install statsmodels matplotlib | mitigate | `checkpoint:human-verify` Task 1 confirms PyPI canonical pre-install ; Plan 10-02 effective install only after Task 1 PASS |
| T-10-02 | Information disclosure | energy-charts.info JSON response | accept | Public CC BY 4.0 data, no PII, no auth ; `load_prices` retry logic + cache layer existent (5bis-A baseline integrity preserved) |
| T-10-03 | Denial of service | EPEX bootstrap 2019-2025 download | mitigate | Cache-aside pattern (`data/epex_15min.parquet`), retry already in `ingest_energy_charts`, fallback SMARD then ENTSO-E documented Task 3 |
| T-10-04 | Repudiation | 10-01-NOTES.md decisions (Pitfall 1 threshold, Q2 forwards path) | mitigate | Notes committed to git, traceable per decision in subsequent plans via reference |
| T-10-SC | Tampering | pip install (Plan 10-02 future) | mitigate | slopcheck unavailable → `[ASSUMED]` packages gated par blocking-human Task 1 of Plan 10-01 (this plan) |

Pas de threat model spécifique au scope Plan 10-01 au-delà de ces 5 — pas de cryptographie, pas d'authentification, pas de PII, pas de surface attaquable nouvelle dans le code core.
</threat_model>

<verification>
- `pytest tests/test_phase10_infra.py -x` : tous les tests verts (cible : ≥7 tests)
- `pytest tests/` : suite globale reste verte (≥272 passed comme post-Phase 5, +N nouveaux tests infra Phase 10)
- `python -c "from pfc_shaping.validation.block_masks import ALL_BLOCKS; from pfc_shaping.validation.scorecard import ABLATION_GRID, list_vintages_2024_2025, derive_forwards_from_epex_hist; assert len(ALL_BLOCKS)==5 and len(ABLATION_GRID)==4 and len(list_vintages_2024_2025())==24"` : OK
- `python -c "import pandas as pd; df = pd.read_parquet('data/epex_15min.parquet'); assert df.index.tz is not None and len(df) > 100000"` : OK
- `grep -E "Pitfall 1|Q2|Threshold decision|derive_forwards_from_epex_hist" .planning/phases/PFC-LT-10-backtest-par-bloc-vs-hfc-ompex/10-01-NOTES.md` : ≥4 hits
- `grep "PFC FMV Quality Scorecard" .planning/ROADMAP.md && grep "Phase 10B" .planning/ROADMAP.md` : 2 hits
- `grep -E "BT-06|BT-07|BT-08|BT-09|BT-10" .planning/REQUIREMENTS.md` : ≥5 hits ; `grep -E "BT-10B-01|BT-10B-02" .planning/REQUIREMENTS.md` : 2 hits
- `grep "D-A0-1" .planning/PROJECT.md && grep "Hildmann 4/4" .planning/PROJECT.md` : 2 hits
- `grep -c "^# matplotlib>=3.7.0" pfc_shaping/requirements.txt` == 1 (in-place replace, no orphan duplicate)
</verification>

<success_criteria>
1. **Infra livrée** : `pfc_shaping/validation/{block_masks.py, scorecard.py}` importables, `ALL_BLOCKS` (5 items) + `ABLATION_GRID` (4 items) + `list_vintages_2024_2025` (24 items) + `derive_forwards_from_epex_hist` (callable) exposés.
2. **Cache prêt** : `data/epex_15min.parquet` couvre ≥2019..2024 tz-aware UTC ; `data/forwards_history_phase10.parquet` couvre 24 vintages.
3. **Décisions empiriques** : `10-01-NOTES.md` documente le ratio holiday/weekend 2019-2023 mesuré + threshold Hildmann Pillar 1.2 décidé + forwards-as-of-vintage path retenu + body derive_forwards_from_epex_hist implémenté.
4. **Pivot doc acté** : ROADMAP/REQUIREMENTS/PROJECT.md reflètent D-A0-1..D-A0-4 (Phase 10 scorecard + Phase 10B deferred + BT-06..BT-10 créés + D-FLIP-1 wording cohérent + BT-08 IC80-only/IC95-déféré).
5. **Tests verts** : `tests/test_phase10_infra.py` ≥7 tests PASS, suite globale reste verte.
6. **Pré-autorisation pypi** : Task 1 human-verify completed (approved) → Plan 10-02 peut installer statsmodels + matplotlib sans nouveau checkpoint.
7. **requirements.txt hygiène** : 1 ligne `# matplotlib>=3.7.0` (in-place replace, pas de duplicate orphelin), 1 ligne `# statsmodels==0.14.6` (nouvelle).
</success_criteria>

<output>
Create `.planning/phases/PFC-LT-10-backtest-par-bloc-vs-hfc-ompex/10-01-SUMMARY.md` à la complétion :
- Liste des fichiers créés/modifiés (avec lignes ajoutées approximatives)
- Lien vers `10-01-NOTES.md` (décisions empiriques)
- Threshold Hildmann Pillar 1.2 retenu (pour consommation Plan 10-02)
- Forwards-as-of-vintage path retenu + signature `derive_forwards_from_epex_hist` (pour consommation Plan 10-02)
- Statut human-verify Task 1 (approved/rejected) + ce qui est pré-autorisé pour Plan 10-02
- Tests count delta (avant Phase 10 → après Plan 10-01)
</output>
