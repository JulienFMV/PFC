---
phase: 10
plan: 04
type: execute
wave: 4
depends_on: ["10-03"]
files_modified:
  - pfc_shaping/validation/scorecard.py
  - .planning/phases/10-pfc-fmv-quality-scorecard/10-VERIFICATION.md
  - .planning/phases/10-pfc-fmv-quality-scorecard/10-PREFLIGHT.md
  - .planning/phases/10-pfc-fmv-quality-scorecard/REPRODUCIBILITY-EXCEPTIONS.md
  - .planning/phases/10-pfc-fmv-quality-scorecard/figures/pillar1_seasonal_correlation_scatter.png
  - .planning/phases/10-pfc-fmv-quality-scorecard/figures/pillar2_mae_per_horizon_bar.png
  - .planning/phases/10-pfc-fmv-quality-scorecard/figures/pillar2_scatter_pred_vs_realised.png
  - .planning/phases/10-pfc-fmv-quality-scorecard/figures/pillar3_ic80_observed_vs_nominal.png
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - scripts/preflight_phase10.py
  - scripts/run_phase10_scorecard.py
  - tests/test_phase10_reproducibility.py
autonomous: false
requirements: [BT-06, BT-07, BT-08, BT-09, BT-10]

must_haves:
  truths:
    - "Le 96-build ablation grid (24 vintages × 4 configs) est exécutable end-to-end via scripts/run_phase10_scorecard.py et produit un dict pickle/parquet cache de PFC outputs"
    - "Pillar 5 peer review table comparative 9×6 est rédigée dans 10-VERIFICATION.md avec sources sourcées (reference_pfc_state_of_art.md + code PFC FMV)"
    - "10-VERIFICATION.md est lisible standalone par un trader senior FMV : executive summary 1 page + table of contents + 5 sections (1 par pillar) + figures matplotlib embedded inline"
    - "Verdict SC#1 final (Hildmann 4/4 sur Config 4 real-run agrégé) est documenté dans 10-VERIFICATION.md ET dans PROJECT.md (D-FLIP-1 entry flippée OU bloquée selon résultat)"
    - "Si SC#1 PASS : PROJECT.md entrée D-FLIP-1 update wording 'Flag PFC_LT_USE_SEASONAL_HOURLY_SHAPE flippé default ON le YYYY-MM-DD post-Phase 10 SC#1 PASS' ; si FAIL : entrée documente le blocker explicite + next steps"
    - "Tous les requirements BT-06..BT-10 ont du contenu réel dans 10-VERIFICATION.md (table, figure, ou text per pillar)"
    - "Per-vintage pred_baseline construction est documentée et testée : climatology=scalar(bloc), persistence_y1=scalar(bloc, vintage_date), forwards_flat=scalar(bloc, vintage_date, horizon) — broadcast vers Series alignée sur realised.index sub-window per horizon"
    - "D-A6-3 reproducibility contract (atol=1e-12 rtol=0) est asserted via tests/test_phase10_reproducibility.py — re-run subset 4 builds (Config 4 × 4 vintages Q1/Q2/Q3/Q4 2024) prouve assert_frame_equal exact tolerance"
    - "[C5 REVIEWS] Tolerance double-niveau : primaire atol=1e-12 (target), fallback atol=1e-10 autorisé UNIQUEMENT via entrée dans REPRODUCIBILITY-EXCEPTIONS.md nommant (pillar_key, machine, libversion, observed_delta, suspected_root_cause, signoff). Aucune relaxation silencieuse possible. Si fallback invoqué, le test print un INFO explicite ; sinon le test FAIL avec un message guidant la création d'une entrée légitime."
    - "[C6 REVIEWS] Le Preflight Task 0 a émis un verdict `go|no-go` dans 10-PREFLIGHT.md couvrant les 5 checks (data freshness, forwards path = real_eex_xlsx | fallback_diagnostic, disk space ≥5GB, runtime benchmark ≤2.5h, verdict file écrit). Full 96-build run BLOQUÉ si verdict == no-go sans résolution explicite."
  artifacts:
    - path: "pfc_shaping/validation/scorecard.py"
      provides: "Orchestrator complet : run_scorecard_full(epex_source, output_dir) qui itère 96-build grid, dispatch aux 4 pillar computers (run_scorecard_pillar_1 + compute_cell_kpis grid + compute_pillar3_coverage + compute_pillar4_dm), génère figures matplotlib, écrit KPIs DataFrame parquet ; helper _build_pred_baseline_for_vintage(baseline_name, bloc, vintage_date, horizon_label, epex_hist, forwards_asof) -> pd.Series"
      exports: ["run_scorecard_full", "render_figures", "render_markdown_report", "_build_pred_baseline_for_vintage"]
    - path: "scripts/run_phase10_scorecard.py"
      provides: "CLI runner pour le 96-build grid : `python scripts/run_phase10_scorecard.py --epex-source parquet --output-dir .planning/phases/.../` — Mac Mini local execution, ~1.5-2h compute budget total. Flag --reproducibility-subset 4 pour la subset re-run reproductibilité"
    - path: ".planning/phases/10-pfc-fmv-quality-scorecard/10-VERIFICATION.md"
      provides: "Scorecard final : executive summary + 5 pillar sections + verdict SC#1 + figures embedded inline + Pillar 5 peer review table 9×6 + gap analysis 3 paragraphes"
    - path: ".planning/phases/10-pfc-fmv-quality-scorecard/figures/*.png"
      provides: "4 figures matplotlib DPI publication-grade (≥150 dpi) : seasonal correlation scatter (Pillar 1) + MAE per horizon bar (Pillar 2) + scatter pred vs realised per bloc (Pillar 2) + IC80 observed vs nominal reliability-like overlay (Pillar 3)"
    - path: ".planning/PROJECT.md"
      provides: "D-FLIP-1 entry update : flip ON gated by SC#1 PASS (PROJECT.md timestamped 2026-MM-DD) OU blocker documenté si SC#1 FAIL"
    - path: "tests/test_phase10_reproducibility.py"
      provides: "D-A6-3 contract test : re-run subset 4 builds (Config 4 × 4 vintages Q1/Q2/Q3/Q4 2024) avec seed=42 + assert_frame_equal primary atol=1e-12 rtol=0 + [C5 REVIEWS] fallback documenté atol=1e-10 via REPRODUCIBILITY-EXCEPTIONS.md + helper _load_documented_exceptions"
    - path: ".planning/phases/10-pfc-fmv-quality-scorecard/REPRODUCIBILITY-EXCEPTIONS.md"
      provides: "[C5 REVIEWS] template documenté pour fallback tolerance 1e-10 — état initial vide ; chaque entrée = (pillar_key, machine, libversion, observed_delta, suspected_root_cause, signoff) ; parseable par _load_documented_exceptions"
    - path: "scripts/preflight_phase10.py"
      provides: "[C6 REVIEWS] CLI preflight Task 0 : 5 checks (data freshness, forwards path, disk space, runtime bench, verdict file) ; exit code 0 si verdict=go, sinon != 0 (BLOQUE Task 1)"
    - path: ".planning/phases/10-pfc-fmv-quality-scorecard/10-PREFLIGHT.md"
      provides: "[C6 REVIEWS] verdict file go/no-go avec 4 sections check + Final Verdict ; lu par Task 1 cost confirmation"
  key_links:
    - from: "scripts/run_phase10_scorecard.py"
      to: "pfc_shaping.validation.scorecard.run_scorecard_full"
      via: "CLI argparse → run_scorecard_full(epex_source='parquet', output_dir=Path('.planning/phases/10-pfc-fmv-quality-scorecard'))"
      pattern: "run_scorecard_full\\("
    - from: "10-VERIFICATION.md Pillar 1 section"
      to: ".planning/phases/10-pfc-fmv-quality-scorecard/figures/pillar1_seasonal_correlation_scatter.png"
      via: "Markdown embed `![Pillar 1 seasonal correlation](figures/pillar1_seasonal_correlation_scatter.png)`"
      pattern: "!\\[.*\\]\\(figures/"
    - from: ".planning/PROJECT.md D-FLIP-1 entry"
      to: "tests/test_phase10_hildmann.py SC#1 gate result"
      via: "Verdict SC#1 sur real-run (not mock) Config 4 agrégé sur 24 vintages → flip ON OR blocker documenté"
      pattern: "D-FLIP-1.*PASS|D-FLIP-1.*FAIL|D-FLIP-1.*blocked"
    - from: "10-VERIFICATION.md Pillar 5 table"
      to: "~/.claude/projects/-Users-julienbattaglia-Desktop-PFC/memory/reference_pfc_state_of_art.md"
      via: "Sources Pillar 5 cellules : KYOS, Volue, EULER, Benth-Koekebakker 2007, Caldana 2017, Hildmann 2013"
      pattern: "KYOS|Volue|EULER|Benth-Koekebakker|Caldana|Hildmann"
    - from: "tests/test_phase10_reproducibility.py"
      to: "pfc_shaping.validation.scorecard.run_scorecard_full"
      via: "Run twice (vintages_limit=4, config 4 only), assert assert_frame_equal(check_exact=False, atol=1e-12, rtol=0) on KPIs DataFrames"
      pattern: "assert_frame_equal.*atol=1e-12.*rtol=0"
---

<objective>
Plan 10-04 (wave 4) — Assembly final du scorecard Phase 10 : exécuter le 96-build ablation grid end-to-end (24 vintages × 4 configs) sur Mac Mini real-run, générer les 4 figures matplotlib, rédiger Pillar 5 (peer review SOTA literature), écrire `10-VERIFICATION.md` lisible standalone par un trader senior FMV, asserter le contrat D-A6-3 reproducibility (re-run subset ≥4 builds = atol=1e-12 rtol=0), et flipper D-FLIP-1 (gated by SC#1 Hildmann 4/4 PASS).

Purpose : Cristalliser l'argument SOTA-by-construction de la PFC FMV. C'est le livrable final de la Phase 10 — le document qui sera référencé en interne FMV pour valider que la PFC est state-of-the-art. SC#1 réel (vs mock CI Plan 10-02) tranche le flip D-FLIP-1 default OFF → ON.

**[REVIEWS iter 1 — C3/C5/C6 surgical edits]** :
- **C6 (HIGH risk reducer, MEDIUM severity)** — **NOUVELLE Task 0 Preflight** insérée en tête du plan AVANT Task 1 (cost human-verify) : 5 checks go/no-go (data freshness ≤14j, forwards_source = real_eex_xlsx ou explicit fallback override, disk space ≥5GB, runtime bench micro-run extrapolation ≤2.5h hard cap, verdict file `10-PREFLIGHT.md`). Verdict no-go BLOQUE le full run.
- **C3 (MEDIUM)** — Task 2 `render_markdown_report` propage la colonne `forwards_source` dans les KPIs parquets Pillar 2/4 + ajoute une **gate-eligibility banner** en tête de Pillar 1 dans `10-VERIFICATION.md` : `"✓ Gate-eligible run"` si tous forwards real, sinon `"⚠ Diagnostic only — not gate-eligible"`. SC#1 ne peut PAS être satisfait par des cellules fallback (cohérent avec Plan 10-01 marker).
- **C5 (MEDIUM)** — Task 5 reproducibility tolerance **double-niveau** : primaire `atol=1e-12 rtol=0` ; fallback `atol=1e-10` autorisé UNIQUEMENT via entrée dans `REPRODUCIBILITY-EXCEPTIONS.md` (template livré, état initial vide) nommant (pillar_key, machine, libversion, observed_delta, suspected_root_cause, signoff). Aucune relaxation silencieuse — le test FAIL avec message explicit si 1e-12 échoue sans entrée correspondante.

Output :
- `pfc_shaping/validation/scorecard.py` étendu avec `run_scorecard_full` + helpers figures + helper markdown report + helper `_build_pred_baseline_for_vintage` (warning #8 fix)
- `scripts/preflight_phase10.py` (**[C6 REVIEWS] NEW Task 0 preflight**) + `10-PREFLIGHT.md` verdict file
- `scripts/run_phase10_scorecard.py` (CLI runner Mac Mini local) avec flag `--reproducibility-subset`
- `10-VERIFICATION.md` (executive summary + 5 sections pillars + verdict SC#1 + figures embedded + **[C3 REVIEWS] gate-eligibility banner + forwards_source annotations**)
- 4 figures PNG publication-grade dans `figures/`
- `tests/test_phase10_reproducibility.py` (D-A6-3 contract assertion, blocker #3 fix, **[C5 REVIEWS] double-niveau tolerance**)
- `REPRODUCIBILITY-EXCEPTIONS.md` template (**[C5 REVIEWS] fallback 1e-10 audit-trail**)
- `PROJECT.md` D-FLIP-1 entry flip ON OR blocker documenté

**Mode** : autonomous=false car 2 checkpoint:human-verify (Task 1 cost-confirmation avant 96-build real-run + Task 4 final review du scorecard avant commit D-FLIP-1).
</objective>

<execution_context>
@/Users/julienbattaglia/Desktop/PFC/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julienbattaglia/Desktop/PFC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-pfc-fmv-quality-scorecard/10-CONTEXT.md
@.planning/phases/10-pfc-fmv-quality-scorecard/10-RESEARCH.md
@.planning/phases/10-pfc-fmv-quality-scorecard/10-01-NOTES.md
@.planning/phases/10-pfc-fmv-quality-scorecard/10-01-SUMMARY.md
@.planning/phases/10-pfc-fmv-quality-scorecard/10-02-SUMMARY.md
@.planning/phases/10-pfc-fmv-quality-scorecard/10-03-SUMMARY.md
@pfc_shaping/validation/block_masks.py
@pfc_shaping/validation/scorecard.py
@pfc_shaping/validation/structural_tests.py
@pfc_shaping/validation/christoffersen.py
@pfc_shaping/validation/dm_test.py

<interfaces>
<!-- Tous les primitives Phases 10-01..10-03 sont disponibles. -->

From pfc_shaping/validation/scorecard.py (Plans 10-01..10-03 cumul) :
```python
@dataclass(frozen=True) class AblationConfig: ...  # 4 configs
ABLATION_GRID: list[AblationConfig]  # 4 items, index 3 = bowl_on_floors_off = Config 4 production target
def list_vintages_2024_2025() -> list[pd.Timestamp]: ...  # 24 items
def build_one(config, vintage, epex_hist, forwards_asof, with_uncertainty=False) -> pd.DataFrame: ...
def derive_forwards_from_epex_hist(epex_hist, vintage, horizon_days=3*365) -> dict: ...
def run_scorecard_pillar_1(config_name: str, cache_dir, epex_source: str) -> dict[str, TestResult]: ...
def mz_test(realised, predicted) -> dict: ...
def compute_cell_kpis(pred, realised, bloc, horizon_label) -> dict: ...
def compute_pillar3_coverage(pfc_with_ic, realised, bloc, ic_level=0.80) -> dict: ...
    # ic_level=0.95 raises ValueError (blocker #4 explicit defer Phase 5ter)
def compute_pillar4_dm(pred_pfc, pred_baseline, realised, bloc, h_months) -> dict: ...
```

From pfc_shaping/validation/structural_tests.py + christoffersen.py + dm_test.py :
- test_arb_free, test_holiday_weekend, test_seasonal_profile, test_continuity, TestResult, HOLIDAY_WEEKEND_RANGE
- lr_unconditional_coverage
- diebold_mariano, baseline_climatology, baseline_persistence_y1, baseline_forwards_flat

From data/ caches (Plan 10-01 Task 3) :
- data/epex_15min.parquet : EPEX hist 15-min UTC 2019-2025
- data/forwards_history_phase10.parquet : 24 vintages × keys forwards as-of

From RESEARCH §Pattern 5 compute budget mitigation Pitfall 7 :
- Pillar 3 (with_uncertainty=True) seulement sur Config 4 (24 builds × ~90-120s = 36-50 min)
- Pillars 1+2+4 (with_uncertainty=False) sur les 4 configs (96 builds × ~25-40s = 40-64 min)
- Total estimé : 1.5-2h Mac Mini (vs 4h sans la mitigation)

From CONTEXT D-A6-3 :
- Reproducibility contract : `assert_frame_equal(scorecard_kpis_run1, scorecard_kpis_run2, check_exact=False, atol=1e-12, rtol=0)`
- Convention 5bis-A/B/5 tolerance préservée
- Random seed=42 (Uncertainty bootstrap deterministic)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0 — [C6 REVIEWS] Preflight go/no-go check (data freshness + forwards path + disk space + runtime bench + verdict file) — BLOCKS Task 1 si verdict != go</name>
  <files>scripts/preflight_phase10.py, .planning/phases/10-pfc-fmv-quality-scorecard/10-PREFLIGHT.md</files>
  <read_first>
    - .planning/phases/10-pfc-fmv-quality-scorecard/10-REVIEWS.md (codex row 6 — pipeline lourd 96 builds, mitigation = preflight unique avant full run)
    - .planning/phases/10-pfc-fmv-quality-scorecard/10-01-NOTES.md (Q2 forwards path + C3 forwards_source decision : si `fallback_diagnostic`, le preflight DOIT le surface explicitement)
    - data/epex_15min.parquet (lecture seule, vérifier mtime + range)
    - data/forwards_history_phase10.parquet (lecture seule, vérifier colonne forwards_source + valeur)
    - pfc_shaping/validation/scorecard.py (FORWARDS_SOURCE_REAL, FORWARDS_SOURCE_FALLBACK_DIAGNOSTIC constants + build_one signature pour le runtime bench)
    - pfc_shaping/validation/block_masks.py (ALL_BLOCKS pour le micro-bench)
  </read_first>
  <action>
    Créer `scripts/preflight_phase10.py` (CLI Python ~120 lignes) qui exécute les 5 checks suivants dans cet ordre et écrit un verdict file `.planning/phases/10-pfc-fmv-quality-scorecard/10-PREFLIGHT.md`. Si verdict != `go`, exit code != 0 → BLOCKS Task 1 (cost human-verify ne peut pas s'exécuter sans verdict `go` ou override user explicit dans Task 1).

    **Check 1 — Data freshness** :
    - `data/epex_15min.parquet` : mtime ≤ 14 jours OU override `--allow-stale-data` (passé en argv) → `os.path.getmtime` vs `time.time()`.
    - `data/forwards_history_phase10.parquet` : idem (mtime ≤ 14 jours OU override).
    - Si l'un manque : `verdict = no-go (data missing)`.
    - Si stale ET pas d'override : `verdict = no-go (data stale, run /gsd:execute-plan 10-01 Task 3 to refresh OR pass --allow-stale-data)`.

    **Check 2 — Forwards path** :
    - Lire `data/forwards_history_phase10.parquet`, vérifier `'forwards_source' in df.columns` et `df['forwards_source'].nunique() == 1`.
    - Si valeur unique == `"real_eex_xlsx"` → `forwards_path = REAL` (gate-eligible).
    - Si valeur unique == `"fallback_diagnostic"` → `forwards_path = FALLBACK_DIAGNOSTIC` ; décision branch :
       a. Si argv `--accept-fallback-diagnostic` passé → continue avec WARNING explicite "Plan 10-04 final run will be DIAGNOSTIC ONLY ; SC#1 cannot be satisfied" propagé dans 10-PREFLIGHT.md.
       b. Sinon → `verdict = no-go (forwards_source=fallback_diagnostic — SC#1 NOT gate-eligible, run from FMV poste OR pass --accept-fallback-diagnostic)`.
    - Si colonne `forwards_source` absente : `verdict = no-go (Plan 10-01 C3 contract violated — re-run Plan 10-01 Task 3)`.

    **Check 3 — Disk space** :
    - Calcul budget output : 96 PFC caches × ~25 MB ≈ 2.4 GB + 4 KPIs parquets × ~5 MB + 4 figures PNG × ~500 KB + 10-VERIFICATION.md ≈ **budget total ~2.5 GB**.
    - Mesurer free space sur le mount où vit `output_dir` (default `.planning/phases/10-pfc-fmv-quality-scorecard`) via `shutil.disk_usage(output_dir.parent)`.
    - Seuil dur : free space ≥ **5 GB** (2x marge). Si < 5 GB → `verdict = no-go (disk space {X} GB < 5 GB threshold)`.
    - Si 5-10 GB → WARNING dans 10-PREFLIGHT.md mais continue.

    **Check 4 — Runtime benchmark micro-run** :
    - Exécuter 1 micro-build : `build_one(ABLATION_GRID[3], vintage=last_business_day_of_month(2024, 6), epex_hist=df_epex, forwards_asof=fwds_v, with_uncertainty=False)` (Config 4, vintage Juin 2024, no uncertainty).
    - Mesurer wall time `t_micro` via `time.perf_counter()`.
    - Extrapolation full 96-build : `t_full_estimate = t_micro × 96 × 1.4` (le facteur 1.4 = overhead Pillar 3 Uncertainty(n_boot=500) sur Config 4 seul, per RESEARCH Pitfall 7).
    - Budget documenté : **2.5h (= 9000s) hard cap, 2h (= 7200s) soft target**.
    - Si `t_full_estimate > 9000s` → `verdict = no-go (runtime estimate {t_full_estimate/3600:.1f}h > 2.5h budget — investigate slowdown OR raise budget)`.
    - Si `7200s < t_full_estimate ≤ 9000s` → WARNING dans 10-PREFLIGHT.md mais continue.
    - Si `t_full_estimate ≤ 7200s` → OK.

    **Check 5 — Output verdict file** :
    - Écrire `.planning/phases/10-pfc-fmv-quality-scorecard/10-PREFLIGHT.md` avec format :
      ```
      # Plan 10-04 — Preflight Go/No-Go [C6 REVIEWS]
      **Generated** : <ISO timestamp UTC>
      **Script** : scripts/preflight_phase10.py
      **Verdict** : **<go|no-go>**

      ## Check 1 — Data freshness
      - epex_15min.parquet : mtime <ISO>, age <X> days, threshold ≤14 days → <PASS|FAIL>
      - forwards_history_phase10.parquet : mtime <ISO>, age <X> days → <PASS|FAIL>

      ## Check 2 — Forwards path
      - forwards_source unique value : <real_eex_xlsx|fallback_diagnostic>
      - Gate-eligible : <Y|N (DIAGNOSTIC ONLY)>
      - Decision : <continue|no-go|continue with --accept-fallback-diagnostic warning>

      ## Check 3 — Disk space
      - Free space at output_dir mount : <X> GB
      - Threshold ≥5 GB → <PASS|FAIL>
      - Budget output total : ~2.5 GB

      ## Check 4 — Runtime benchmark
      - Micro-run (Config 4, vintage 2024-06-28, no uncertainty) : <t_micro> sec
      - Extrapolation full 96-build : <t_full_estimate> sec = <t_full_estimate/3600:.2f> h
      - Budget hard cap : 2.5h ; soft target : 2h
      - Status : <PASS within target|PASS over soft target (WARNING)|FAIL over hard cap>

      ## Final Verdict
      - **<go|no-go>**
      - Next step si go : `/gsd:execute-plan 10-04 Task 1` (cost confirmation human-verify)
      - Next step si no-go : addresser le(s) check(s) FAIL avant re-run preflight
      ```
    - Exit code 0 si verdict == `go`, exit code 1 sinon.

    **Exécution Plan 10-04 Task 0** :
    `python scripts/preflight_phase10.py --output-dir .planning/phases/10-pfc-fmv-quality-scorecard` (sans aucun override = preflight strict).
    Si exit code != 0 → STOP, ne pas déclencher Task 1. Adresser le blocker (refresh data, libérer disque, ou accepter le fallback-diagnostic explicitement) puis re-run.
  </action>
  <verify>
    <automated>test -f scripts/preflight_phase10.py && python scripts/preflight_phase10.py --output-dir .planning/phases/10-pfc-fmv-quality-scorecard || (test -f .planning/phases/10-pfc-fmv-quality-scorecard/10-PREFLIGHT.md && grep -E "^\*\*Verdict\*\* : \*\*(go|no-go)\*\*" .planning/phases/10-pfc-fmv-quality-scorecard/10-PREFLIGHT.md) && test -f .planning/phases/10-pfc-fmv-quality-scorecard/10-PREFLIGHT.md && grep -q "Check 1 — Data freshness" .planning/phases/10-pfc-fmv-quality-scorecard/10-PREFLIGHT.md && grep -q "Check 2 — Forwards path" .planning/phases/10-pfc-fmv-quality-scorecard/10-PREFLIGHT.md && grep -q "Check 3 — Disk space" .planning/phases/10-pfc-fmv-quality-scorecard/10-PREFLIGHT.md && grep -q "Check 4 — Runtime benchmark" .planning/phases/10-pfc-fmv-quality-scorecard/10-PREFLIGHT.md && grep -q "Final Verdict" .planning/phases/10-pfc-fmv-quality-scorecard/10-PREFLIGHT.md</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/preflight_phase10.py` existe et est exécutable (shebang + main)
    - `.planning/phases/10-pfc-fmv-quality-scorecard/10-PREFLIGHT.md` existe avec les 5 sections (Check 1..4 + Final Verdict)
    - Le file contient exactement une ligne `**Verdict** : **go**` OR `**Verdict** : **no-go**` (pas d'ambiguïté)
    - Check 1 mentionne mtime + age + threshold 14 days
    - Check 2 mentionne forwards_source valeur unique + gate-eligible Y/N (lit la colonne créée par Plan 10-01 C3)
    - Check 3 mentionne free space + threshold 5 GB + budget 2.5 GB
    - Check 4 mentionne t_micro mesuré + extrapolation 96 builds + budget hard cap 2.5h / soft 2h
    - Si verdict == `go` → exit code 0 ET Task 1 peut s'exécuter
    - Si verdict == `no-go` → exit code != 0 ; Task 1 NE doit PAS être déclenchée sans résolution du blocker
    - **Override flags documentés dans la CLI** : `--allow-stale-data`, `--accept-fallback-diagnostic` (cas d'usage : user assume explicitement le statut diagnostic-only ; alors Plan 10-04 Task 6 D-FLIP-1 sera BLOCKED par construction puisque non gate-eligible)
  </acceptance_criteria>
  <done>Preflight go/no-go verdict cristallisé dans `10-PREFLIGHT.md` ; Task 1 (cost human-verify) débloquée si `go` ; full 96-build run BLOQUÉ si `no-go` jusqu'à résolution explicite. C6 REVIEWS (HIGH risk reducer) wired.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1 — Cost confirmation avant 96-build real-run (Mac Mini 1.5-2h compute)</name>
  <what-built>Le scorecard Plan 10-04 nécessite un 96-build ablation grid réel sur Mac Mini (vs mock CI réduits Plans 10-02/10-03). Compute budget estimé : 1.5-2h (RESEARCH §Pattern 5). Pas réversible facilement (intermediate caches éviteront re-run mais le 1st run dominé par I/O EPEX + bootstrap Uncertainty bootstrap). Avant de déclencher Task 2 (run_scorecard_full), confirmer disponibilité Mac Mini + accord opérateur. Note : la reproducibility test Task 5 ajoutera ~2-3 min (subset 4 builds Config 4) post-real-run.</what-built>
  <how-to-verify>
    1. **[C6 REVIEWS preflight gate]** Vérifier que `.planning/phases/10-pfc-fmv-quality-scorecard/10-PREFLIGHT.md` existe et contient `**Verdict** : **go**` (output Task 0). SI verdict == `no-go` → STOP, retourner à Task 0 et adresser le blocker. Acceptable d'avancer avec verdict `go` mais WARNING(s) (e.g. forwards_source=fallback_diagnostic accepté explicitement) — auquel cas Task 6 D-FLIP-1 sera mécaniquement BLOCKED.
    2. Vérifier que Mac Mini est dispo pour ~2h compute (pas de session active, pas de tâche concurrente lourde).
    3. Vérifier que `data/epex_15min.parquet` (~245k rows, 2019-2025) et `data/forwards_history_phase10.parquet` existent et sont récents : `ls -lh data/epex_15min.parquet data/forwards_history_phase10.parquet` (déjà checké par preflight Task 0, redondance acceptable).
    4. Optionnel : si Mac Mini indispo ou si on veut un dry-run préalable, on peut limiter à 1 vintage × 4 configs (4 builds, ~2-3 min) en passant `--vintages-limit 1` au CLI Task 2. Préciser dans la réponse "approved" si dry-run d'abord OR full run direct.
    5. Si OK pour full run, type "approved — full 96-build run" ou "approved — dry-run 1 vintage first" ou "postponed — re-trigger Task 1 plus tard".
  </how-to-verify>
  <resume-signal>Type "approved — full 96-build run", "approved — dry-run first", or "postponed". Si preflight verdict == no-go, le seul resume légitime est "preflight blocker resolved — re-run Task 0".</resume-signal>
</task>

<task type="auto">
  <name>Task 2 — run_scorecard_full orchestrator + CLI script + 96-build real-run + per-vintage pred_baseline construction</name>
  <files>pfc_shaping/validation/scorecard.py, scripts/run_phase10_scorecard.py</files>
  <read_first>
    - .planning/phases/10-pfc-fmv-quality-scorecard/10-RESEARCH.md §Pattern 5 (compute budget mitigation) + §Common Pitfalls Pitfall 5 (pool across vintages BEFORE MZ) + Pitfall 6 (seed determinism) + Pitfall 7 (Uncertainty only Config 4)
    - .planning/phases/10-pfc-fmv-quality-scorecard/10-01-NOTES.md (forwards_path retenu) + 10-02-SUMMARY.md (HOLIDAY_WEEKEND_RANGE + Config 3 smoke verdict) + 10-03-SUMMARY.md (compute budget mesuré + IC95 defer audit-trail)
    - pfc_shaping/validation/scorecard.py (toutes les primitives Plans 10-01..10-03 disponibles)
    - pfc_shaping/data/calendar_ch.py (build_calendar ou enrich_15min_index pour fournir calendar_df aux fit() chains)
    - pfc_shaping/validation/dm_test.py (baseline_climatology, baseline_persistence_y1, baseline_forwards_flat — signatures pour _build_pred_baseline_for_vintage helper)
  </read_first>
  <action>
    Étendre `pfc_shaping/validation/scorecard.py` :

    **Sub-step 2a — Helper `_build_pred_baseline_for_vintage` (warning #8 fix)** :

    Avant de wirer Pillar 4 dans run_scorecard_full, créer un helper explicite qui clarifie la construction des 3 baselines per vintage (la version actuelle de Plan 10-04 laissait cette étape implicite dans step 5d, ce qui était sous-spécifié) :

    ```
    def _build_pred_baseline_for_vintage(
        baseline_name: str,                 # 'climatology' | 'persistence_y1' | 'forwards_flat'
        bloc: BlockMask,
        vintage_date: pd.Timestamp,         # tz-aware UTC
        horizon_label: str,                 # 'M+1' | 'M+3' | 'M+6' | 'Y+1' | 'Y+2'
        epex_hist: pd.Series,               # full hist 2019-2025
        epex_realised: pd.Series,           # full realised 2024-2025 (subset of hist for vintage<2026)
        forwards_asof: dict,                # forwards as-of vintage_date (from forwards_history_phase10.parquet OR derive_forwards_from_epex_hist)
        realised_window: pd.DatetimeIndex,  # the realised.index sub-window for this (vintage, horizon) cell
    ) -> pd.Series:
        """Construct broadcast Series aligned on realised_window per baseline type.

        Convention per D-A4-1 CONTEXT :
        - climatology      : scalar(bloc) constant per bloc, broadcast over realised_window
                             scalar = baseline_climatology(epex_hist, bloc, years=(2019, 2023))
                             Independent of vintage_date and horizon_label.

        - persistence_y1   : scalar(bloc, vintage_date) constant per (bloc, vintage_date), broadcast over realised_window
                             scalar = baseline_persistence_y1(epex_realised, vintage_date, bloc, window_days=15)
                             Window = vintage_date - 1yr ± 15 days. NaN if empty window.

        - forwards_flat    : scalar(bloc, vintage_date, horizon_label) constant per (bloc, vintage_date, horizon_label), broadcast over realised_window
                             scalar = baseline_forwards_flat(forwards_asof, horizon_label, vintage_date)
                             Lookup priority Month > Quarter > Year per assembler convention.
        """
        from pfc_shaping.validation.dm_test import (
            baseline_climatology, baseline_persistence_y1, baseline_forwards_flat,
        )
        if baseline_name == "climatology":
            scalar = baseline_climatology(epex_hist, bloc, years=(2019, 2023))
        elif baseline_name == "persistence_y1":
            scalar = baseline_persistence_y1(epex_realised, vintage_date, bloc, window_days=15)
        elif baseline_name == "forwards_flat":
            scalar = baseline_forwards_flat(forwards_asof, horizon_label, vintage_date)
        else:
            raise ValueError(f"Unknown baseline_name: {baseline_name}")
        # Broadcast scalar → pd.Series aligned on realised_window
        return pd.Series(scalar, index=realised_window, name=f"baseline_{baseline_name}")
    ```

    **Sub-step 2b — Fonction principale `run_scorecard_full`** :

    `def run_scorecard_full(epex_source: str = "parquet", output_dir: Path = Path("."), vintages_limit: int | None = None, cache_intermediate: bool = True) -> dict` :
    1. Charger EPEX : si epex_source=="parquet" → `pd.read_parquet("data/epex_15min.parquet")["price"]` ; si "mock" → `_synth_epex_hist_for_mock()` (sanity option, pas le default real-run).
    2. Charger forwards : `pd.read_parquet("data/forwards_history_phase10.parquet")`.
    3. `vintages = list_vintages_2024_2025()` ; si `vintages_limit` → `vintages = vintages[:vintages_limit]`.
    4. Boucle 4 configs × len(vintages) builds :
       - Pour Config 4 (production target) : `with_uncertainty = True` (Pitfall 7 — Pillar 3 needs IC ; n_boot=500 seed=42).
       - Pour Configs 1/2/3 : `with_uncertainty = False`.
       - Call `pfc_v_c = build_one(config, vintage, epex_hist, forwards_asof_v, with_uncertainty=...)`.
       - Cache `output_dir/cache/pfc_{config.name}_{vintage_str}.parquet` si cache_intermediate=True (~25 MB par PFC × 96 = ~2.4 GB local, gitignored).
    5. Dispatch aux pillar computers :
       a. **Pillar 1** : `pillar1_results = run_scorecard_pillar_1("bowl_on_floors_off", cache_dir=output_dir/"cache", epex_source=epex_source)` → 4 TestResult.
       b. **Pillar 2** : pour chaque (config × bloc × horizon ∈ {M+1, M+3, M+6, Y+1, Y+2}), agréger pred sur les 24 vintages (Pitfall 5 pooling), `kpi_cell = compute_cell_kpis(pred_pooled, realised_aligned, bloc, horizon_label)`. Stocker en long-format DataFrame (rows = config × bloc × horizon × KPI keys).
       c. **Pillar 3** : pour Config 4 seul, pour chaque bloc, `cov_cell = compute_pillar3_coverage(pfc_with_ic_pooled, realised_aligned, bloc, ic_level=0.80)` (IC80 only — IC95 déférée Phase 5ter cf. Plan 10-03 notes.ic95_deferral). Stocker en long-format DataFrame.
       d. **Pillar 4** : pour chaque (config × bloc × baseline ∈ {climatology, persistence_y1, forwards_flat} × horizon), construire pred_baseline via `_build_pred_baseline_for_vintage(baseline, bloc, vintage_date, horizon_label, epex_hist, epex_realised, forwards_asof_v, realised_window)` (warning #8 fix, explicit per-vintage construction), puis `dm_cell = compute_pillar4_dm(pred_pfc, pred_baseline, realised, bloc, h_months_from_horizon)`. Stocker en long-format DataFrame.
    6. Sauver tous les DataFrames KPIs en parquet dans `output_dir/scorecard_kpis_{pillar}.parquet` (cols deterministic order, sort_index, dtype-stable pour D-A6-3 reproducibility).
    7. Return dict `{"pillar1": pillar1_results, "pillar2_df": ..., "pillar3_df": ..., "pillar4_df": ...}`.

    Helper `render_figures(scorecard_results: dict, output_dir: Path) -> list[Path]` :
    - `import matplotlib.pyplot as plt ; matplotlib.use("Agg")` (no GUI)
    - Figure 1 (Pillar 1) : `pillar1_seasonal_correlation_scatter.png` — scatter monthly_PFC_signature vs monthly_EPEX_hist_signature, annotate Pearson r, dpi=150.
    - Figure 2 (Pillar 2) : `pillar2_mae_per_horizon_bar.png` — bar chart MAE par horizon × bloc × config (faceted), dpi=150.
    - Figure 3 (Pillar 2) : `pillar2_scatter_pred_vs_realised.png` — scatter pred vs realised, 5 panels (1 par bloc), config 4, ligne y=x reference, dpi=150.
    - Figure 4 (Pillar 3) : `pillar3_ic80_observed_vs_nominal.png` — reliability-like : bar chart observed_freq vs nominal 0.20 par bloc, dpi=150.
    - Retourne `[path_f1, path_f2, path_f3, path_f4]`.

    Helper `render_markdown_report(scorecard_results: dict, output_dir: Path, holiday_weekend_range: tuple, sc1_verdict: dict) -> Path` :
    - Génère `output_dir/10-VERIFICATION.md` (overwrite si existant) avec la structure :
      ```
      # Phase 10 — PFC FMV Quality Scorecard (5-pillar SOTA replication)
      **Generated** : <ISO timestamp>
      **Config target** : bowl_on_floors_off (Config 4)
      **Vintages** : <N> (last business day of each month <range>)

      ## Executive Summary (1 page)
      [verdict SC#1 4/4 PASS/FAIL + 1-2 bullets par autre pillar]

      ## Table of Contents
      [auto-generated]

      ## Pillar 1 — Structural Quality (Hildmann 2013) — SC#1 UNIQUE GATE
      [table : test | observed | threshold | passed | forwards_source]
      [verdict global : 4/4 PASS or X/4 FAIL]
      [**[C3 REVIEWS] gate-eligibility banner** : si TOUTE cellule a `forwards_source == "real_eex_xlsx"` → "✓ Gate-eligible run" ; sinon → "**⚠ Diagnostic only — not gate-eligible (forwards derived from EPEX-history fallback)**" affiché en tête de section + en suffixe de chaque ligne fallback]
      [figure 1 embedded]

      ## Pillar 2 — Empirical Accuracy (KYOS-style)
      [table KPIs par (config × bloc × horizon × forwards_source) — MAE, RMSE, bias, mz_alpha, mz_beta, mz_p_value, low_power_flag, forwards_source]
      [**[C3 REVIEWS]** : cellules avec `forwards_source == "fallback_diagnostic"` taggées "(diagnostic)" en suffixe]
      [figures 2 + 3 embedded]

      ## Pillar 3 — Probabilistic (Christoffersen unconditional, Config 4 only, IC80 only)
      [table coverage par bloc — observed_freq, nominal_p=0.20, lr_stat, p_value, degenerate]
      [note: IC95 deferred to Phase 5ter (uncertainty.py:51-194 expose p10/p90 only, extension scope-creep)]
      [figure 4 embedded]

      ## Pillar 4 — Diebold-Mariano vs Naive Baselines
      [table DM par (config × bloc × baseline × horizon × forwards_source) — dm_stat, p_value, mae_pfc, mae_baseline, delta_mae, better_than_baseline_yn, forwards_source]
      [**[C3 REVIEWS]** : cellules avec `forwards_source == "fallback_diagnostic"` taggées "(diagnostic)" en suffixe ; le verdict comparatif `better_than_baseline` reste informational mais NON gate-eligible]

      ## Pillar 5 — Peer Review SOTA Literature (rédigé Task 3)
      [PLACEHOLDER — Task 3 le remplit]

      ## Annexes
      [holiday_weekend_range source 10-01-NOTES Pitfall 1 decision]
      [forwards-as-of-vintage path source 10-01-NOTES Q2 decision]
      [IC95 deferral audit-trail (Plan 10-03 notes.ic95_deferral)]
      [reproducibility contract atol=1e-12 rtol=0 — verified Task 5]
      [compute summary : N builds × M seconds = total time]
      ```

    Créer `scripts/run_phase10_scorecard.py` :
    - Shebang + docstring + `from __future__ import annotations`
    - `argparse` : `--epex-source {parquet,mock}` (default parquet), `--output-dir` (default `.planning/phases/10-pfc-fmv-quality-scorecard`), `--vintages-limit` (default None ; pour dry-run = 1), `--no-figures` (skip render_figures), `--no-report` (skip render_markdown_report), `--reproducibility-subset N` (run vintages_limit=N + config 4 only, pour la reproducibility test Task 5).
    - Main : appelle `run_scorecard_full` puis `render_figures` puis `render_markdown_report`. Print summary (total runtime, SC#1 verdict, KPIs locations).
    - Logging INFO niveau par défaut, redirect log file `output_dir/run_phase10.log`.

    **EXECUTION** : selon Task 1 decision :
    - "approved — full 96-build run" → `python scripts/run_phase10_scorecard.py --epex-source parquet --output-dir .planning/phases/10-pfc-fmv-quality-scorecard`. Background possible avec `run_in_background=True` (~1.5-2h).
    - "approved — dry-run first" → `python scripts/run_phase10_scorecard.py --epex-source parquet --vintages-limit 1` (~5 min, valide le pipeline end-to-end avant full run, puis re-run full).
    - Logger l'output complet dans `run_phase10.log`.

    Vérifier post-run : 96 fichiers cache dans `output_dir/cache/`, 4 parquet KPIs dans `output_dir/`, 4 figures PNG dans `output_dir/figures/`, `10-VERIFICATION.md` créé avec sections 1-4 remplies (Pillar 5 = placeholder pour Task 3).
  </action>
  <verify>
    <automated>cd /Users/julienbattaglia/Desktop/PFC && python -c "from pfc_shaping.validation.scorecard import run_scorecard_full, render_figures, render_markdown_report, _build_pred_baseline_for_vintage; print('imports OK')" && test -f scripts/run_phase10_scorecard.py && python scripts/run_phase10_scorecard.py --help 2>&1 | grep -q "vintages-limit\|epex-source\|reproducibility-subset" && test -f .planning/phases/10-pfc-fmv-quality-scorecard/10-VERIFICATION.md && test -d .planning/phases/10-pfc-fmv-quality-scorecard/figures && ls .planning/phases/10-pfc-fmv-quality-scorecard/figures/*.png | wc -l | tr -d ' ' | grep -qE "^[4-9]$"</automated>
  </verify>
  <acceptance_criteria>
    - `pfc_shaping/validation/scorecard.py` expose `run_scorecard_full`, `render_figures`, `render_markdown_report`, `_build_pred_baseline_for_vintage` (signatures correctes)
    - `_build_pred_baseline_for_vintage` accepte les 3 baseline_name ∈ {climatology, persistence_y1, forwards_flat} et retourne pd.Series broadcast sur realised_window (warning #8 fix)
    - `scripts/run_phase10_scorecard.py` existe, executable, `--help` affiche argparse options incl. `--reproducibility-subset`
    - Post-run : `.planning/phases/10-pfc-fmv-quality-scorecard/cache/` contient ≥4 PFC parquet files (≥1 vintage × 4 configs en dry-run, OR 96 fichiers en full run)
    - 4 figures PNG existent dans `figures/` (`pillar1_seasonal_correlation_scatter.png`, `pillar2_mae_per_horizon_bar.png`, `pillar2_scatter_pred_vs_realised.png`, `pillar3_ic80_observed_vs_nominal.png`)
    - `10-VERIFICATION.md` existe avec sections Pillar 1-4 remplies (Pillar 3 mentionne IC80 only + IC95 defer), Pillar 5 = placeholder à remplir Task 3
    - **[C3 REVIEWS] `10-VERIFICATION.md` contient une banner gate-eligibility en tête de Pillar 1** : soit `"✓ Gate-eligible run"` (si tous forwards_source==real_eex_xlsx), soit `"⚠ Diagnostic only — not gate-eligible (forwards derived from EPEX-history fallback)"` — assertable via `grep -E "Gate-eligible run|Diagnostic only — not gate-eligible" 10-VERIFICATION.md`
    - **[C3 REVIEWS] KPIs parquets Pillar 2 et Pillar 4 contiennent une colonne `forwards_source`** propagée depuis `data/forwards_history_phase10.parquet` (Plan 10-01 Task 3 sub-step 3c) — assertable via `python -c "import pandas as pd; assert 'forwards_source' in pd.read_parquet('output_dir/scorecard_kpis_pillar2.parquet').columns"`
    - KPIs parquets `scorecard_kpis_pillar{2,3,4}.parquet` existent dans `output_dir/`
    - `run_phase10.log` contient `SC#1 verdict: PASS/FAIL X/4` ligne explicite
    - **[C3 REVIEWS] `run_phase10.log` contient une ligne `SC#1 gate-eligible: <Y|N (forwards_source=fallback_diagnostic)>`** propagée depuis le preflight Task 0 + scorecard parquet output
  </acceptance_criteria>
  <done>96-build grid réalisé end-to-end (ou dry-run 4-build si déférre), tous les KPIs computés via _build_pred_baseline_for_vintage explicit per-vintage construction, figures matplotlib générées, scorecard skeleton (Pillars 1-4 sections remplies, IC80 only) écrit. Pillar 5 rédaction = Task 3. Reproducibility test = Task 5.</done>
</task>

<task type="auto">
  <name>Task 3 — Pillar 5 : peer review SOTA literature table + gap analysis</name>
  <files>.planning/phases/10-pfc-fmv-quality-scorecard/10-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/10-pfc-fmv-quality-scorecard/10-CONTEXT.md §D-A5-1..D-A5-4 (Pillar 5 spec : table 9×6, gap analysis 3 paragraphes)
    - ~/.claude/projects/-Users-julienbattaglia-Desktop-PFC/memory/reference_pfc_state_of_art.md (sources Pillar 5 : KYOS, Volue, EULER, Benth-Koekebakker 2007, Caldana 2017, Hildmann 2013)
    - pfc_shaping/lt/model/msfc_spline.py (pour colonne PFC FMV "level smoothness MSFC")
    - pfc_shaping/lt/model/shape_hourly.py (pour colonne PFC FMV "shape seasonal × type_jour × hour" + bowl deepening 5bis-B)
    - pfc_shaping/lt/model/water_value.py (delta-additif WV Phase 5 — innovation à mentionner)
    - pfc_shaping/calibration/cascading.py (spread-additif Peak Phase 5)
    - pfc_shaping/calibration/arbitrage_free.py (calibration jointe arb-free)
    - .planning/phases/10-pfc-fmv-quality-scorecard/10-VERIFICATION.md (placeholder Pillar 5 à remplacer)
  </read_first>
  <action>
    Remplacer le placeholder `## Pillar 5 — Peer Review SOTA Literature` dans `10-VERIFICATION.md` par le contenu suivant (~2 pages markdown) :

    **Table comparative 9×6** :
    Colonnes : `PFC FMV` | `KYOS KyCurve` | `Volue HPFC` | `EULER (Phinergy)` | `Benth-Koekebakker 2007` | `Caldana 2017`.
    Lignes (9 features methodology) :
    1. **Level smoothness (B)** — MSFC PCHIP linéaire Plan 5 sans floor : oui PFC FMV (msfc_spline.py linéaire, no log-prix per D-A1-1). KYOS : oui (proprietary max smoothness). Volue : oui. EULER : oui. Benth-Koekebakker 2007 : OUI (référence académique max smoothness). Caldana 2017 : oui (thin granularity adaptation).
    2. **Shape (f_H) seasonal × type_jour × hour** — oui PFC FMV (ShapeHourly 5bis-A view + 5bis-B bowl deepening). KYOS : oui (analogue-day). Volue : oui. EULER : oui. Benth-Koekebakker : non (pure max-smoothness, no shape decomposition). Caldana : partial.
    3. **Arbitrage-freeness (joint calibration)** — oui PFC FMV (ArbitrageFreeCalibrator.fit). Tous vendors : oui (table-stakes). Benth-Koekebakker : oui par construction. Caldana : oui.
    4. **Negative prices (4 floors removed)** — oui PFC FMV (Phase 5 ctor args defaults negative-ready). KYOS post-2022 : oui. Volue post-2023 : oui. EULER : partial. Benth-Koekebakker : oui (no floors by construction). Caldana : oui.
    5. **Probabilistic (IC80 bootstrap, IC95 deferred)** — partial PFC FMV (Uncertainty class n_boot=500 IC80 only, IC95 défère Phase 5ter). KYOS : oui (advanced regime-aware IC80/IC95). Volue : oui. EULER : partial. Benth-Koekebakker : non (deterministic). Caldana : partial.
    6. **Peak-offpeak (spread additif)** — oui PFC FMV (Phase 5 spread additif, peut être négatif). KYOS : oui. Volue : oui. EULER : oui (ratio-based legacy). Benth-Koekebakker : non. Caldana : oui.
    7. **Hourly granularity (15-min)** — oui PFC FMV (grille 15-min UTC). KYOS : oui (15-min ou hourly selon config). Volue : oui (15-min). EULER : hourly (60-min). Benth-Koekebakker : daily ou hourly. Caldana : oui (15-min innovation).
    8. **Multi-market (CH + DE + FR + AT + IT)** — partial PFC FMV (CH only en production, FR/AT/IT en HOLD Phase 3). KYOS : oui (tous marchés EU). Volue : oui. EULER : oui. Benth-Koekebakker : market-agnostic. Caldana : 1 marché par fit.
    9. **Delta-additif WV (sign-invariant)** — INNOVATION PFC FMV (Phase 5 D-A3-1, sign-invariant par construction). KYOS : non (multiplicatif legacy). Volue : non. EULER : non. Benth-Koekebakker : N/A (no hydro modulation). Caldana : N/A.

    **Format table markdown** : utiliser cells de format `oui ✓` / `non ✗` / `partial` + commentaire bref (≤ 15 mots) en italique sur la même cell.

    **Gap analysis** (3 paragraphes ~150 mots chacun) :

    Paragraph 1 — **Où PFC FMV est SOTA** : "PFC FMV est state-of-the-art sur 6 des 9 features : level smoothness MSFC (linéaire Benth-Koekebakker-aligned), arbitrage-freeness via calibration jointe, negative prices (4 floors retirés Phase 5), shape seasonal × type_jour × hour (Phase 5bis-A view 3D + Phase 5bis-B bowl deepening qui creuse le duck curve pour pricer correctement les profile deals GRD), hourly granularity 15-min (aligne avec EPEX intraday products), et delta-additif WV sign-invariant (innovation maison non observée dans la literature). Sur ces 6 dimensions, la PFC FMV est qualitativement au niveau des vendors propriétaires KYOS/Volue et techniquement supérieure à Benth-Koekebakker 2007 (qui n'a pas de shape decomposition) et à Caldana 2017 (qui est mono-marché)."

    Paragraph 2 — **Où il y a gap actionnable** : "3 dimensions présentent un gap actionnable : (a) probabilistic light only — Uncertainty class expose IC80 (p10/p90) seul, IC95 (p2.5/p97.5) déférée Phase 5ter (extension uncertainty.py:51-194 requise pour level= param) ; Christoffersen conditional + reliability diagrams sont aussi déférés Phase 5ter ; KYOS et Volue ont une calibration régime-aware (volatility clusters). (b) Pas de peer review des forwards inputs — la calibration EEX est consommée brute sans test de cohérence vs forwards alternatifs (ICE, Refinitiv) ; Phase 7 (gouvernance commodities) couvre cela. (c) Single-market CH non multi-zone — Phase 3 (FR/AT/IT activation) et Phase 4 (basis cross-border) sont en HOLD pour prioriser la qualité CH, mais structurellement les vendors couvrent les 5 marchés EU."

    Paragraph 3 — **Où on innove vs literature** : "PFC FMV introduit trois innovations méthodologiques non documentées dans la literature académique ni dans les vendor docs publics : (a) delta-additif WaterValueCorrection (Phase 5 D-A3-1) qui garantit la sign-invariance par construction du water-value adjustment — la littérature applique un multiplicateur f_wv qui inverse la sémantique en regime négatif. (b) ctor args defaults negative-ready convention (Phase 5 D-A2-1) — pattern explicit-floor-removal via 4 kwargs (enforce_positivity, enforce_m_factor_floor, enforce_floor, allow_negative_peak) qui rend le rollback opérateur traçable au callsite. (c) Master flag audit-trail INFO log only (Phase 5 D-A2-2) — pattern délibéré qui évite le silent-revert via env-var magique ; les vendors propriétaires utilisent typiquement un master switch implicite."

    **Sources** : ajouter à la fin du Pillar 5 une sous-section `### Sources` listant explicitement les 6 références (avec dates pour les académiques + version pour les vendors si trackable).

    Vérifier que le contenu Pillar 5 ne casse pas la structure markdown du reste de `10-VERIFICATION.md` (headers cohérents, table of contents toujours valide).
  </action>
  <verify>
    <automated>cd /Users/julienbattaglia/Desktop/PFC && test -f .planning/phases/10-pfc-fmv-quality-scorecard/10-VERIFICATION.md && grep -q "Pillar 5 — Peer Review SOTA" .planning/phases/10-pfc-fmv-quality-scorecard/10-VERIFICATION.md && grep -q "KYOS KyCurve\|Volue HPFC\|Benth-Koekebakker" .planning/phases/10-pfc-fmv-quality-scorecard/10-VERIFICATION.md && grep -c "^[0-9]\.\|level smoothness\|arbitrage-freeness\|negative prices\|delta-additif" .planning/phases/10-pfc-fmv-quality-scorecard/10-VERIFICATION.md | tr -d ' ' | grep -qE "^[5-9]$|^[1-9][0-9]$" && ! grep -q "PLACEHOLDER" .planning/phases/10-pfc-fmv-quality-scorecard/10-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `10-VERIFICATION.md` section "Pillar 5" contient table comparative 9×6 (9 lignes features × 6 colonnes vendors/literature) avec cellules sourcées
    - 3 paragraphes gap analysis présents (où SOTA / où gap actionnable / où on innove), chacun ~100-200 mots
    - Paragraph 2 mentionne explicitement IC95 deferred + uncertainty.py:51-194 reference (audit-trail blocker #4)
    - Sous-section Sources liste explicitement KYOS, Volue, EULER, Benth-Koekebakker 2007, Caldana 2017, Hildmann 2013
    - Aucune occurrence "PLACEHOLDER" dans le markdown (Task 2 a laissé un placeholder Pillar 5, Task 3 le remplace)
    - Table of Contents du markdown reste cohérente (header Pillar 5 = niveau ##)
    - `grep -E "delta-additif|ctor args negative-ready|master flag audit-trail" .planning/phases/10-pfc-fmv-quality-scorecard/10-VERIFICATION.md` retourne ≥3 hits (les 3 innovations PFC FMV documentées)
  </acceptance_criteria>
  <done>Pillar 5 rédigé, sourcé, et lisible standalone par un trader senior FMV ; scorecard final contient les 5 sections pillars complètes + executive summary + figures embedded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4 — Review final scorecard + decision D-FLIP-1 flip</name>
  <what-built>10-VERIFICATION.md est complet (Pillars 1-5 + executive summary + 4 figures embedded). Le verdict SC#1 (Hildmann 4/4 sur Config 4 real-run) est documenté dans l'executive summary. Avant de flipper D-FLIP-1 dans PROJECT.md (Task 6), un human review est requis pour : (a) confirmer que SC#1 PASS effectivement (vs faux positif mock CI Plan 10-02), (b) que les 5 piliers du scorecard sont lisibles standalone par un trader senior, (c) que les figures sont publication-grade.</what-built>
  <how-to-verify>
    1. Ouvrir `.planning/phases/10-pfc-fmv-quality-scorecard/10-VERIFICATION.md` dans un viewer markdown (ou IDE preview).
    2. Lire l'Executive Summary (1 page) : verdict SC#1 explicite + 1-2 bullets par autre pillar.
    3. Vérifier Pillar 1 : table 4 tests Hildmann avec observed/threshold/passed ; verdict global 4/4 PASS ou X/4 FAIL.
    4. Vérifier Pillars 2, 3, 4 : tables lisibles + figures embedded inline visibles (open figure files dans `figures/` pour confirmer rendu). Pillar 3 doit mentionner IC80 only + IC95 deferred.
    5. Vérifier Pillar 5 : table comparative + 3 paragraphes gap analysis.
    6. Confirmer que le document est utilisable comme argument interne FMV (clarté, completeness, no jargon non expliqué).
    7. **Decision D-FLIP-1** :
       - Si SC#1 PASS 4/4 → type "approved — flip D-FLIP-1 ON".
       - Si SC#1 FAIL (au moins 1 Hildmann test FAIL) → type "approved — block D-FLIP-1, document blocker in PROJECT.md".
       - Si scorecard nécessite revision contenu → type "needs revision: <description>".
  </how-to-verify>
  <resume-signal>Type "approved — flip D-FLIP-1 ON", "approved — block D-FLIP-1, document blocker", or "needs revision: <description>".</resume-signal>
</task>

<task type="auto">
  <name>Task 5 — D-A6-3 Reproducibility contract test (subset 4 builds re-run, atol=1e-12 rtol=0)</name>
  <files>tests/test_phase10_reproducibility.py</files>
  <read_first>
    - .planning/phases/10-pfc-fmv-quality-scorecard/10-CONTEXT.md §D-A6-3 (reproducibility contract spec : assert_frame_equal check_exact=False atol=1e-12 rtol=0)
    - pfc_shaping/validation/scorecard.py (run_scorecard_full signature + cache_dir convention)
    - tests/conftest.py (autouse env hygiene — fixture tmp_path_factory pour cache_dir isolation)
    - .planning/phases/05-msfc-log-prix-retire-silent-floors/05-CONTEXT.md (precedent tolerance contract convention)
  </read_first>
  <behavior>
    - Re-run subset 4 builds (Config 4 only × 4 vintages Q1/Q2/Q3/Q4 2024) avec seed=42 dans tmp_dir_1 puis tmp_dir_2 séparés
    - **[C5 REVIEWS — tolerance double-niveau]** Cible primaire : KPIs DataFrames (pillar2_df, pillar3_df, pillar4_df) bit-numerical-identical à `atol=1e-12 rtol=0` ; Pillar 1 TestResult.observed (scalars float) idem.
    - **[C5 REVIEWS] Fallback documenté `atol=1e-10`** : si la tolerance 1e-12 échoue sur un tuple (Config, Pillar, machine, library-version) spécifique pour cause de non-determinism chaîne pandas/statsmodels/numpy, le test peut relâcher à `atol=1e-10` UNIQUEMENT si une entrée correspondante existe dans `.planning/phases/10-pfc-fmv-quality-scorecard/REPRODUCIBILITY-EXCEPTIONS.md` nommant : (tuple), observed delta, suspected root cause, signoff. Aucune relaxation silencieuse n'est autorisée.
    - **[C5 REVIEWS] Forbidden** : `pytest.approx`, `np.isclose(default tolerance)`, ou autre relaxation par défaut (silencieuse). Le test DOIT échouer si 1e-12 échoue ET aucune exception n'est documentée.
    - Test runtime < 3 minutes (4 builds × ~25s = ~2 min + comparison overhead)
    - Si fail (1e-12 ET pas d'exception documentée) : message explicit "Reproducibility contract violated : <DataFrame name> differ at atol=1e-12 ; max abs diff = X ; document in REPRODUCIBILITY-EXCEPTIONS.md OR fix determinism" pour debug
    - Si pass via fallback 1e-10 : message INFO "Reproducibility relaxed to 1e-10 per REPRODUCIBILITY-EXCEPTIONS.md entry <id> (tuple={...})"
  </behavior>
  <action>
    Créer `tests/test_phase10_reproducibility.py` :

    ```
    """D-A6-3 Reproducibility contract test (Plan 10-04 Task 5).

    Per CONTEXT.md §D-A6-3 : assert_frame_equal(scorecard_kpis_run1, scorecard_kpis_run2,
    check_exact=False, atol=1e-12, rtol=0) — convention 5bis-A/B/5 tolerance préservée.

    Subset = 4 builds (Config 4 × 4 vintages Q1/Q2/Q3/Q4 2024) — full 96 grid réécutée Plan 10-04 Task 2.
    Justification subset : 4 builds × ~25s = ~2min total compute vs 96 builds × 1.5-2h. Subset suffit
    pour prouver determinism (même chaine seed=42 + np.random.default_rng / np.random.RandomState).
    """

    from __future__ import annotations

    import pandas as pd
    import pytest

    from pfc_shaping.validation.scorecard import ABLATION_GRID, run_scorecard_full


    REPRODUCIBILITY_TOLERANCE_PRIMARY = {"check_exact": False, "atol": 1e-12, "rtol": 0}
    REPRODUCIBILITY_TOLERANCE_FALLBACK = {"check_exact": False, "atol": 1e-10, "rtol": 0}  # [C5 REVIEWS]
    EXCEPTIONS_FILE = ".planning/phases/10-pfc-fmv-quality-scorecard/REPRODUCIBILITY-EXCEPTIONS.md"


    def _load_documented_exceptions(exceptions_path):
        """[C5 REVIEWS] Parse REPRODUCIBILITY-EXCEPTIONS.md, return set of authorized pillar_keys for 1e-10 fallback.

        Each row of the markdown table must contain 6 columns:
            pillar_key | machine | libversion | observed_delta | suspected_root_cause | signoff

        Returns set of pillar_key strings for which the fallback is documented and authorized.
        """
        from pathlib import Path
        p = Path(exceptions_path)
        if not p.exists():
            return set()
        text = p.read_text(encoding="utf-8")
        documented = set()
        for line in text.splitlines():
            if line.startswith("|") and any(k in line for k in ("pillar1", "pillar2_df", "pillar3_df", "pillar4_df")):
                cells = [c.strip() for c in line.split("|") if c.strip()]
                if len(cells) >= 6:
                    documented.add(cells[0])
        return documented


    @pytest.fixture(scope="module")
    def epex_parquet_available():
        """Skip si data/epex_15min.parquet absent (Plan 10-01 Task 3 required)."""
        from pathlib import Path
        if not Path("data/epex_15min.parquet").exists():
            pytest.skip("data/epex_15min.parquet not bootstrapped (Plan 10-01 Task 3 required)")
        return True


    @pytest.mark.timeout(300)
    def test_reproducibility_subset_4_builds_config_4(epex_parquet_available, tmp_path_factory):
        """D-A6-3 contract : primary atol=1e-12 rtol=0 ; [C5 REVIEWS] fallback atol=1e-10 ONLY via documented entry."""
        out_dir_1 = tmp_path_factory.mktemp("phase10_repro_run1")
        result_1 = run_scorecard_full(
            epex_source="parquet", output_dir=out_dir_1, vintages_limit=4, cache_intermediate=True,
        )
        out_dir_2 = tmp_path_factory.mktemp("phase10_repro_run2")
        result_2 = run_scorecard_full(
            epex_source="parquet", output_dir=out_dir_2, vintages_limit=4, cache_intermediate=True,
        )
        documented_exceptions = _load_documented_exceptions(EXCEPTIONS_FILE)

        # Pillar 1 : TestResult.observed scalars identical
        for key in ["arb_free", "holiday_weekend", "seasonal_profile", "continuity"]:
            obs_1 = result_1["pillar1"][key].observed
            obs_2 = result_2["pillar1"][key].observed
            diff = abs(obs_1 - obs_2)
            pillar_key = f"pillar1.{key}"
            if diff >= 1e-12:
                if pillar_key in documented_exceptions and diff < 1e-10:
                    print(f"INFO [C5 REVIEWS] reproducibility relaxed for {pillar_key}: diff={diff} < 1e-10 per {EXCEPTIONS_FILE}")
                else:
                    raise AssertionError(
                        f"D-A6-3 violation Pillar 1 {key}: obs_1={obs_1}, obs_2={obs_2}, diff={diff}. "
                        f"Primary atol=1e-12 failed. To allow 1e-10 fallback, add entry to {EXCEPTIONS_FILE} "
                        f"with pillar_key='{pillar_key}', machine, libversion, observed delta, suspected root cause, signoff."
                    )

        # Pillar 2/3/4 : KPIs DataFrames identical
        for pillar_key in ["pillar2_df", "pillar3_df", "pillar4_df"]:
            df_1 = result_1[pillar_key].sort_index(axis=0).sort_index(axis=1)
            df_2 = result_2[pillar_key].sort_index(axis=0).sort_index(axis=1)
            try:
                pd.testing.assert_frame_equal(df_1, df_2, **REPRODUCIBILITY_TOLERANCE_PRIMARY)
            except AssertionError as primary_err:
                numeric_cols = df_1.select_dtypes(include="number").columns
                max_diffs = {c: float((df_1[c] - df_2[c]).abs().max()) for c in numeric_cols}
                max_overall = max(max_diffs.values()) if max_diffs else 0.0
                if pillar_key in documented_exceptions and max_overall < 1e-10:
                    try:
                        pd.testing.assert_frame_equal(df_1, df_2, **REPRODUCIBILITY_TOLERANCE_FALLBACK)
                        print(f"INFO [C5 REVIEWS] reproducibility relaxed for {pillar_key}: max_diff={max_overall} < 1e-10 per {EXCEPTIONS_FILE}")
                    except AssertionError as fallback_err:
                        raise AssertionError(
                            f"D-A6-3 violation {pillar_key}: BOTH 1e-12 AND 1e-10 fallback failed. "
                            f"Max abs diffs: {max_diffs}. Fix determinism — fallback exhausted."
                        ) from fallback_err
                else:
                    raise AssertionError(
                        f"D-A6-3 violation {pillar_key}: assert_frame_equal failed at primary atol=1e-12.\n"
                        f"Max abs diffs per column: {max_diffs}\n"
                        f"To allow 1e-10 fallback, add entry to {EXCEPTIONS_FILE} with pillar_key='{pillar_key}', "
                        f"machine, libversion, observed delta, suspected root cause, signoff.\n"
                        f"Original error: {primary_err}"
                    ) from primary_err
    ```

    **[C5 REVIEWS] Créer aussi `.planning/phases/10-pfc-fmv-quality-scorecard/REPRODUCIBILITY-EXCEPTIONS.md`** (template vide, populé seulement si une exception se présente) :
    ```
    # Plan 10-04 — Reproducibility Exceptions [C5 REVIEWS]

    **Purpose** : Documenter UNIQUEMENT les tuples (pillar_key, machine, libversion) où la tolerance primaire `atol=1e-12 rtol=0` du contrat D-A6-3 ne tient pas, justifier le fallback `atol=1e-10`, et tracer le signoff.

    **Convention** : aucune exception silencieuse. Chaque ligne représente une décision explicite signée par l'opérateur.

    **Format strict** (parseable par `_load_documented_exceptions` dans `tests/test_phase10_reproducibility.py`) :

    | pillar_key | machine | libversion | observed_delta | suspected_root_cause | signoff |
    |---|---|---|---|---|---|
    | (exemple : pillar2_df) | (Mac Mini M2 Pro macOS 14.6) | (pandas 2.2.3 + statsmodels 0.14.6 + numpy 1.26) | (1.3e-11 sur colonne mz_p_value) | (statsmodels OLS chain non-déterministe en chunked computation) | (julien 2026-MM-DD) |

    **État Plan 10-04** : <vide = aucune exception nécessaire (test PASS à 1e-12)> OR <N entrées documentées>.

    **NOTE** : si ce fichier reste vide, c'est le bon état — le contrat 1e-12 tient. Si des entrées apparaissent, c'est un signal qu'une investigation determinism est due (Phase ultérieure).
    ```

    Run le test une fois post-creation pour valider qu'il PASS sur l'environnement actuel :
    `python -m pytest tests/test_phase10_reproducibility.py -x -v --timeout=300`.

    Si le test FAIL (e.g. non-determinism détecté dans la chaîne Uncertainty bootstrap ou ShapeHourly fit) :
    - Logger les max abs diffs surface in error message.
    - Bloquer Task 6 (D-FLIP-1 commit) jusqu'à résolution.
    - Investigate seed propagation dans pfc_shaping/lt/model/{uncertainty.py, shape_hourly.py} (chercher `np.random` direct usages non-seedés).
  </action>
  <verify>
    <automated>python -m pytest tests/test_phase10_reproducibility.py -x -v --timeout=300 && grep -c "atol=1e-12\|atol=1e-10\|rtol=0\|REPRODUCIBILITY_TOLERANCE" tests/test_phase10_reproducibility.py | tr -d ' ' | grep -qE "^[5-9]$|^[1-9][0-9]$" && test -f .planning/phases/10-pfc-fmv-quality-scorecard/REPRODUCIBILITY-EXCEPTIONS.md && grep -q "C5 REVIEWS" .planning/phases/10-pfc-fmv-quality-scorecard/REPRODUCIBILITY-EXCEPTIONS.md && grep -q "_load_documented_exceptions" tests/test_phase10_reproducibility.py</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_phase10_reproducibility.py` existe et expose `test_reproducibility_subset_4_builds_config_4`
    - Test PASS sur environnement actuel — cible primaire `atol=1e-12 rtol=0` ; fallback `atol=1e-10` autorisé UNIQUEMENT via entrée dans `REPRODUCIBILITY-EXCEPTIONS.md`
    - **[C5 REVIEWS] `tests/test_phase10_reproducibility.py` contient les 2 constantes** `REPRODUCIBILITY_TOLERANCE_PRIMARY` (atol=1e-12) ET `REPRODUCIBILITY_TOLERANCE_FALLBACK` (atol=1e-10) — assertable via `grep -c "REPRODUCIBILITY_TOLERANCE_PRIMARY\|REPRODUCIBILITY_TOLERANCE_FALLBACK" tests/test_phase10_reproducibility.py` >= 2
    - **[C5 REVIEWS] `tests/test_phase10_reproducibility.py` expose un helper `_load_documented_exceptions`** qui parse REPRODUCIBILITY-EXCEPTIONS.md ; aucun appel à la fallback `1e-10` sans entrée correspondante (silent relaxation interdite)
    - **[C5 REVIEWS] `.planning/phases/10-pfc-fmv-quality-scorecard/REPRODUCIBILITY-EXCEPTIONS.md` existe** avec le template documenté (purpose, convention, format strict, état initial = vide attendu)
    - **[C5 REVIEWS] Si le test PASS via fallback 1e-10** : un message `INFO [C5 REVIEWS] reproducibility relaxed` est printé identifiant le (pillar_key, exception file path) ; jamais de pass silencieux
    - **[C5 REVIEWS] Si le test FAIL** : le message d'erreur cite explicitement le chemin `REPRODUCIBILITY-EXCEPTIONS.md` et les 6 champs requis (pillar_key, machine, libversion, observed_delta, suspected_root_cause, signoff) pour ajouter une entrée légitime
    - Si data/epex_15min.parquet absent → pytest.skip avec message clair (pas un fail, juste skipped en absence de cache)
    - Runtime < 5 minutes (subset = 4 builds × ~25s + 2 runs = ~3-4 min total)
    - Error message explicit en cas de fail (surface max abs diff per column)
    - Suite globale `pytest tests/` reste verte
  </acceptance_criteria>
  <done>D-A6-3 reproducibility contract asserted in CI ; toute régression future de determinism détectée immédiatement via re-run subset 4 builds. [C5 REVIEWS] tolerance double-niveau (primary 1e-12, fallback documenté 1e-10) + REPRODUCIBILITY-EXCEPTIONS.md template livrés.</done>
</task>

<task type="auto">
  <name>Task 6 — PROJECT.md D-FLIP-1 flip OR blocker entry + Phase 10 close-out</name>
  <files>.planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/PROJECT.md (Key Decisions table, entrée D-FLIP-1 actuelle ligne ~85)
    - .planning/phases/10-pfc-fmv-quality-scorecard/10-VERIFICATION.md (verdict SC#1 explicit)
    - Output Task 4 (decision human-verify : flip ON / block / needs revision)
    - Output Task 5 (reproducibility contract PASS / FAIL)
  </read_first>
  <action>
    **Pré-condition Task 5** : si Task 5 reproducibility test FAIL → BLOCK Task 6, ne pas écrire D-FLIP-1 flip ni close-out. Emit blocker explicite : "D-A6-3 reproducibility contract violated — fix determinism before D-FLIP-1 commit".

    Selon decision Task 4 :

    **Si "approved — flip D-FLIP-1 ON"** :
    Localiser l'entrée D-FLIP-1 actuelle dans `.planning/PROJECT.md` (ligne ~85 dans la table Key Decisions). Update le contenu :
    - Ajouter une nouvelle entrée timestamped 2026-MM-DD (date du jour réel) :
      ```
      | 2026-MM-DD | **Flag PFC_LT_USE_SEASONAL_HOURLY_SHAPE flippé default ON suite à Phase 10 SC#1 PASS : 4 tests structurels Hildmann (arb-free <0.01 €/MWh, holiday/weekend ratio ∈ <range from 10-01-NOTES>, seasonal corr >0.85, continuity <2 €/MWh) PASS sous Config 4 (bowl ON + floors negative-ready) sur agrégat 24 vintages 2024-2025. Pillars 2-5 produisent contenu informational (cf 10-VERIFICATION.md). D-A6-3 reproducibility contract verified (Task 5 subset 4 builds atol=1e-12 rtol=0 PASS). D-FLIP-1 cristallisé.** (D-FLIP-1 PASS) | Phase 10 PFC FMV Quality Scorecard livré. Pivot D-A0-2 cohérent : qualité absolue structurelle prime sur statistique/comparative. Réf : .planning/phases/10-pfc-fmv-quality-scorecard/10-VERIFICATION.md |
      ```
    - PAS de modification automatique du code production pour flipper le default (Phase 10 mesure, ne push pas en prod) — l'opérateur ferait cela via une PR séparée référençant cette decision. Documenter ce point explicitement dans l'entrée.

    **Si "approved — block D-FLIP-1, document blocker"** :
    Ajouter entrée :
    ```
    | 2026-MM-DD | **Flip default ON de PFC_LT_USE_SEASONAL_HOURLY_SHAPE BLOQUÉ : Phase 10 SC#1 FAIL (X/4 Hildmann tests passent). Blocker(s) : <liste des tests fail avec observed/threshold>. Next steps : <option recalibration threshold OR Phase 5bis-C bowl tuning OR investigate forwards quality>. D-A6-3 reproducibility PASS (le determinism n'est pas en cause, seulement le verdict structurel). D-FLIP-1 reste default OFF. Cohérent EPFL principle "no production change without empirical validation gate".** (D-FLIP-1 BLOCKED) | Phase 10 PFC FMV Quality Scorecard livré ; verdict structural fail. Réf : .planning/phases/10-pfc-fmv-quality-scorecard/10-VERIFICATION.md |
    ```

    **Si "needs revision"** : NE PAS écrire dans PROJECT.md. Émettre un blocker explicite : "Plan 10-04 Task 4 demande revision Pillar X — re-trigger Plan 10-04 Task 2/3 après update du contenu". STOP.

    **Dans tous les cas (PASS ou BLOCKED)** :
    Update également la table de Traceability dans `.planning/REQUIREMENTS.md` (ligne ajoutée Plan 10-01 Task 4) :
    - Ligne `BT-01, BT-02, BT-04, BT-06..BT-10 | Phase 10 | active` → `BT-01, BT-02, BT-04, BT-06..BT-10 | Phase 10 | completed YYYY-MM-DD`

    Update également `.planning/ROADMAP.md` :
    - Active section : Phase 10 entrée → marquer `[x]` (livré) si SC#1 PASS, OR garder `[ ]` avec note "BLOCKED — see PROJECT.md D-FLIP-1" si BLOCKED.
    - Plans listing : marquer tous les 4 plans `[x]`.

    Note d'orchestration : ne pas créer 10-SUMMARY.md à ce niveau (chaque plan a déjà son SUMMARY individuel). La phase-level summary est ROADMAP + PROJECT.md updates.
  </action>
  <verify>
    <automated>cd /Users/julienbattaglia/Desktop/PFC && grep -E "D-FLIP-1 PASS|D-FLIP-1 BLOCKED" .planning/PROJECT.md && grep -E "Phase 10.*completed|Phase 10.*BLOCKED|\[x\] \*\*Phase 10:" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/PROJECT.md` contient nouvelle entrée Key Decisions timestamped (date du jour) avec verdict D-FLIP-1 PASS ou BLOCKED (cohérent avec Task 4 decision)
    - PROJECT.md entrée mentionne explicitement D-A6-3 reproducibility verified (Task 5 PASS)
    - `.planning/REQUIREMENTS.md` Traceability table : Phase 10 ligne marquée `completed YYYY-MM-DD`
    - `.planning/ROADMAP.md` Active section : Phase 10 entry `[x]` (si PASS) OR note "BLOCKED" (si FAIL)
    - Phase 10B (deferred) reste inchangée dans REQUIREMENTS.md et ROADMAP.md (rien à clôturer)
    - Si PASS : PROJECT.md mentionne explicitement Config 4 + 4 tests Hildmann avec observed/threshold + référence 10-VERIFICATION.md + D-A6-3 verified
    - Si BLOCKED : PROJECT.md mentionne les tests fail spécifiques + next steps actionnables
  </acceptance_criteria>
  <done>Phase 10 close-out documenté ; D-FLIP-1 cristallisé (flip OU blocker) ; D-A6-3 reproducibility verified Task 5 ; ROADMAP + REQUIREMENTS reflètent l'état completed/blocked ; Plan 10-04 livré.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| run_scorecard_full output → 10-VERIFICATION.md | Contenu généré automatiquement (KPIs, tables, verdict SC#1) — boundary : un bug dans render_markdown_report pourrait propager un faux verdict → mitigation : Task 4 human-verify checkpoint avant D-FLIP-1 |
| 96-build cache parquet → re-run reproducibility | Cache local Mac Mini (gitignored) ; tolerance contract atol=1e-12 rtol=0 (RESEARCH Pitfall 6) verified Task 5 |
| .planning/PROJECT.md update D-FLIP-1 | Decision boundary irreversible (git history préservera mais le default flag flip impacte production) ; gated par Task 4 human-verify ET Task 5 reproducibility PASS |
| _build_pred_baseline_for_vintage scalar broadcast | Decision boundary : si scalar=NaN (e.g. persistence Y-1 window vide) → propagation NaN dans DM test → degenerate flag attendu, pas de silent skip |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04-01 | Tampering | 96-build seed determinism re-run | mitigate | **Task 5 reproducibility test ASSERTED via tests/test_phase10_reproducibility.py** : subset 4 builds atol=1e-12 rtol=0 PASS pre-requirement pour Task 6 D-FLIP-1 commit |
| T-10-04-02 | Repudiation | 10-VERIFICATION.md verdict SC#1 not traceable | mitigate | Verdict inscrit dans 10-VERIFICATION.md (committed git) + PROJECT.md entry timestamped + reference cross-document + D-A6-3 mention explicite |
| T-10-04-03 | Tampering | Pillar 5 sources non vérifiables | mitigate | Section Sources explicite + sourcée vs reference_pfc_state_of_art.md (mémoire user committed externe) ; cellule cells avec commentaires courts ≤15 mots |
| T-10-04-04 | Denial of service | 96-build run takes >2h → CI timeout | mitigate | Background execution + intermediate cache (cache_intermediate=True) ; vintages_limit pour dry-run préalable Task 1 ; reproducibility subset 4 builds Task 5 = ~3 min only |
| T-10-04-05 | Elevation of privilege | scripts/run_phase10_scorecard.py CLI runner | accept | Pure read-only sur data caches + write only à output_dir ; pas d'effet secondaire sur code prod (D-FLIP-1 flip est dans PROJECT.md doc, pas dans code) |
| T-10-04-06 | Information disclosure | Cache PFC parquets (~2.4 GB) gitignored | mitigate | Vérifier `.gitignore` couvre `.planning/phases/*/cache/` ou ajouter ; cache local Mac Mini seulement |
| T-10-04-07 | Repudiation | _build_pred_baseline_for_vintage logic implicit (warning #8 historique) | mitigate | Helper explicite + docstring D-A4-1 référence + 3 cases couverts (climatology/persistence_y1/forwards_flat) + ValueError sur unknown baseline_name |
</threat_model>

<verification>
- `pytest tests/` : suite globale reste verte (≥323 tests cumul Phase 10 + tests reproducibility)
- `python scripts/run_phase10_scorecard.py --help` : argparse working incl. `--reproducibility-subset`
- `test -f .planning/phases/10-pfc-fmv-quality-scorecard/10-VERIFICATION.md && ! grep -q "PLACEHOLDER" .planning/phases/10-pfc-fmv-quality-scorecard/10-VERIFICATION.md` : OK
- `ls .planning/phases/10-pfc-fmv-quality-scorecard/figures/*.png | wc -l` ≥ 4
- `grep -E "D-FLIP-1 PASS|D-FLIP-1 BLOCKED" .planning/PROJECT.md` : 1 hit (selon decision Task 4)
- `grep -E "Pillar [1-5] —" .planning/phases/10-pfc-fmv-quality-scorecard/10-VERIFICATION.md | wc -l` >= 5 (les 5 pillars présents)
- `grep -E "Executive Summary|Table of Contents|Annexes" .planning/phases/10-pfc-fmv-quality-scorecard/10-VERIFICATION.md` : 3 hits
- `grep -E "IC95.*Phase 5ter|p10/p90 only" .planning/phases/10-pfc-fmv-quality-scorecard/10-VERIFICATION.md` : ≥1 hit (audit-trail blocker #4 dans annexes ou Pillar 3)
- Aucune occurrence "TBD" ou "TODO" dans 10-VERIFICATION.md (contenu complet)
- `.planning/REQUIREMENTS.md` Phase 10 entry marquée `completed` ; `.planning/ROADMAP.md` Phase 10 `[x]`
- `python -m pytest tests/test_phase10_reproducibility.py -x` : PASS (D-A6-3 contract verified, blocker #3 résolu)
- `grep -c "atol=1e-12.*rtol=0" tests/test_phase10_reproducibility.py` ≥ 1
- **[C5 REVIEWS]** `test -f .planning/phases/10-pfc-fmv-quality-scorecard/REPRODUCIBILITY-EXCEPTIONS.md && grep -q "REPRODUCIBILITY_TOLERANCE_FALLBACK" tests/test_phase10_reproducibility.py` : OK
- **[C6 REVIEWS]** `test -f scripts/preflight_phase10.py && test -f .planning/phases/10-pfc-fmv-quality-scorecard/10-PREFLIGHT.md && grep -E "^\*\*Verdict\*\* : \*\*(go|no-go)\*\*" .planning/phases/10-pfc-fmv-quality-scorecard/10-PREFLIGHT.md` : OK (verdict file présent)
- **[C3 REVIEWS]** `grep -E "Gate-eligible run|Diagnostic only — not gate-eligible" .planning/phases/10-pfc-fmv-quality-scorecard/10-VERIFICATION.md` : 1 hit (banner gate-eligibility en tête Pillar 1)
</verification>

<success_criteria>
1. **96-build grid exécuté** : 24 vintages × 4 configs avec Pillar 3 uncertainty seulement Config 4 (Pitfall 7 mitigation) ; intermediate cache prêt pour re-run
2. **Per-vintage pred_baseline construction explicit** : `_build_pred_baseline_for_vintage` helper expose 3 baselines avec docstring D-A4-1 (warning #8 fix)
3. **Scorecard final écrit** : `10-VERIFICATION.md` complet (executive summary + 5 sections pillars + 4 figures embedded inline + Pillar 5 peer review + annexes + IC95 audit-trail), lisible standalone par trader senior FMV
4. **4 figures PNG publication-grade** : Pillar 1 seasonal scatter, Pillar 2 MAE bar + scatter pred/realised, Pillar 3 reliability overlay ; tous ≥150 dpi matplotlib
5. **D-A6-3 reproducibility verified** : `tests/test_phase10_reproducibility.py` PASS sur subset 4 builds — **[C5 REVIEWS] cible primaire atol=1e-12 rtol=0 ; fallback atol=1e-10 documenté via REPRODUCIBILITY-EXCEPTIONS.md (état initial vide attendu)** (blocker #3 résolu)
6. **Verdict SC#1 cristallisé** : PROJECT.md entrée D-FLIP-1 PASS (flip ON) OU BLOCKED (blocker documenté + next steps actionnables) selon real-run Hildmann 4/4 result ; mention explicite D-A6-3 verified ; **[C3 REVIEWS] SC#1 gate-eligibility annotée dans 10-VERIFICATION.md banner + colonne forwards_source dans KPIs parquets**
7. **Pas de régression** : suite globale `pytest tests/` reste verte
8. **Documents planning à jour** : REQUIREMENTS.md Traceability + ROADMAP.md Active reflètent Phase 10 completed/blocked
9. **2 human-verify checkpoints respectés** : Task 1 (cost confirmation 96-build) + Task 4 (review scorecard + D-FLIP-1 decision)
10. **[C6 REVIEWS] Preflight Task 0 livré** : `scripts/preflight_phase10.py` + `10-PREFLIGHT.md` verdict file ; 5 checks (data freshness ≤14j, forwards_source path, disk space ≥5GB, runtime bench micro-run ≤2.5h hard cap, verdict file go|no-go) ; verdict no-go BLOQUE Task 1 cost confirmation
10. **Requirements frontmatter complète** : BT-06..BT-10 listés (warning #5 fix — auparavant que [BT-10])
</success_criteria>

<output>
Plan 10-04 ne crée PAS de 10-04-SUMMARY.md séparé. Le SUMMARY est :
- `10-VERIFICATION.md` lui-même = livrable final Phase 10
- PROJECT.md D-FLIP-1 entry update = decision cristallisée
- ROADMAP.md + REQUIREMENTS.md updates = état completed
- `tests/test_phase10_reproducibility.py` = D-A6-3 contract guard (Task 5)

À la complétion, return au orchestrateur un message structuré :
```
## PHASE 10 COMPLETE

**Verdict SC#1** : <PASS 4/4 | FAIL X/4>
**D-A6-3 Reproducibility** : <VERIFIED | VIOLATED (blocker fix required)>
**D-FLIP-1** : <FLIPPED ON 2026-MM-DD | BLOCKED — blocker details>
**Compute** : N builds × M total minutes Mac Mini
**Scorecard livrable** : .planning/phases/10-pfc-fmv-quality-scorecard/10-VERIFICATION.md
**Pillars** :
  - Pillar 1 Hildmann (gate) : <details 4 tests>
  - Pillar 2 Empirical : <highlights MAE/RMSE/bias par config>
  - Pillar 3 Coverage Config 4 (IC80 only ; IC95 défère Phase 5ter) : <details>
  - Pillar 4 DM vs 3 baselines : <highlights win-rate>
  - Pillar 5 Peer Review : table 9×6 + gap analysis livrés
**Next step** : <Phase 5ter (distribution probabiliste + IC95 + Christoffersen conditional) | Phase 10B (OMPEX benchmark depuis FMV poste) | other>
```
</output>
