---
phase: 10
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - pfc_shaping/validation/christoffersen.py
  - pfc_shaping/validation/dm_test.py
  - pfc_shaping/validation/scorecard.py
  - tests/test_phase10_probabilistic.py
  - tests/test_phase10_dm.py
autonomous: true
requirements: [BT-04, BT-08, BT-09]
notes:
  ic95_deferral: |
    Blocker #4 resolution (checker iter 0) : pfc_shaping/lt/model/uncertainty.py:51-194
    expose UNIQUEMENT IC80 (cols p10/p90 hardcoded percentiles 10/90). Pas de
    paramètre level= ni confidence=. Étendre Uncertainty pour IC95 = scope-creep
    sur Phase 10 (toucherait code core LT model). Décision : IC95 (p2.5/p97.5)
    déférée Phase 5ter (CONTEXT D-A3-3 amendé). Plan 10-03 Task 1 teste IC80
    only — pas de "silent skip", l'absence de IC95 est explicite et documentée.
    Cohérent avec REQUIREMENTS.md BT-08 wording mis à jour Plan 10-01 Task 4.

must_haves:
  truths:
    - "Christoffersen unconditional coverage LR_uc retourne (lr_stat, p_value, observed_freq, nominal_p, n, x) per RESEARCH Pattern 3 — closed-form, no MCMC"
    - "Diebold-Mariano test maison ~30 lignes utilise statsmodels.tsa.stattools.acovf + Bartlett HAC + HLN small-sample correction, retourne (dm_stat, p_value, n, mean_d, var_d, n_lags_hac, degenerate)"
    - "Les 3 baselines maison (climatology, persistence_y1, forwards_flat) sont implémentés comme fonctions deterministes (vintage_date, bloc) → pd.Series"
    - "Edge case DM var_d ≤ 0 (Pitfall 3 RESEARCH) handled : fallback sample variance lag=0 puis NaN p_value + degenerate=True flag, pas de crash"
    - "Edge case Christoffersen n=0 ou x∈{0,n} retourne degenerate=True dict avec p_value=NaN, pas de log(0) crash"
    - "Tests Pillar 3 et Pillar 4 verts sur mock data déterministe ; lag=h-1 vérifié pour h=1 (lag=0 = sample var) et h=24 (lag=23 typique Y+2)"
    - "compute_pillar3_coverage teste IC80 SEUL (binomial LR_uc Christoffersen 1998) — IC95 explicitement déférée Phase 5ter (Uncertainty API ne supporte pas natively, voir notes.ic95_deferral). Aucune silent skip — paramètre ic_level=0.80 fixé + ValueError explicite si caller passe ic_level=0.95"
  acceptance_caveats:
    - "IC95 unconditional coverage NOT tested in Phase 10 — déférée Phase 5ter quand Uncertainty class sera étendue (level= param OR pfc_block_distribution full)"
    - "Si user veut IC95 testé dès Phase 10 → réouvrir CONTEXT D-A3-3 et planifier extension uncertainty.py dans Plan 10-01 (scope-creep accepté explicitement)"
  artifacts:
    - path: "pfc_shaping/validation/christoffersen.py"
      provides: "lr_unconditional_coverage(x, n, p) Christoffersen 1998 LR_uc test per RESEARCH §Pattern 3 lignes 305-351"
      exports: ["lr_unconditional_coverage"]
    - path: "pfc_shaping/validation/dm_test.py"
      provides: "diebold_mariano(errors_a, errors_b, h, loss, hln_correction) + 3 baselines (baseline_climatology, baseline_persistence_y1, baseline_forwards_flat) per RESEARCH §Pattern 4 lignes 359-462"
      exports: ["diebold_mariano", "baseline_climatology", "baseline_persistence_y1", "baseline_forwards_flat"]
    - path: "pfc_shaping/validation/scorecard.py"
      provides: "Pillar 3/4 helpers wired : compute_pillar3_coverage(pfc_with_ic, realised, bloc, ic_level=0.80 only) + compute_pillar4_dm(pred_pfc, pred_baseline, realised, bloc, h_months)"
      exports: ["compute_pillar3_coverage", "compute_pillar4_dm"]
    - path: "tests/test_phase10_probabilistic.py"
      provides: "≥6 pytest Pillar 3 : LR_uc sanity 4 cas (perfect coverage, undercoverage, overcoverage, degenerate n=0|x∈{0,n}) + compute_pillar3_coverage integration IC80 only + test_compute_pillar3_coverage_rejects_ic95"
    - path: "tests/test_phase10_dm.py"
      provides: "≥8 pytest Pillar 4 : DM 4 cas (A clearly better, equal accuracy, B clearly better, edge h=1 lag=0) + 3 baselines deterministic outputs + var_d≤0 edge case + integration compute_pillar4_dm"
  key_links:
    - from: "pfc_shaping/validation/dm_test.py::diebold_mariano"
      to: "statsmodels.tsa.stattools.acovf"
      via: "acovf(d, nlag=h-1, fft=True, demean=True) puis Bartlett-weighted sum gamma_0 + 2*sum(w_k*gamma_k)"
      pattern: "from statsmodels.tsa.stattools import acovf|acovf\\("
    - from: "pfc_shaping/validation/dm_test.py::diebold_mariano"
      to: "scipy.stats.t.sf"
      via: "HLN correction → Student-t df=n-1 (au lieu de N(0,1)) pour small-sample p_value"
      pattern: "from scipy.stats import t|student_t\\.sf"
    - from: "pfc_shaping/validation/christoffersen.py::lr_unconditional_coverage"
      to: "scipy.stats.chi2.sf"
      via: "LR_uc ~ chi2(df=1) → p_value = chi2.sf(lr_stat, df=1)"
      pattern: "chi2\\.sf|from scipy.stats import chi2"
    - from: "pfc_shaping/validation/dm_test.py::baseline_persistence_y1"
      to: "pandas DatetimeIndex shift par 1 year ± 15 jours window"
      via: "mean(EPEX_realised[bloc filter, vintage_date - 1yr ± 15j])"
      pattern: "DateOffset\\(years=1\\)|Timedelta\\(days=15\\)"
---

<objective>
Plan 10-03 (wave 3) — Implémenter Pillar 3 (Christoffersen unconditional coverage = binomial LR test sur **IC80 seul** ; IC95 déférée Phase 5ter cf. notes.ic95_deferral) et Pillar 4 (Diebold-Mariano test vs 3 baselines naive maison — climatology, persistence Y-1, forwards-flat-no-shape).

**Blocker #4 resolution explicite** : `pfc_shaping/lt/model/uncertainty.py:51-194` (vérifié Plan 10-03 revision iter 1) expose UNIQUEMENT IC80 — la méthode `Uncertainty.compute(pfc_df, calendar_df)` retourne un DataFrame avec colonnes `p10` et `p90` hardcoded (percentiles 10 et 90), sans paramètre `level=` ni `confidence=`. Étendre Uncertainty pour IC95 toucherait le code core LT model = scope-creep sur Phase 10. Décision : IC95 explicitement déférée Phase 5ter (CONTEXT D-A3-3 amendé). Plan 10-03 Task 1 teste IC80 SEUL — aucun "silent skip", l'API rejette explicitement ic_level=0.95 avec ValueError pour éviter toute ambiguïté.

Purpose : Compléter le scorecard avec les 2 piliers statistiques. Pillar 3 mesure la calibration probabiliste IC80 (la fréquence empirique des violations IC80 matche-t-elle le nominal 20% ?) en réutilisant l'API Uncertainty bootstrap existante. Pillar 4 mesure si la PFC FMV est significativement meilleure que des baselines naïves (DM test maison ~30 lignes, ZÉRO dep PyPI risquée). Ni l'un ni l'autre n'est un gate (SC#1 Hildmann seul gate), mais ils alimentent le contenu informational du scorecard final (Plan 10-04).

Output :
- `pfc_shaping/validation/christoffersen.py` (LR_uc binomial test ~50 lignes)
- `pfc_shaping/validation/dm_test.py` (DM HAC + 3 baselines ~150 lignes)
- `pfc_shaping/validation/scorecard.py` étendu avec compute_pillar3_coverage (IC80 only, ValueError on ic_level=0.95) + compute_pillar4_dm
- `tests/test_phase10_probabilistic.py` (≥6 tests Pillar 3 incl. test_compute_pillar3_coverage_rejects_ic95)
- `tests/test_phase10_dm.py` (≥8 tests Pillar 4)
</objective>

<execution_context>
@/Users/julienbattaglia/Desktop/PFC/.claude/get-shit-done/workflows/execute-plan.md
@/Users/julienbattaglia/Desktop/PFC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-pfc-fmv-quality-scorecard/10-CONTEXT.md
@.planning/phases/10-pfc-fmv-quality-scorecard/10-RESEARCH.md
@.planning/phases/10-pfc-fmv-quality-scorecard/10-02-SUMMARY.md
@pfc_shaping/validation/block_masks.py
@pfc_shaping/validation/scorecard.py
@pfc_shaping/validation/structural_tests.py
@pfc_shaping/lt/model/uncertainty.py
@pfc_shaping/lt/model/assembler.py

<interfaces>
<!-- Contracts extraits du codebase + outputs Plans 10-01/10-02. -->

From pfc_shaping/lt/model/uncertainty.py (lines 51-194, READ Plan 10-03 revision iter 1 pour blocker #4) :
```python
class Uncertainty:
    def __init__(self, n_boot: int = N_BOOT, seed: int = SEED) -> None: ...
    def fit(self, epex_df: pd.DataFrame, calendar_df: pd.DataFrame) -> "Uncertainty": ...
    def compute(self, pfc_df, calendar_df, reference_date=None) -> pd.DataFrame:
        # Returns DataFrame with columns ['p10', 'p90'] ONLY
        # Internal: boot_stats_ stocke percentiles 10/50/90 hardcoded (lines 110, 116, 119)
        # NO level=/confidence= parameter — IC95 (p2.5/p97.5) NOT SUPPORTED
        # Plan 10-03 IC80 only ; IC95 defer Phase 5ter (notes.ic95_deferral)
```

From pfc_shaping/lt/model/assembler.py (output DataFrame with_uncertainty=True):
- Columns 'price_shape', 'p10', 'p90' (les 2 dernières sont les IC80 bounds — IC95 NON exposé)
- Per Plan 10-01 build_one(with_uncertainty=False par défaut) ; pour Pillar 3 il faut un re-run avec True sur Config 4 SEULEMENT (per RESEARCH Pitfall 7 compute budget mitigation)

From pfc_shaping/validation/block_masks.py et scorecard.py (créés Plans 10-01/10-02) :
- ALL_BLOCKS, ABLATION_GRID, build_one, compute_cell_kpis, mz_test, derive_forwards_from_epex_hist (réutilisés)

From RESEARCH §Pattern 3 canonical (lignes 305-351), §Pattern 4 canonical (lignes 359-462) :
```python
def lr_unconditional_coverage(x: int, n: int, p: float) -> dict:
    """LR_uc = -2 * log( (p^x * (1-p)^(n-x)) / (pi_hat^x * (1-pi_hat)^(n-x)) )
    Distribution: chi-squared with 1 df. Returns dict(lr_stat, p_value, observed_freq, nominal_p, n, x, degenerate)."""

def diebold_mariano(errors_a, errors_b, h=1, loss="mae", hln_correction=True) -> dict:
    """Newey-West HAC with Bartlett-weighted variance + HLN small-sample correction.
    Returns dict(dm_stat, p_value, n, mean_d, var_d, n_lags_hac, degenerate)."""
```

From RESEARCH §Common Pitfalls Pitfall 3, Pitfall 7 :
- Pitfall 3 : var_d ≤ 0 edge (rare for h ≥ 2 small samples) — fallback sample variance lag=0 + emit log.warning at caller
- Pitfall 7 : Pillar 3 with_uncertainty=True ONLY on Config 4 (production target) — 24 builds avec uncertainty ≈ 36-50 min Mac Mini ; les 3 autres configs run sans uncertainty (Plan 10-04 orchestration)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 — Pillar 3 : christoffersen.py + tests probabilistic (IC80 only, IC95 explicit defer)</name>
  <files>pfc_shaping/validation/christoffersen.py, pfc_shaping/validation/scorecard.py, tests/test_phase10_probabilistic.py</files>
  <read_first>
    - .planning/phases/10-pfc-fmv-quality-scorecard/10-RESEARCH.md §Pattern 3 canonical (lignes 305-351)
    - .planning/phases/10-pfc-fmv-quality-scorecard/10-CONTEXT.md §D-A3-1..D-A3-3 (unconditional only, conditional + reliability déférés Phase 5ter)
    - pfc_shaping/lt/model/uncertainty.py lines 51-194 (vérifié : compute() returns p10/p90 only, no level= param ; IC95 nécessite extension code core = scope-creep refusé)
    - pfc_shaping/validation/scorecard.py (Task 2 Plan 10-02 : ne pas casser existing)
    - pfc_shaping/validation/block_masks.py (BlockMask.apply pour filtrer aux rows in-bloc)
  </read_first>
  <behavior>
    - `lr_unconditional_coverage(x=20, n=100, p=0.20)` (perfect coverage 20% violations vs 20% nominal) → lr_stat ≈ 0, p_value > 0.9 (H0 not rejected)
    - `lr_unconditional_coverage(x=5, n=100, p=0.20)` (undercoverage : seulement 5% observed, 20% nominal) → lr_stat > 5, p_value < 0.05 (H0 rejected)
    - `lr_unconditional_coverage(x=40, n=100, p=0.20)` (overcoverage : 40% observed, 20% nominal) → lr_stat > 5, p_value < 0.05
    - `lr_unconditional_coverage(x=0, n=100, p=0.20)` → degenerate=True, p_value=NaN, observed_freq=0.0, pas de log(0) crash
    - `lr_unconditional_coverage(x=100, n=100, p=0.20)` → degenerate=True, p_value=NaN, observed_freq=1.0, pas de crash
    - `lr_unconditional_coverage(x=0, n=0, p=0.20)` → degenerate=True, observed_freq=NaN, n=0, x=0, pas de division-by-zero
    - `compute_pillar3_coverage(pfc_with_ic, realised, bloc=BlockMiddayWeekday(), ic_level=0.80)` retourne dict {bloc, ic_level, nominal_p, x, n, observed_freq, lr_stat, p_value, degenerate} ; n=count(in-bloc), x=count(realised < p10_bound OR realised > p90_bound)
    - **`compute_pillar3_coverage(pfc_with_ic, realised, bloc, ic_level=0.95)` raise ValueError explicite** : `"IC95 not supported in Phase 10 — Uncertainty API exposes p10/p90 only (IC80). IC95 deferred to Phase 5ter (CONTEXT D-A3-3 amendé)."` — pas de silent skip, pas de fallback ambigu
  </behavior>
  <action>
    Créer `pfc_shaping/validation/christoffersen.py` per RESEARCH §Pattern 3 canonical (lignes 305-351) :
    - Header docstring "Pillar 3 — Christoffersen 1998 unconditional coverage LR test. IC80 only in Phase 10. IC95 + Conditional coverage + reliability diagrams deferred Phase 5ter (D-A3-3 amendé Plan 10-03 revision iter 1, raison: Uncertainty API uncertainty.py:51-194 expose p10/p90 only)."
    - `from __future__ import annotations` ; imports numpy as np, from scipy.stats import chi2
    - `def lr_unconditional_coverage(x: int, n: int, p: float) -> dict` per canonical impl :
      - Degenerate guards : `if n == 0 or x == 0 or x == n: return {"lr_stat": NaN, "p_value": NaN, "observed_freq": (NaN if n == 0 else x/n), "nominal_p": p, "n": n, "x": x, "degenerate": True}`
      - `pi_hat = x / n`
      - `log_lik_null = x * np.log(p) + (n - x) * np.log(1 - p)`
      - `log_lik_alt = x * np.log(pi_hat) + (n - x) * np.log(1 - pi_hat)`
      - `lr_stat = -2.0 * (log_lik_null - log_lik_alt)`
      - `p_value = float(chi2.sf(lr_stat, df=1))`
      - return dict {"lr_stat": float(lr_stat), "p_value": p_value, "observed_freq": float(pi_hat), "nominal_p": p, "n": int(n), "x": int(x), "degenerate": False}

    Étendre `pfc_shaping/validation/scorecard.py` (additif) :
    `def compute_pillar3_coverage(pfc_with_ic: pd.DataFrame, realised: pd.Series, bloc: BlockMask, ic_level: float = 0.80) -> dict` :
    - **Guard IC95 explicit reject** (blocker #4 fix) :
      ```
      if not np.isclose(ic_level, 0.80):
          raise ValueError(
              f"compute_pillar3_coverage: ic_level={ic_level} not supported. "
              f"Phase 10 tests IC80 only (Uncertainty.compute returns p10/p90, no IC95 bounds). "
              f"IC95 deferred to Phase 5ter (CONTEXT D-A3-3 amendé). "
              f"To enable IC95: extend pfc_shaping/lt/model/uncertainty.py to support level= param, then re-open this guard."
          )
      ```
    - Vérifier que pfc_with_ic a colonnes "price_shape", "p10", "p90" (sinon ValueError explicite)
    - nominal_p = 0.20 (violation probability pour IC80 = 1.0 - 0.80)
    - Aligner pfc_with_ic et realised via inner join sur DatetimeIndex tz-aware UTC
    - Appliquer mask `bloc.apply(aligned.index)` pour filtrer aux rows in-bloc
    - Compter violations : `x = int(((realised_masked < pfc_masked["p10"]) | (realised_masked > pfc_masked["p90"])).sum())`
    - `n = int(mask.sum())`
    - Return `{**lr_unconditional_coverage(x, n, nominal_p), "bloc": bloc.name, "ic_level": ic_level}`

    Créer `tests/test_phase10_probabilistic.py` :
    - Classe `TestLrUnconditionalCoverage` (5 tests) : test_perfect_coverage (x=20, n=100, p=0.20 → p>0.9), test_undercoverage (x=5, p<0.05), test_overcoverage (x=40, p<0.05), test_degenerate_n_0 (n=0, degenerate=True), test_degenerate_x_in_extremes (x=0 OR x=n, degenerate=True no log(0) crash)
    - Classe `TestComputePillar3Coverage` (3 tests) :
      - `test_synth_pfc_realised_in_bounds` : construire pfc_with_ic + realised synth tel que ~20/100 violations exact pour IC80 → p_value > 0.5 (sanity)
      - `test_missing_p10_p90_raises_valueerror` : pfc_with_ic sans col p10 ou p90 → ValueError explicit
      - **`test_compute_pillar3_coverage_rejects_ic95`** (blocker #4 fix) : `compute_pillar3_coverage(pfc_synth, realised_synth, BlockMiddayWeekday(), ic_level=0.95)` doit raise `ValueError` ; assert message contient "IC95" ET "Phase 5ter" ET "uncertainty.py". Sanity check : aucun silent skip, l'API est explicite.
    - Optionnel `TestPillar3Integration` (1 test) : end-to-end utilisant Uncertainty(n_boot=50, seed=42) (n_boot réduit pour CI <60s) + build_one(with_uncertainty=True) + compute_pillar3_coverage(..., ic_level=0.80). Sanity : dict structure correcte, n > 0. Pas un strict gate KPI.
    Total : ≥9 tests Pillar 3 (5 unit LR + 3 compute_pillar3 incl. IC95 reject + 1 optional integration).
  </action>
  <verify>
    <automated>cd /Users/julienbattaglia/Desktop/PFC && python -m pytest tests/test_phase10_probabilistic.py -x -q --timeout=180 && python -c "from pfc_shaping.validation.christoffersen import lr_unconditional_coverage; r = lr_unconditional_coverage(x=20, n=100, p=0.20); assert r['p_value'] > 0.9, f'perfect coverage should not reject H0, got {r}'; print('OK', r)" && python -c "from pfc_shaping.validation.scorecard import compute_pillar3_coverage; print('import OK')" && python -m pytest tests/test_phase10_probabilistic.py::TestComputePillar3Coverage::test_compute_pillar3_coverage_rejects_ic95 -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pfc_shaping/validation/christoffersen.py` expose `lr_unconditional_coverage` ; signature et returns conformes au RESEARCH Pattern 3
    - `pfc_shaping/validation/scorecard.py` expose `compute_pillar3_coverage` (additif, n'a pas cassé Plan 10-02 exports)
    - `tests/test_phase10_probabilistic.py` : ≥9 tests PASS, dont les 3 cas perfect/under/over coverage + les 3 edge cases degenerate + test_compute_pillar3_coverage_rejects_ic95
    - **test_compute_pillar3_coverage_rejects_ic95 PASS** : `compute_pillar3_coverage(..., ic_level=0.95)` raise ValueError avec message mentionnant "IC95", "Phase 5ter", "uncertainty.py" (blocker #4 résolu — pas de silent skip)
    - lr_unconditional_coverage sur (x=20, n=100, p=0.20) retourne p_value > 0.9 (sanity perfect coverage)
    - Suite globale `pytest tests/` reste verte
    - Aucun crash sur edge cases n=0, x=0, x=n
  </acceptance_criteria>
  <done>Pillar 3 primitives en place (IC80 only). IC95 explicitement déférée Phase 5ter via guard ValueError + documentation. Plan 10-04 final assembly utilisera compute_pillar3_coverage(ic_level=0.80) sur Config 4 avec Uncertainty(n_boot=500) pour produire la table de coverage par (bloc).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 — Pillar 4 : dm_test.py (DM + HAC + 3 baselines) + tests DM</name>
  <files>pfc_shaping/validation/dm_test.py, pfc_shaping/validation/scorecard.py, tests/test_phase10_dm.py</files>
  <read_first>
    - .planning/phases/10-pfc-fmv-quality-scorecard/10-RESEARCH.md §Pattern 4 canonical (lignes 359-462) + §Common Pitfalls Pitfall 3 (var_d ≤ 0 edge) + §Don't Hand-Roll (statsmodels.tsa.stattools.acovf canonical)
    - .planning/phases/10-pfc-fmv-quality-scorecard/10-CONTEXT.md §D-A4-1..D-A4-3 (3 baselines spec : climatology, persistence_y1, forwards_flat_no_shape)
    - pfc_shaping/validation/scorecard.py (Plan 10-02 : ne pas casser existing)
    - pfc_shaping/validation/block_masks.py (BlockMask.apply)
  </read_first>
  <behavior>
    - `diebold_mariano(errors_a=Series_low_mae, errors_b=Series_high_mae, h=1, loss='mae')` (A clearly better) → dm_stat < -2 (mean_d négatif), p_value < 0.05
    - `diebold_mariano(errors_a=Series_same_distribution, errors_b=Series_same_distribution, h=1, loss='mae')` (equal accuracy) → |dm_stat| < 2, p_value > 0.05
    - `diebold_mariano(errors=arr_n3, errors=arr_n3, h=1)` (n<8 too few obs) → degenerate=True, p_value=NaN
    - `diebold_mariano(errors=long_series, h=24, loss='mae')` (Y+2 horizon, lag=23) → returns dict avec n_lags_hac=23, sample size suffisant pour Bartlett weighted
    - Edge case construit pour forcer var_d≤0 (rare mais documenté Pitfall 3) → fallback sample variance lag=0 OR degenerate=True, jamais de NaN propagation silencieuse
    - `baseline_climatology(epex_hist=Series_2019_2023, bloc=BlockMiddayWeekday())` retourne 1 scalar = mean(EPEX | bloc filter, 2019-2023)
    - `baseline_persistence_y1(epex_realised=Series_with_2024_2025, vintage_date=Timestamp("2024-06-30"), bloc=BlockMiddayWeekday(), window_days=15)` retourne 1 scalar = mean(EPEX | bloc filter, vintage-1yr ± 15j)
    - `baseline_forwards_flat(forwards_asof: dict, horizon_label: str)` retourne 1 scalar = forward Cal/Q/M correspondant à horizon (e.g. "M+1" → forward du mois next vintage)
    - `compute_pillar4_dm(pred_pfc: Series, pred_baseline: Series, realised: Series, bloc: BlockMask, h_months: int)` retourne dict {bloc, h_months, dm_stat, p_value, mae_pfc, mae_baseline, delta_mae, better_than_baseline_yn, degenerate}
  </behavior>
  <action>
    Créer `pfc_shaping/validation/dm_test.py` per RESEARCH §Pattern 4 canonical (lignes 359-462) :
    - Header docstring "Pillar 4 — Diebold-Mariano test + 3 naive baselines maison (climatology / persistence Y-1 / forwards-flat-no-shape). Maison ~30 lignes via statsmodels.tsa.stattools.acovf + HLN small-sample correction. Aucune dep arch ou dieboldmariano PyPI (slop risk)."
    - `from __future__ import annotations` ; imports numpy as np, pandas as pd, from scipy.stats import t as student_t, from statsmodels.tsa.stattools import acovf, logger
    - Fonction `diebold_mariano(errors_a: np.ndarray, errors_b: np.ndarray, h: int = 1, loss: str = "mae", hln_correction: bool = True) -> dict` per canonical impl :
      - Guard `if len(errors_a) != len(errors_b): raise ValueError(...)`
      - `n = len(errors_a)`
      - `if n < max(h + 1, 8): return {"dm_stat": NaN, "p_value": NaN, "n": n, "mean_d": NaN, "var_d": NaN, "n_lags_hac": h - 1, "degenerate": True}`
      - Loss differential : `d = np.abs(errors_a) - np.abs(errors_b)` si loss=="mae" ; `d = errors_a**2 - errors_b**2` si loss=="mse" ; sinon ValueError.
      - `d_mean = float(np.mean(d))` ; `n_lags = max(h - 1, 0)`
      - `gammas = acovf(d, nlag=n_lags, fft=True, demean=True)`
      - Bartlett weighted variance : si n_lags == 0 → `var_d = float(gammas[0])` ; sinon `weights = 1.0 - np.arange(1, n_lags + 1) / float(n_lags + 1) ; var_d = float(gammas[0] + 2.0 * np.sum(weights * gammas[1:]))`
      - Pitfall 3 edge case : if var_d ≤ 0 → fallback `var_d = float(gammas[0])` + log.warning("DM var_d ≤ 0 after Bartlett HAC, falling back to sample variance lag=0") ; if still ≤ 0 → return degenerate dict
      - `dm_stat = d_mean / np.sqrt(var_d / n)`
      - HLN correction si hln_correction=True : `adj = np.sqrt(max((n + 1 - 2*h + h*(h-1)/n) / n, 1e-12)) ; dm_stat *= adj ; p_value = float(2 * student_t.sf(abs(dm_stat), df=n - 1))` ; sinon `from scipy.stats import norm ; p_value = float(2 * norm.sf(abs(dm_stat)))`
      - Return dict {"dm_stat": float(dm_stat), "p_value": p_value, "n": int(n), "mean_d": d_mean, "var_d": var_d, "n_lags_hac": n_lags, "degenerate": False}

    Fonctions baselines (toutes deterministes) :
    - `baseline_climatology(epex_hist: pd.Series, bloc: BlockMask, years: tuple[int, int] = (2019, 2023)) -> float` : filtre `epex_hist.loc[(epex_hist.index.year >= years[0]) & (epex_hist.index.year <= years[1])]`, apply mask, return float(mean()).
    - `baseline_persistence_y1(epex_realised: pd.Series, vintage_date: pd.Timestamp, bloc: BlockMask, window_days: int = 15) -> float` : window = `[vintage_date - pd.DateOffset(years=1) - pd.Timedelta(days=window_days), vintage_date - pd.DateOffset(years=1) + pd.Timedelta(days=window_days)]`, filtrer epex_realised dans window, apply mask, return float(mean()) ; if window vide → return NaN (logger.warning).
    - `baseline_forwards_flat(forwards_asof: dict, horizon_label: str, vintage_date: pd.Timestamp) -> float` : map horizon_label → product_key : "M+1" → forward du mois `vintage_date + pd.DateOffset(months=1)` (format "YYYY-MM"), "M+3" idem +3, "M+6" idem +6, "Y+1" → forward Cal "YYYY", "Y+2" → "YYYY". Lookup priorité Month > Quarter > Year per assembler convention. Return float(forwards_asof[key]) ; if key absent → return NaN.

    Étendre `pfc_shaping/validation/scorecard.py` (additif) :
    `def compute_pillar4_dm(pred_pfc: pd.Series, pred_baseline: pd.Series | float, realised: pd.Series, bloc: BlockMask, h_months: int) -> dict` :
    - Si `pred_baseline` est un float (e.g. baseline_climatology), broadcast vers Series alignée sur realised.index.
    - Aligner pred_pfc, pred_baseline, realised via inner join.
    - Appliquer mask bloc.
    - Compute errors_a = realised - pred_pfc, errors_b = realised - pred_baseline (convention errors_a = PFC, errors_b = baseline ; mean_d négatif → PFC meilleur).
    - Compute mae_pfc = mean(|errors_a|), mae_baseline = mean(|errors_b|).
    - dm = diebold_mariano(errors_a.values, errors_b.values, h=h_months, loss="mae", hln_correction=True)
    - Return {**dm, "bloc": bloc.name, "h_months": h_months, "mae_pfc": float(mae_pfc), "mae_baseline": float(mae_baseline), "delta_mae": float(mae_pfc - mae_baseline), "better_than_baseline": "Y" if (dm["mean_d"] < 0 and dm["p_value"] < 0.05) else "N"}

    Créer `tests/test_phase10_dm.py` :
    - Classe `TestDieboldMariano` (5 tests) : test_A_clearly_better (errors_a small, errors_b big, h=1 → mean_d<0, p<0.05), test_equal_accuracy (errors_a, errors_b même distribution → |dm_stat|<2, p>0.05), test_n_too_few_degenerate (n=3 → degenerate=True), test_h_24_lag_23 (n=100, h=24 → n_lags_hac=23, no crash), test_var_d_negative_fallback (construire input qui force var_d<0 via input synthétique pathologique → fallback OR degenerate=True, never crash)
    - Classe `TestBaselines` (3 tests) : test_baseline_climatology_returns_mean (sanity), test_baseline_persistence_y1_window (vintage 2025-06-30, window ±15 days around 2024-06-30 → mean of bloc filter in that window), test_baseline_forwards_flat_horizon_mapping (M+1, M+3, Y+1, Y+2 all return correct lookup ; missing key → NaN + warning)
    - Classe `TestComputePillar4Dm` (1 test) : test_end_to_end_synth (3 baselines × 1 bloc × 1 horizon, vérifier dict structure + better_than_baseline logic)
    Total : ≥9 tests Pillar 4.
  </action>
  <verify>
    <automated>cd /Users/julienbattaglia/Desktop/PFC && python -m pytest tests/test_phase10_dm.py -x -q --timeout=120 && python -c "from pfc_shaping.validation.dm_test import diebold_mariano, baseline_climatology, baseline_persistence_y1, baseline_forwards_flat; import numpy as np; r = diebold_mariano(np.array([0.1]*20), np.array([2.0]*20), h=1); assert r['p_value'] < 0.05, f'A clearly better should reject H0, got {r}'; print('OK', r)" && python -c "from pfc_shaping.validation.scorecard import compute_pillar4_dm; print('import OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `pfc_shaping/validation/dm_test.py` expose `diebold_mariano` + 3 baselines (`baseline_climatology`, `baseline_persistence_y1`, `baseline_forwards_flat`)
    - `pfc_shaping/validation/scorecard.py` expose `compute_pillar4_dm` (additif Task 1)
    - `tests/test_phase10_dm.py` : ≥9 tests PASS, dont DM A-better/equal/degenerate + 3 baselines deterministic + var_d≤0 edge case + integration
    - DM sanity : `diebold_mariano(np.array([0.1]*20), np.array([2.0]*20), h=1)` retourne p_value < 0.05 (A clearly better)
    - Aucune dep externe ajoutée (zéro `pip install arch` ou `dieboldmariano`)
    - Suite globale `pytest tests/` reste verte (au moins +18 tests cumulé Plan 10-03)
    - Aucune référence à `pfc_shaping.ct.*`
  </acceptance_criteria>
  <done>Pillar 4 primitives en place. Plan 10-04 final assembly utilisera compute_pillar4_dm sur les 4 configs × 3 baselines × 5 horizons pour produire la table DM du scorecard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| statsmodels.tsa.stattools.acovf input/output | Calcul interne pure (numpy arrays in, numpy array out) ; nan-handling via demean=True ; pas de surface attaquable |
| log.warning sur Pitfall 3 var_d ≤ 0 fallback | Boundary : caller voit le warning + degenerate flag → décision en aval (scorecard cell marquée degenerate=True) |
| Persistence Y-1 window (vintage - 1yr ± 15 days) | Boundary : si fenêtre tombe sur date pré-2024 où epex_realised n'a pas de data → NaN propagation contrôlée |
| compute_pillar3_coverage ic_level= guard | Decision boundary : ic_level=0.95 raise ValueError explicite (pas de silent skip) — blocker #4 défense |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-01 | Tampering | DM test impl maison vs PyPI alt | mitigate | Maison ~30 lignes (auditable line-by-line), zéro dep PyPI risquée (RESEARCH §Standard Stack reject `arch` et `dieboldmariano==0.1.x`) |
| T-10-03-02 | Information disclosure | NaN propagation Christoffersen / DM | mitigate | Degenerate guards explicites (n=0, x∈{0,n}, n<max(h+1,8), var_d≤0) + flag `degenerate: True` dans tous les outputs, jamais de silent NaN |
| T-10-03-03 | Repudiation | DM lag choice h-1 not traced | mitigate | Docstring `diebold_mariano` cite DM 1995 §3.1 + Bartlett weights = R `forecast::dm.test` default ; n_lags_hac inclus dans output dict |
| T-10-03-04 | Denial of service | Pillar 3 integration test (n_boot=500) timeout CI | mitigate | Integration test optionnel utilise n_boot=50 + reduce vintages, timeout pytest=180s ; full uncertainty (n_boot=500) reste Plan 10-04 real-run only |
| T-10-03-05 | Repudiation | IC95 silent skip (warning #4 historique) | mitigate | Guard ValueError explicite dans compute_pillar3_coverage avec message mentionnant uncertainty.py + Phase 5ter ; test pytest dédié assert le raise |
</threat_model>

<verification>
- `pytest tests/test_phase10_probabilistic.py tests/test_phase10_dm.py -x` : tous verts (cible ≥9 + ≥9 = 18 tests)
- `pytest tests/` : suite globale reste verte (≥305 + 18 = ≥323 tests cumul Phase 10 Plans 01..03)
- `python -c "from pfc_shaping.validation.christoffersen import lr_unconditional_coverage; from pfc_shaping.validation.dm_test import diebold_mariano, baseline_climatology, baseline_persistence_y1, baseline_forwards_flat; from pfc_shaping.validation.scorecard import compute_pillar3_coverage, compute_pillar4_dm; print('all imports OK')"`
- `grep -c "from statsmodels.tsa.stattools import acovf\|acovf(" pfc_shaping/validation/dm_test.py` ≥ 1 (utilisation explicite)
- `grep -c "from scipy.stats import chi2\|chi2.sf" pfc_shaping/validation/christoffersen.py` ≥ 1
- `grep -E "from arch|from dieboldmariano" pfc_shaping/validation/dm_test.py` : 0 hits (zéro slop risk dep)
- `grep -c "degenerate" pfc_shaping/validation/dm_test.py pfc_shaping/validation/christoffersen.py` ≥ 4 (edge cases handled explicitement)
- `grep -E "IC95.*Phase 5ter|ic_level.*0\.95.*ValueError" pfc_shaping/validation/scorecard.py` ≥ 1 (blocker #4 defense explicite)
- `python -m pytest tests/test_phase10_probabilistic.py::TestComputePillar3Coverage::test_compute_pillar3_coverage_rejects_ic95 -x` PASS
</verification>

<success_criteria>
1. **Pillar 3 livré (IC80 only)** : `lr_unconditional_coverage` + `compute_pillar3_coverage(ic_level=0.80)` ; closed-form Christoffersen 1998 LR via scipy.stats.chi2
2. **IC95 explicitement déférée** : `compute_pillar3_coverage(ic_level=0.95)` raise ValueError avec message explicite ; aucun silent skip (blocker #4 résolu)
3. **Pillar 4 livré** : `diebold_mariano` + 3 baselines maison + `compute_pillar4_dm` ; ~30-line DM via statsmodels.tsa.stattools.acovf + HLN correction
4. **Edge cases handled** : degenerate flag dans tous les outputs (n=0, x∈{0,n}, n<max(h+1,8), var_d≤0), pas de log(0) ni de NaN silent
5. **Tests verts** : `pytest tests/test_phase10_probabilistic.py tests/test_phase10_dm.py` ≥18 tests PASS, incl. test_compute_pillar3_coverage_rejects_ic95
6. **Pas de régression** : suite globale `pytest tests/` reste verte
7. **Pas de dep externe risquée** : zéro `pip install arch` ou `dieboldmariano` (RESEARCH §Don't Hand-Roll respecté)
</success_criteria>

<output>
Create `.planning/phases/10-pfc-fmv-quality-scorecard/10-03-SUMMARY.md` à la complétion :
- Files créés/modifiés (counts + lignes)
- Verdict DM sanity (A clearly better test → p<0.05) + Christoffersen sanity (perfect coverage → p>0.9)
- **Verdict IC95 explicit defer** : test_compute_pillar3_coverage_rejects_ic95 PASS, ValueError message verbatim, mention Phase 5ter / uncertainty.py extension scope (blocker #4 audit-trail)
- Tests count delta (avant Plan 10-03 → après)
- Pré-autorisation pour Plan 10-04 : toutes les primitives 5 piliers prêtes pour final assembly (compute_pillar3_coverage IC80-only, compute_pillar4_dm + ceux de Plans 10-02 : run_scorecard_pillar_1, compute_cell_kpis)
- Compute budget réel mesuré : 1 build avec Uncertainty(n_boot=50) en mock CI ≈ X sec, projection avec n_boot=500 Plan 10-04 ≈ Y min sur 24 vintages
</output>
