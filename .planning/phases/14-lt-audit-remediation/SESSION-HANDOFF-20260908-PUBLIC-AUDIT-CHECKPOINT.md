# D315 — public audit checkpoint and national CH priority

The user explicitly requested a commit/push of the current state to GitHub for
a Claude audit, and continuation of national CH source qualification. This
authorizes publication of the reviewed code/tests/protocols to the existing
repository, not production promotion or release of customer portfolio details.

Audit entry: docs/model/PFC-CH-AUDIT-ENTRYPOINT-20260908.md.
Repository: https://github.com/JulienFMV/PFC (public).
Branch: fix/lt-audit-remediation.
Pre-checkpoint local HEAD: e4bfd457d3d37d654be54abfbf10d821640c1650.
Observed remote HEAD: d743d24947694cfe2c0f964af1eeb06e704c7ccf.
The local history has19unpublished commits before this checkpoint. The new
checkpoint includes reviewed accumulated LT source/test/protocol work.
Exact created commit and remote verification are recorded locally in
build/lt-national-readiness-20260908/ and reported to the user after push.

## Corrected priority

National Swiss PFC quality is primary. Client consumption/production budgets
are separate economic exposure data, not national load/generation proxies.
Their mapping/ownership gaps do not block national hourly model work. No client
file is requested for that work. BLOC13 remains outside the required roadmap.

D304 retained; D305–D307 and subsequent comparisons remain evidence. Solver
monthly levels, assembler, EEX projection, no per-month choices and all six
false authorities are invariant. GPU and bounded Warehouse start authorization
persist, but neither creates scientific or production authority.

## Scope and preservation

137modified/untracked files were inventoried before this step, plus206outgoing
history blobs in the preceding19commits. Basic credential-pattern screening
found no matching secrets. Review included allowed paths, text/binary size,
Git ignore rules and the actual public destination. Raw data, model artifacts,
build outputs, workbook and credentials are not staged or force-added.

Granular business-source notes have public summaries at the same document
paths. Public index content is prepared from repo-local copies; full local
notes remain byte-preserved. After commit those deliberate local additions
can remain modified relative to the public branch. Do not indiscriminately
stage/push them in a later session. Exact public/private file mapping is in
the local checkpoint manifest; no local data values are sent to an auditor.

New shared content: this handoff, the audit entry point, a D315 decision and
top-level handoff priority. No production/model change is made in this step.

## Verification and continuation

The checkpoint matrix runs all34changed/new test modules plus the minimum LT
modules already included in that count. Its XML and log are local under
build/lt-national-readiness-20260908/. The exact outcome is inserted into the
public checkpoint review record after the run, before commit. A checkpoint is
not a statement that every historical phase failure or external CI gate passed.

National work reuses D300 actual source receipts and existing availability/
materialization contracts. Next budget: at most6new metadataGETs,4SELECTs,
2001rows per statement,180seconds per statement, at most one Warehouse start
with600second bound and auto-stop≤45minutes. No client volume extraction,
AFRY,T057,CT change, model selection or monthly patch. Outputs go under the
fresh build/lt-national-readiness-20260908/ task root.

Canonical root and Git top were checked before every shell action. All mutable
runtime/cache/temp paths are below that task root; existing repo-local Python
is build/conda-runtime-v41-model-source/python.exe -B. No elevation, external
mutable path, project executable or browser runtime. No message is sent to
Claude; the user initiates the independent audit against the published commit.
