# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
dist/
build/
.eggs/
*.egg

# Virtual environment
.venv/
venv/
env/

# Data caches (Parquet stores locaux — ne pas versionner)
pfc_shaping/data/*.duckdb
pfc_shaping/data/*.duckdb.wal
pfc_shaping/data/*.parquet
pfc_shaping/data/raw/
pfc_shaping/data/raw/**/*.xlsx
# Phase 10 root-level data caches (EPEX 2019-2025 + forwards as-of-vintage)
data/*.parquet
data/AFRY_*.xlsx
/Price_Report_EEX.xlsx
data/_cache/
data/_h_cache/

# Phase 10 scorecard per-vintage PFC caches (96 builds × ~4 MB = ~400 MB local Mac Mini)
# Gitignored per T-10-04-06 — local Mac Mini only, regenerated via scripts/run_phase10_scorecard.py
.planning/phases/*/cache/
.planning/phases/*/run_phase10.log

# Model artifacts
pfc_shaping/model/artifacts/
*.safetensors

# Local research source copies (keep citations and summaries in Markdown)
docs/research/*.pdf

# Output PFC — keep latest for dashboard demo
pfc_shaping/output/*.csv
pfc_shaping/output/*.log

# Logs
pfc_shaping/logs/*.log

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Eval / autoresearch logs
eval.log
eval_lear.log
eval_stderr.log
prod_stderr.log
results.tsv
results_lear.tsv

# Runtime outputs (generated, never source)
pfc_shaping/output/*
!pfc_shaping/output/.gitkeep
output/*.parquet
output/*.csv
output/*.xlsx
output/*.png
output/**
output/hfc_diagnostics/

# Generated LT audit/report artifacts
.planning/phases/*/CH-HFC-*.md
.planning/phases/*/CH_HFC_*.md
.planning/phases/*/LOCAL_TEST_CH_PFC_*-BUILD.md
.planning/phases/*/LOCAL_TEST_CH_PFC_*-GOVERNANCE.md

# Secrets — NE JAMAIS COMMITTER
.env
*.token
AutogluonModels/

# GSD local install — machine-specific paths, regenerated per-machine via npx
.claude/

# Power BI QA project generated/runtime files
powerbi/data/
powerbi/**/.pbi/
powerbi/**/*.bak_*/
*.bak_*
