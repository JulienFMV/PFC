# PFC Shaping 15min — FMV SA
# Python >= 3.10

# Data & calcul
pandas==2.3.3
numpy==2.0.2
scipy==1.13.1
pyarrow==21.0.0      # Parquet I/O

# Machine Learning
scikit-learn==1.6.1  # HuberRegressor, LinearRegression
lightgbm==4.6.0

# Lissage gaussien
# (scipy.ndimage inclus dans scipy)

# SMARD.de API (Bundesnetzagentur) — source primaire prix EPEX
requests==2.32.5

# ENTSO-E Transparency API (fallback prix + source load/generation)
entsoe-py==0.7.11
python-dotenv==1.2.1

# Databricks SQL Connector (forwards EEX depuis EULER)
databricks-sql-connector>=3.0.0

# Calendrier fériés CH/DE
holidays==0.83

# Configuration
PyYAML==6.0.3
duckdb>=1.1.0

# Tests
pytest==8.4.2

# Optionnel : visualisation (backtest / diagnostic)
# matplotlib>=3.7.0  # Phase 10 (Plan 10-02 wiring) : scorecard figures PNG, human-verified Plan 10-01 Task 1
# statsmodels==0.14.6  # Phase 10 (Plan 10-02 wiring) : MZ regression + DM HAC variance (Plan 10-03), human-verified Plan 10-01 Task 1
# seaborn>=0.12.0

# Optionnel : planificateur (alternative à cron)
# APScheduler>=3.10.0
